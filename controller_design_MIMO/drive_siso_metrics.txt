SISO Youla-H DRIVE baseline — synthesis metrics (GENERATED by
controller_design_MIMO/synthesize_drive_siso.py; plan Phase 3 / §5 bullet 1)

── plant (plant_mimo.drive_plant, nominal OP/params) ──
states               = 4  (Pade(2) VESC delay + tau_v lag + 1st-order mechanics)
G22(0)               = 3.708521 (m/s)/A
mechanical pole      = -0.121922 rad/s  (near-integrator)
b_eff                = 0.359670 N*s/m   force/amp = 1.333843 N/A
NOTE: G22(0) = 3.709, NOT the ~30 anticipated in the task brief — b_eff is dominated
      by the wheel-referred motor free-run loss (0.3596 of 0.3719 N*s/m, VESC doc §12.3),
      not by aero.  The Youla-H correction is tiny either way (see below).

── weights (plan §3, drive channel; papers' philosophy, target bandwidth 24 rad/s) ──
Wp = strictly_proper_2nd_order_weight(dc = 10000, wc = 50 rad/s)
Wd = makeweight(0.5, 125, 40)
Wu = makeweight(0.2, 300, 10)

DEVIATION 1 (Wd break).  The papers' Wd break is 18 rad/s.  Placing the T-rolloff
weight BELOW the Wp corner makes Wp and Wd contend over the same decade, inflating
gamma and collapsing the achieved crossover.  Break set to 2.5x the Wp corner, which
preserves the papers' intent (T rolled off just above crossover).

DEVIATION 2 (Wp corner above the target).  On this plant the achieved S/T crossover
lands consistently BELOW the Wp corner (gamma_opt >> 1 throughout: the specs are being
shaped, not met).  Wp is therefore cornered at 50 rad/s to land the achieved crossover
near the papers' 24 rad/s.  Iteration ladder actually run (Wd break = 2.5*WC,
Wu = makeweight(dc, 300, hf)):
    WC=24, Wu(0.3,300,20) -> gamma_opt  9.14, wc 12.3, PM 56.6, worst-corner ||S|| 1.64
    WC=24, Wu(0.1,300, 5) -> gamma_opt  5.60, wc 15.2, PM 46
    WC=40, Wu(0.3,300,20) -> gamma_opt 13.25, wc 16.9, PM 54,   worst-corner ||S|| 1.89
    WC=50, Wu(0.2,300,10) -> gamma_opt 13.03, wc 20.7, PM 49,   worst-corner ||S|| 2.41  <- CHOSEN
    WC=60, Wu(0.1,300, 5) -> wc 24.4 but PM 42 (fails the >45 gate), ||Y||inf 80 A/(m/s)
WC = 50 is the most aggressive setting that still clears PM > 45 deg and keeps the
worst-corner ||S||inf under the 2.5 target.

── H-inf (DGKF, hinf_mimo.hinfsyn_dgkf on AugPlantMIMO 1x1 blocks) ──
gamma_opt            = 13.0341
gamma_used           = 13.6858
||Tzw||inf           = 13.6629
H-inf controller ord = 8

── Youla-H DC correction (papers' scalar recipe) ──
T_H(0)               = 0.9986363198
gain scale 1/T_H(0)  = 1.0013655424   (|1 - 1/T_H(0)| = 1.366e-03)
Gc_YH raw order      = 24

── reduction / realization ──
kI                   = 50.534066986   (sign gate: positive v-error -> positive current)
remainder order      = 23 -> 4
HSV (first 8)        = [1.6611e+02 2.1228e+01 3.4935e+00 6.2828e-03 4.4320e-04 3.1870e-04 5.7196e-05 3.0002e-06]
reduced-vs-full relerr = 2.610e-03   (gate < 2e-2)
T(0) reduced         = 1.000000000000   (gate |T(0)-1| < 1e-9)

── loop shape / margins (nominal, reduced controller) ──
S/T crossover        = 20.75 rad/s   (H-inf full order: 20.75)
||S||inf nominal     = 1.3851
||T||inf nominal     = 1.3642
phase margin         = 48.9 deg at 20.75 rad/s
delay margin         = 41.12 ms   (gate > 4 ms = 2*Ts)
gain margin          = 18.3 dB

── robustness ──
corner family        = 24 drive_corners() x v0 in (0.5, 2.0, 5.0) = 72 plants
                       (the drive plant depends on the OP ONLY through b_eff(v0);
                        op_grid() holds v0 = 2.0 fixed, so sweeping it would repeat
                        the same plant 10x — v0 is swept instead)
continuous unstable  = 0
worst ||S||inf cont. = 2.4074 at ({'K_v': 2.0, 'pole_factor': 0.5, 'tau_v': 0.005, 'Td_v': 0.004}, 0.5)
discrete max |z|     = 0.9998
worst ||S||inf disc. = 2.5879 at ({'K_v': 2.0, 'pole_factor': 0.5, 'tau_v': 0.005, 'Td_v': 0.004}, 0.5)

── discretization ──
Ts                   = 2.0 ms (500 Hz motor channel)
SOS sections         = 2  (discrete remainder order 4)
clamp                = +-20.0 A

── anti-windup (DEVIATION 3, forced by a measured property of this design) ──
Gs_red(0)            = 367.7 A/(m/s)  -- the NON-INTEGRAL branch alone saturates the
                       +-20 A actuator for |e| > 54.4 mm/s, so its lag states wind up
                       independently of the integrator.
shipped scheme       = integrator-only back-calculation (share_controller.h pattern).
                       Measured on the 0->2 m/s step: standing error -0.101 m/s and a
                       6.3 mm/s limit cycle -> FAILS the sim gates.
AW boundary re-run at the revised +-20 A clamp (2026-08-04).  e_sat scales with the
clamp, so quadrupling the actuator quadrupled it (54.4 mm/s, was 13.6 mm/s at
+-5 A).  Step-amplitude scan of the SHIPPED integrator-only scheme (pass = final
error < 1e-4 m/s AND tail p-p < 1e-5 m/s):
    step  0.02 m/s: final err -3.780e-09 m/s, tail p-p 1.77e-10 m/s -> OK
    step  0.05 m/s: final err -9.450e-09 m/s, tail p-p 4.41e-10 m/s -> OK
    step  0.10 m/s: final err -1.890e-08 m/s, tail p-p 8.83e-10 m/s -> OK
    step  0.20 m/s: final err -3.780e-08 m/s, tail p-p 1.77e-09 m/s -> OK
    step  0.50 m/s: final err -8.309e-04 m/s, tail p-p 5.20e-05 m/s -> FAILS
    step  1.00 m/s: final err -2.800e-02 m/s, tail p-p 1.75e-03 m/s -> FAILS
    step  2.00 m/s: final err -1.009e-01 m/s, tail p-p 6.31e-03 m/s -> FAILS
VERDICT: integrator-only AW is now CLEAN up to ~0.2 m/s steps and only breaks
from ~0.5 m/s.  At +-5 A the boundary sat an order of magnitude lower, so the
scheme failed essentially every useful transient.  It STILL fails the 0->2 m/s gate,
so the Hanus form remains REQUIRED for this baseline -- the honest restatement is
"needed for large transients", not "needed always".
scheme used          = Hanus self-conditioning on the 5-state discrete realization:
                         u_unsat = Cd x + Dd e ; u = clamp(u_unsat)
                         x[k+1]  = (Ad - Bd Cd/Dd) x + Bd u/Dd,  Dd = 1.035961461e+00
                       Exactly equivalent to the biquad+integrator form when unsaturated
                       (gated: max |diff| = 6.32e-10 A over an 100-sample sequence).
Teensy cost note     = this baseline needs a 5-state state-space realization, NOT the
                       shipped biquad cascade.  Phase-6 implementation-cost datapoint.

── time-domain (discrete, clamped, Hanus-conditioned) ──
step 0->2 m/s : peak i = 20.000 A, 2% settle = 0.452 s,
                overshoot = 14.8 %, final err = -3.81e-07 m/s,
                tail p-p = 1.77e-08 m/s (no limit cycle)
                NOTE: the step is ACTUATOR-LIMITED -- +-20 A gives a_max = F/m =
                9.043 m/s^2, so 0->2 m/s cannot physically take less than
                ~0.22 s.  The 20.7 rad/s design bandwidth is a SMALL-SIGNAL
                spec; this is a large-signal event.  At the revised clamp the rail is
                touched only briefly (202 ms) -- at +-5 A the loop sat on the rail
                for most of the transient instead.
regen 2->0 m/s: peak i = -20.000 A, on the -20 A rail 202 ms,
                2% settle = 0.452 s, final v = 3.81e-07 m/s,
                reverse excursion = -0.29582 m/s, tail p-p = 1.77e-08 m/s
                WINDUP GATE REDEFINED at +-20 A: the old ABSOLUTE bound (excursion
                < 0.10 m/s) was only meetable because +-5 A rail-limited the transient
                and suppressed the loop's own linear undershoot.  The undershoot now
                measured (-0.2958 m/s = 14.8 %) equals the step's 14.8 % overshoot and is
                loop shaping, not windup.  The gate now compares the SATURATED event
                against the SAME event with the clamp removed (linear undershoot
                -0.5702 m/s): saturation must add no excursion.  Measured excess
                = -0.2744 m/s (negative = saturation REDUCED the excursion).

── artifacts ──
drive_siso_coeffs.h        (shipped biquad format, DRIVE_CTRL_ prefix, NOT built)
figures/drive_siso_step.csv
