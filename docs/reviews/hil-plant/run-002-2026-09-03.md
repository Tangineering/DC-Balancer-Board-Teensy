# hil-plant review — run 002 (2026-09-03)

Target: `docs/HIL_PLANT.md` at commit `207a186` (3296 lines). Trigger: the operator's standing rule
that a plant physics change is reviewed after one campaign has measured it; three campaigns (D
`hil_report_20260902_220604`, E `hil_report_20260903_031220`, F `hil_report_20260903_063659`) have
run since run 001. Operator instruction: go over this review before continuing the tooling work.

## Focus (from the arguments)

1. Per-node bleed (§4.8, §4.2): `TODO(calibrate)` values from an operator recollection; the latch-shift
   prediction (+1.5 s) against the measurement (+2.62 s); the anchors that moved.
2. The structural loss map behind the DP bound (§9.4, §9.4.3): runs sit −0.09 to −0.80 % below their
   matched bound; the "not a policy result" boundary.
3. The regen model and the RegenManager two-level release (§3.4, §3.6, §4.4): the residual 18–20 ms
   FC_CHARGE handoff; the realizable fraction 0.63 vs 0.707.
4. The switching-ring estimator (§8.5): fixed 1.95 V load-dump term vs the physical i·√(L/C) ring;
   the 0.5 A verdict gate.
5. The fiat MPPT mirror (§4.6): frozen carry across unpowered spans; the cruise window moved to 29.1 s.
6. The governor model's asymmetry law (§4.4a, `governor_model.py`): the dV0 term without the droop-slope
   term (`ASYM_DROOP_SCALE_FC` 0.9434); the board's converged ratio 0.476 vs the model's 0.491 at
   delivered share 0.50.
7. The campaign measurements as physics validation (§11): the fw v26 step-transient limit measured
   through the plant; the single-source bus law and the unmodelled 24–45 ms cut deferral.
8. Internal consistency after the bleed change (stale 2 kΩ / 0.94 s / band figures; §-numbering).

## Round 1 — Codex (task-class, read-only)

Prompt: scratchpad `plant_r2_prompt.md` (composed per `codex:gpt-5-4-prompting`; adversarial stance;
authority order firmware → plant code → datasheets → bench record → controller model → ledgers;
ledger digest with the re-raise rule; severity contract; grounding rules). Launched 2026-09-03.

Thread ready: `01a0687b-2c7b-7701-aaad-152616d24930`. (verbatim findings appended on completion)

### Round-1 findings (verbatim, Codex thread 01a0687b-2c7b-7701-aaad-152616d24930)

VERDICT: No-ship: the document is candid about several bleed-era caveats, but still presents model-generated outcomes and impossible HIL witnesses as board physics.

1. **Major — vacuous validation can certify an unexecuted MPPT manager.** `docs/HIL_PLANT.md:1795` calls `mppt_thresh_cnt` “positive evidence the manager ran.” Under HIL, it did not: `teensy_controller/teensy_controller.ino:11823-11842` explicitly bypasses `ag105ManageMpptThreshold()` and sets the count from instantaneous `V_chg`; the real manager samples only FC-charge power, excluding regen (`:11465-11468`). The count also survives an unpowered span (`:11405-11411`). New evidence reopens settled PLANT-R1-F1: campaign D recorded 849 cruise ticks carrying a regen-derived frozen 27 (`HIL Results/hil_report_20260902_220604/HIL_FINDINGS.md:160-176,479`); E merely moves the window to hide it (`…031220…:144`). At 18.0878 V, `floor((18.0878−3−11)/0.088)=46`, clamped to 27—an output the real manager cannot derive from that regen window. Replace the scenario claim with “mirror-only byte exercise”; exclude unpowered/settling/regen history from its assertions and add a bench-manager test.

2. **Major — the bleed-era “measurements” are circular plant outputs, not hardware validation.** `docs/HIL_PLANT.md:1677-1681` says “what it actually measured” and “Every prediction … confirmed,” while `:1637-1641` correctly says both resistances remain `TODO(calibrate)`. The HIL boundary injects engineering-unit rails and bypasses the analog/power hardware (`:71-78`); `tools/hil_electrical.py:448-449` hard-codes 30 kΩ/60 kΩ. Thus the reported 92% V-MOT retention is predetermined: `τ=60,000·470 µF=28.2 s`, and `exp(−2.323/28.2)=0.9209`. It cannot validate the 60 kΩ value. The +2.6174 s latch shift does disprove the document’s +1.5 s prediction by 74.5% (or makes it 70% optimistic), as the document admits at `:1688`, but only within the changed model. Re-label node-retention, clamp-dwell, and warm-reclose numbers as *simulated plant predictions whose firmware response was observed*. Do a dark-node scope/SD decay capture before treating them as board anchors.

3. **Major — the governor omits one half of the adopted asymmetry fit, invalidating walk-derived MDAC pins and weakening the DP separability claim.** `docs/HIL_PLANT.md:1080-1084` says ΔV0 and FC droop scale are one fit that “must move together,” but `tools/governor_model.py:624-658` implements only ΔV0. The electrical plant applies both (`tools/hil_electrical.py:1826-1837`). With scale \(s=0.9434\), the effective parallel resistance is \(R_{eq}/K=s/[s(1-r)+r]\): 0.9881 at \(r=0.20\), 0.9520 at 0.84—a 3.61%-of-\(K\) swing; with \(K_Gg_{par}=1.95079·0.148922=0.29052\,Ω\), that is about 10.5 mΩ, or 15.7 mV at 1.5 A. Campaign F measured the exposure: correct delivered 0.50 share but +3.06% MDAC error, rising to +10.44% at 0.20 (`…063659…:80-86`). Add the scale to `_delivered_share()`, `_ratio_for_delivered()`, and code inversion; then rederive walks, MDAC pins, MPC, and the loss-map bound.

4. **Minor — the claimed implicit billing of FC_BUS/BT_BUS forward drops is false in the source model.** `docs/HIL_PLANT.md:1725-1730` says the drops are billed through `r_fix`. But `tools/hil_electrical.py:2003-2015` calls `_source_current(i, v_src, v[N_BUS])`, and `:2338-2342` uses `v_out=v_bus`, never `N_OFC`/`N_OBT` or the switch’s forward drop. A 1 A conducting link therefore omits at least \(1·35\text{ mV}=35\text{ mW}\), plus \(i^2(R_{ON}+R_{SHUNT})\), from source draw. `R_FIX` changes the DP bus-voltage law; it cannot make `_source_current()` charge the source for a voltage it never receives. This is roughly 0.22% at 16 W—material beside quoted ±0.03–0.31% bound agreement. Refer source current to each boost-output node, recalibrate hydrogen/DP tables, or explicitly declare the loss intentionally unbilled.

5. **Major — the switching-ring gate suppresses a known false verdict; it is not physics.** `docs/HIL_PLANT.md:2316-2325` still describes `over_absmax` as a boost-death signature, while `tools/hil_electrical.py:1545-1573` adds a fixed 1.95 V for every cut over 50 mA and merely gates the verdict at 0.5 A. The gate is a firmware share-cut threshold, not an LC parameter. For the documented 65 mA event, \(i\sqrt{L/C}=0.065\sqrt{1.5\text{ nH}/10\text{ µF}}=0.796\text{ mV}\), versus 1.95 V: 2,450× too large. Even at the new 0.5 A gate, the energy ceiling is only 6.12 mV, still 319× smaller. All three historical abs-max events were this false 65 mA class (`…220604…:333-337`). Replace the event model with an energy/damping bound using measured \(L,C,ESR\) and switch waveform; until then, mark both `peak_v` and safety verdicts as non-certifying, not “boost-death” evidence.

6. **Minor — “lower-bound reference” is false for the published run metric, despite a later honest caveat.** `docs/HIL_PLANT.md:2749-2756` introduces the DP as a lower bound, but the bleed-era comparison reports causal runs **below** it by −0.09% to −0.80% (`:2924-2938`). A lower bound exists only for the DP’s DC-\(G_{fc}\) stage-cost model; it is not a lower bound on logged dynamic-integrator `h2_cum_g`. The document eventually states this correctly, including the sign and ~0.8% magnitude, but too late for a reader who encounters the benchmark/table first. Rename it at first mention to “offline model optimum/reference,” add the signed ±0.8% boundary in each results table caption, and prohibit frontier/policy claims inside that interval.

7. **Minor — the fidelity envelope overstates what the campaigns validate about transient limits and single-source planning.** `docs/HIL_PLANT.md:3124-3135` says fault/control logic is trustworthy because the stimulus is exact. It is exact only relative to a chosen plant waveform: the boost current surrogate has `TAU_R=100 µs` (`tools/hil_electrical.py:1621-1627`), not a measured board current transient. The fw-v26 clamp’s 25.6% EMA under-read versus 12% headroom (`…031220…:78-81`) is therefore a firmware response to that HIL waveform; whether it is conservative or optimistic on hardware is unverified. Likewise, the 1.9453/2.0579 “measured” single-source laws are explicitly measurements of the hi-fi engine (`tools/hil_plant_sim.py:4322-4342`), while F shows the board/firmware defers a commanded cut 24–45 ms and the planner charges the single-source stage immediately, producing a +0.18% economics error (`…063659…:100-114`). Scope these as HIL-model results and bench-measure current slew and cut timing before using them as safety or MPC-validation evidence.

8. **Minor — several current-state statements are stale or internally conflicting.** `docs/HIL_PLANT.md:589` says FTP75c “validates the regen model end to end,” and `:645` says no campaign has run it; the later bleed record instead reports 0.63 realizable regen versus 0.707 modelled (`:1717-1723`). Also `:2794` still states the DP grid is [0.25, 0.75], while the implementation declares [0.15, 0.85] (`tools/gen_dp_ems_table.py:365-401`) and the document corrects itself only in misplaced `§9.4.3` after §11 (`docs/HIL_PLANT.md:3181-3189`). A reader can therefore reproduce the wrong control set or overstate regen fidelity. Date-scope the old §3.6 claims, point it to §4.8’s post-campaign result, update §9.4’s active text, and move/renumber §9.4.3 under §9.4.

Claims checked and found sound:

- `docs/HIL_PLANT.md:564-581` correctly says the FC-charge/regen handoff is bounded, not closed, and that walk regen duty is an upper bound.
- `:1637-1641` and `:1688` honestly retain the bleed values as uncalibrated and disclose the latch-shift prediction failure.
- `:2924-2938` does state the below-bound sign and ~0.8% magnitude honestly once reached.
- The signed, output-referred regen credit and FC-charge/regen exclusivity agree with `tools/gen_dp_ems_table.py:847-853,933-938`.
- Remaining 2 kΩ and 0.94 s references I checked are explicitly historical/reversal context, not current parameter claims.

## Round 1 verification (finding-verifier agents; severity assigned here, not by Codex)

### PLANT-R2-F1 (fable) — PARTIAL, minor
The only confirmable element is doc drift: `HIL_PLANT.md:1795` still says the `mppt_thresh_cnt`
column "is the positive evidence the manager ran" — the run-001 fix landed at :1297-1320, :1613 and
:1746 but missed the scenario row. Everything else is PLANT-R1-F1 re-observed (the mirror's regen
bypass, 27 at 18.08 V, "manager never called" — verbatim in run 001; the across-gap carry was
documented in the suite banner BEFORE campaign D at `run_hil_suite.py:5450-5453`), so the re-raise
rule is not met. "E moves the window to hide it" is refuted: the 29.1 s edge is derived
(28.1 + 0.30 lead-in + 0.5 settle + 0.2 pad; D's first mirror-live tick 28.9494 s) and the artefact is
PINNED (`mppt_threshold_braking_mirror_artifact*` requires the count to reach 27 for ≥ 600 ticks, "so
the day the mirror is taught the regen exclusion, this entry fails"). Quantified: the mirror gives
floor(4.0878/0.088) = 46 → clamped 27; the hardware manager on the FC-only cruise (windowed min
≈ 14.3-14.45 V) gives 15; 27 needs a windowed-min V_chg ≥ 16.376 V on the FC path, below the ~15.95 V
bus less the RT1987 drop — unreachable, as run 001 recorded. Minimal fix: one sentence at :1795.
Adjacent: `HIL_SCENARIOS.md:333` same phrasing; `HIL_MODE.md:397` implies the manager acts under HIL;
three stale `.ino` line citations in `run_hil_suite.py` (5422/5467/5452); the bench suite has NO
test with `REGEN_ENABLE` high during the manager's OBSERVE (regen exclusion at `.ino:11533` unpinned)
— a legitimate test follow-on.

### PLANT-R2-F6 (sonnet) — PARTIAL, minor
The semantic core is right (a lower bound on the DC-Gfc stage cost, not on the logged dynamic
`h2_cum_g`; runs land below it: `ems-ftp75-mpc` 0.99882, `ems-ftp75-sdp` 0.99916 on campaign D's
table, inside the ±0.8 % band). The "too late for a reader" framing overstates: the first mention
(:2745-2757) already warns "never as 'the run beat the optimum'", and `MATCHED_DP_GFC_NOTE` is appended
UNCONDITIONALLY to every generated `EMS_COMPARISON.md` (`hil_report_analysis.py:1685-1688`, :2041;
test :2372) in the same subsection as the table. What is absent: the ±0.8 % NUMBER in the note and the
table caption (`hil_ems_comparison.py:698-699`), and the unqualified "lower-bound leg" label (:677).
No thesis `.tex` cites the DP bound yet. Minimal fix: rename at :2749 to "offline model-optimum
reference (a lower bound on the Gfc DC-gain stage cost, not on the logged h2)"; carry the magnitude in
`MATCHED_DP_GFC_NOTE` and the Table caption; rename the "lower-bound leg" label; update the note's
test. Adjacent: do not conflate this bias with the separate, fixed "run beat its bound" incident of the
narrow grid (:3200-3204).

### PLANT-R2-F2 (fable) — PARTIAL, minor (the core charge CONFIRMED and worse than stated; the "claims the values validated" arm REFUTED)
§4.8's campaign-D figures are plant outputs, not hardware measurements — confirmed. Worse: the two
headline retention numbers are not even the plant's own output. "τ 0.94 → 28.2 s, 92 %" is a closed
form on 470 µF, but the plant stamps N_MOT at 470 µF + 0.5 mF VESC = 970 µF: τ = 58.2 s (log-linear fit
on the comm-loss trace over 2101 samples: 58.1995 s), retention 0.9515 across the 2.323 s teardown
(0.9629 over the exponential span; a −0.183 V `reverse_block` step at the latch); the pre-era τ was
1.94 s, not 0.94 (0.94 is the simple engine's 2 kΩ × 470 µF). "65 of 65" is a B/C carry-over (D: 70 of
70). The +1.5 s latch-shift figure had no written derivation (commit 82edd3c); recomputed on the plant's
constants at the scenario's 14.377 V bus and 33.786 W draw: removed bleed 0.0965 W (bus) + 0.0999 W per
other node; TWO pack-fed nodes → 0.578 % → +1.49 s (= the published +1.5 s: the estimate omitted a
node); THREE (N_OBT, N_BUS, N_MOT with MOT_PWR closed) → 0.869 % → +2.24 s; measured +2.6174 s
(1.015 %); the +0.38 s residual is the lower sag term at the state-condition latch. So "the latch-shift
model is ~70 % optimistic" dignifies an under-counted hand estimate as a model. REFUTED: the document
never claims the 30/60 kΩ VALUES were validated (`TODO(calibrate)` at :1637 and :1675, "a second move
is expected after the bench decay capture"). Minimal fix (doc-only, five sites): §4.8 header → "the
campaign compared the offline walk against the hi-fi engine and recorded the firmware's response";
column "measured" → "plant output (firmware response observed)" with a per-row classification; :1688
→ the three-node arithmetic; :1699-1703 → "95 % (τ 1.94 → 58.2 s on the 970 µF node)"; same at
`run_hil_suite.py:545-546`, the D ledger :318, D's HIL_SUMMARY headline, and the CLAUDE.md 2026-09-03
addendum. Adjacent: the τ figures have mixed node capacitances across engines since before this round;
the comm-loss trace reports V_bus 0.0000 one tick after the latch while N_MOT holds 15.78 V (C_VBUS τ
should be ~1 s — report-side zeroing or engine behaviour, unchecked); bleed-power statements "at 16 V"
over-bill the 14.377 V soc-depletion bus by 24 %.

### PLANT-R2-F8 (sonnet) — CONFIRMED, minor
All four items verified: :589 ("validates the regen model end to end", pre-campaign) vs :1717-1723
(measured 0.63 vs 0.707 = 89 % realization); :643-651 "No campaign has run any of this" while §4.8 records
the campaign that ran the ftp75c legs; :2794 "The share control grid is [0.25, 0.75]" stated as current
with no forward pointer; §9.4.3 (:3181-3295, actively maintained through 2026-09-03) placed after §11.
The code is correct (`gen_dp_ems_table.py:363-401`, [0.15, 0.85], n_share 57), so the exposure is a
reader citing a "current" figure from prose. The other candidate-stale strings (2 kΩ at :1634/1648/1669,
the (0.25, 0.75) at :3232) are correctly scoped history/reversal text. Minimal fix table: date-scope :589
and point to §4.8; prepend a SUPERSEDED banner at :643; rewrite :2794 as "was ... until 2026-09-02; now
[0.15, 0.85], see §9.4.3"; MOVE §9.4.3 to after §9.4.2 (before §10, line 3074). Adjacent: :1798's
"[0.25, 0.75]" is the minority-current clip band, not the grid — do not sweep-edit it.

### PLANT-R2-F4 (opus) — PARTIAL, minor (code claim CONFIRMED; impact claim REFUTED)
`_source_current` refers both stack currents at `v[N_BUS]` (`hil_electrical.py:2011-2012`, :2337-2342);
the boost-link drop (`RT_V_FWD` 0.035 + i·(0.021 + 0.002)) is charged to NO source, and the sentence
"billed implicitly through the bus law's r_fix" is false as an energy statement — appearing in the
bus law LOWERS V_bus and hence the bill; the fixed part sits in V0_EFF (`DP_BUS_V0_EFF` 15.871722 =
`V0_NOLOAD` 15.906716 − 0.035 = 15.871716, agreement 6e-6 V), the resistive part in R_FIX (0.017986 Ω
vs the two-link parallel 0.0165 Ω). Executed probe: under-bill 0.250 % at 0.19 A/channel, 0.275 % at
0.34, 0.318 % at 0.60, 0.362 % at 0.86 (Codex's 0.22 % is the rt_v_fwd-only floor). REFUTED impact: the
DP demand model, `ems_walk` and `mpc_ems` all price the same N_BUS boundary (`dp_loss_map_20260902.md`
§1 already says so), so the omission cancels in every table-vs-run deviation; it biases the ABSOLUTE
level of `h2_cum_g` and pack drain by 0.25-0.36 %, inside the declared absolute-scale caveat. The
share-dependent residue is the i² part only (rt_v_fwd is linear in the total): 0.087 % of source power
between an even split and single-source at 1.15 A, in favour of single-source — the same order and
sign as the low end of the reported `ems-mpc-single` gain (caveat wherever that gain is quoted).
Minimal fix: rewrite :1725-1731 (text supplied in the verifier report); optional pin
`abs(DP_BUS_V0_EFF − (V0_NOLOAD − RT_V_FWD)) < 1e-4`. Codex's "refer source current to the boost-output
node" REJECTED as non-minimal (moves every h2 anchor ~0.3 %, breaks the bleed-era pins, opens a
plant-vs-DP gap unless the loss map is re-fitted in lockstep). Adjacent: `p_bal_w` item 4 bills the
MOT_PWR drop but the two boost-link drops have no item.

### PLANT-R2-F5 (opus) — PARTIAL, minor (doc claim CONFIRMED; Codex's physics REFUTED; a NEW defect: the repo's own proposed replacement model is the wrong loop)
The unhedged ":2319 `over_absmax: true` is the boost-death signature" stands as written — confirmed. The
census is confirmed exactly: 3 `over_absmax` events in the whole corpus (1028 `sw_ring` events, not
803; 314 in the ≥ 0.5 A class, ZERO of which raise the verdict), all the 65 mA regen opens. REFUTED:
the bench record's Death-5 mechanism is NOT a node LC ring but the BOOST's output-cap hot loop
(`boost-bringup-debug.md:1540, 1557`), V = L_hotloop·di/dt with di/dt "backed out of FC's survival"
at the ~15 A cycle-limit commutation (:1572-1573: 4 nH × 1.3 A/ns ≈ 5.2 V on BT → 22.7 V → death; the
FC's 1.5 nH × 1.3 A/ns = 1.95 V IS the record's "≲ 2 V under abs-max"). The record ALSO says the ring
scales with the commutated current (:195), so `hil_electrical.py:644-646` ("no scaling law documented
anywhere in this repo") is false. Current-consistent form: peak = v_node + (1.95/15)·i_cut = 0.130 V/A
(t_off 11.5 ns): 8.5 mV at 65 mA, 65 mV at 0.5 A, 0.829 V at the 6.38 A scp-inrush cut. Codex's
i·√(L/C) (0.796 mV / 6.12 mV / 78 mV) is the node's characteristic-impedance ring in a DIFFERENT loop
and understates the recorded mechanism 10.6× at 6.38 A — and three repo sites (`HIL_PLANT.md:2350-2352`,
`run_hil_suite.py:1159-1160`, the `hil_electrical.py` header) name it as "the physically correct fix":
the deferred fix is itself the wrong model (new defect). Verdict invariance: under the fixed, the
scaled and the LC form, all 1028 events give 0 → 0 → 0 verdicts; worst class member `ems-sdp-braking`
BT_BUS 0.7438 A at 15.5413 V (fixed 17.49 V); scp-inrush MOT_PWR 6.3797 A at v_node 1.5453 V (deep in
soft-start). No exposure through the 0.5 A gate: a masked kill needs a ≥ 0.5 A cut at v_node > 18.05 V;
the class maximum is 15.5413 V (2.51 V margin); only the chopper-clamped node reaches 18.05 V and every
cut there is 65-79 mA. Minimal fix: reword :2319 ("a bound, not a computed ring; non-certifying");
replace the three "i·√(L/C)" sites with the hot-loop, current-scaled form and its provenance; correct
the :644-646 comment; the gate stands. Adjacent: the estimator stamps the boost hot-loop L on the
RT1987 node — say so in §8.5; the "0.66 A largest non-teardown cut" claim holds (bleed-era 0.5809 A).

### PLANT-R2-F7 (fable) — PARTIAL, minor (scoping point real but narrower; Codex's TAU_R mechanism REFUTED; single-source-law half REFUTED)
§11.1's bullets are firmware-LOGIC claims and true as written; what is missing is a statement that
the fw v26 transient figures at :1055-1072 (25.6 % under-read, the 4.3-vs-25-tick race, the −0.191 A
"plant lag") are conditioned on the plant's load-rise and split realization. REFUTED: `TAU_R` 100 µs
is NOT in the share-step path — `hil_electrical.py:1702` sets `r_droop = RE_MAX·g_code·droop_scale`
algebraically in the code; `TAU_R` acts only on the constant no-load target (:1724-1725). Sensitivity
even if it did: 0.012 A of a 0.239 A error (5 %); with `DT_SUB_MAX` 50 µs the engine re-splits within one
1 ms tick regardless. Direction bracket: the engine's algebraic re-split is the ZERO-LAG limit, so for a
given reference trajectory it delivers the MAXIMUM I_fc; a real converter (τ_r 7-25 µs predicted in
`system_model.md` §6e, 100 µs `TODO(calibrate)`, bench step test undone) delivers ≤ that; the only
component where hardware is faster is the ~1-tick HIL frame round trip (≤ 0.07 A at 70.5 A/s). Real
peak on the E stimulus brackets [≈ 1.49, ≤ 1.68] A; OC_FC latches on a single raw sample at 1.40 A
in every case — the suite's outcome is plant-invariant. Race recomputed: 1.647 A is plant-free algebra;
reference clock 4.67 ticks (s_prev from the CSV); filter clock 24.9 ticks — the "5.8" is a
stimulus-and-plant number through I_new. The single-source law: the doc already says "Probing the
hi-fi engine" (:3256-3262); the cut deferral is campaign F's ledgered F2, absent from §9.4.3's
"Plant-side consequence" paragraph (:3290). Minimal fix: three wording changes (a "load-rise rate and
delivered-split trajectory during a transient" exclusion in §11.1; :1057 "against the hi-fi plant; the
delivered peak is an upper bound for a lagging converter"; a §11.2 row for the bench step test for τ_r
+ a `W 4.0 0.15` joint-step run to place the board inside the bracket). Adjacent (real): the race
formula as PRINTED in `fw26_current_ceiling_governor.md:456-457` and the E ledger has a spurious
leading "1 −" (evaluates to 6.4 ticks, not the 25 quoted); correct form
ln((I_new − I_new·CEIL/LIMIT)/(I_new − I_old))/ln(1 − α) = 24.9. `system_model.md:374` τ_r 100 µs vs
§6e's 7-25 µs — quote one bracket.

### PLANT-R2-F3 (opus) — PARTIAL, MAJOR (low end): mechanism CONFIRMED and reproduces the board to ≤ 0.05 %; the DP-bound consequence REFUTED; Codex's fix INSUFFICIENT
`governor_model.py:638` carries only the ΔV0 term. The correct static split law with both fit
parameters AND the 0.033 Ω common series floor (`DROOP_FIXED_SERIES_OHM` = 0.010 + RT_R_ON + R_SHUNT):
R_FC = ρ·k_d/r + R_f, R_BT = k_d/(1−r) + R_f, α = (ΔV0/I_tot + R_BT)/(R_FC + R_BT). Against the
campaign-F board windows: forward error ≤ 1e-4 on all nine converged windows; inverted, r = 0.4758 /
0.3755 / 0.1778 / 0.7428 for α 0.50 / 0.40 / 0.20 / 0.7504 against the board's 0.4759 / 0.3756 /
0.1779 / 0.7428 (the ΔV0-only law: +3.09 / +5.09 / +10.46 / +0.06 % — F's +3.1 / +5.0 / +10.4 %
exactly; at r ≈ 0.74 the ρ and R_f terms cancel, which is why regions 1 and 8 pass). Two independent
validations: CAL-1 RMS 0.0064 (the adopted fit's 0.0063; the ΔV0-only law gives 0.0173) and the loss
map's mirror-pair probe reproduced to 1e-5 (K 0.30674 / 0.31215). Codex's R_eq arithmetic confirmed
(3.61 % of K = 10.494 mΩ = 15.74 mV at 1.5 A) but droop-term-only: with the floor the realized parallel
resistance swings 8.5 mΩ and is non-monotone (min 0.30705 Ω near r 0.68). REFUTED: `DP_BUS_K_G` was
fitted on a 120-point probe of the hi-fi engine with the asymmetry ON — the governor's law is nowhere
on that path — so K_G does not move; the separability caveat is pre-existing (`hil_plant_sim.py:
4271-4277`) but its "±0.9 %" is band-interior: true K_eff runs +4.16 % (r 0.15) to −0.47 % (r 0.68)
against 0.308502 Ω. h2 bias bound on `ems-sdp` ≤ 0.05 % (≤ 6e-6 g of 0.0124009 g); a rail-pinned leg
≤ 0.2 % — inside the ±0.8 % tolerance, above the 65 ppm floor; the bound stands. Safety unmoved: the
1.647 A necessary condition recomputes to 1.64537 A (−1.7 mA); the 1.2500 A calibration is
current-space. NEW: the map is wrong even with `--asymmetry off` (±0.0096 of share at the band rails
from the missing 0.033 Ω floor) — era-independent; and `hil_plant_sim._apply_simple_asymmetry`
(:1540-1554, the simple-mode PLANT) has the identical defect ("M1 with ρ = 1" fed M2's ΔV0, a pairing
the fit rejects). Exposure (why major): three committed number sets are wrong by a computed amount —
the walked region table (`run_hil_suite.py:6493-6504`, up to +10.5 %), `_FW26_SWEEP_MDAC_PIN[12]`
(5339/5293 vs measured 5377/5259, 3.1 % against a 2 % band — already a FAIL adjudication), and every
walk/MPC delivered-share prediction (0.004-0.019 of share against the MPC's 5e-3 Gate-1 acceptance;
cf. `mpc_design_20260901.md:663-671`). Minimal fix: two constructor parameters `droop_scale_fc` and
`r_series_ohm` (defaults keep every caller bit-identical), the law above with its closed-form inverse
(A·r² + B·r + C = 0, verified vs brentq and vs the board at seven points, max |Δr| 0.0004), wired from
`electrical.asym_droop_scale_fc` and `DROOP_FIXED_SERIES_OHM` the way `dv0_v` already is; re-walk and
re-pin region 12 to (5377, 5259) (the corrected law predicts r 0.4758 → codes 1281/1163); tests: the
delivered-share section + CAL-1 RMS ≤ 0.0064 + equality against `hil_electrical`'s DC solve at three
ratios. Duplicates campaign-F F1 (symptom) — this supplies the mechanism and the second missing term.
Overlaps `WORK_QUEUE.md` §0c item 6 (my ΔV0-only explanation was incomplete: the series floor is the
second term).

## Round 2 — Codex counter-review (verbatim, resumed thread)

F1 — **CONCEDE.** Reduce to minor documentation drift. The HIL mirror’s impossibility and the 46→27 clamp arithmetic were already settled in run 001; campaign D did not satisfy the re-open rule. I withdraw “E moves the window to hide it”: the 29.1 s edge is derived and the braking artefact is deliberately pinned. The surviving defect is `docs/HIL_PLANT.md:1795` calling the byte positive evidence that the real manager ran.
F2 — **REFINE.** Keep minor, but narrow it: §4.8 must classify these as hi-fi-plant outputs plus observed firmware response, not physical validation. I withdraw the claim that the document says the 30/60 kΩ values were validated. The stronger arithmetic correction is \(C_{MOT}=470+500=970\ \mu F\), so \(\tau=60k·970\ \mu F=58.2s\) and retention over 2.323 s is 95.15%, not 92%. The +1.5 s estimate was an undocumented hand estimate, not a calibrated model; the record should show the two-/three-node calculation and residual.
F3 — **REFINE.** Retain **major**, but only for the governor/walk/simple-model fidelity, not the DP bound. The demonstrated exposure remains 10.46% MDAC-ratio error at low share, sufficient to make published walk pins wrong. The correct law includes both \(\rho=0.9434\) and \(R_f=0.033\ \Omega\): \(R_{FC}=\rho k_d/r+R_f\), \(R_{BT}=k_d/(1-r)+R_f\). My DP consequence is withdrawn: `K_G` was fitted through the hi-fi plant, not the governor surrogate, and the reported ≤0.2% rail-pinned impact is inside the existing ±0.8% comparison tolerance. The fix must also correct asymmetry-off and simple mode, not merely add \(\rho\).
F4 — **REFINE.** Retain minor as an accounting-boundary defect, not a relative-DP defect. `_source_current()` demonstrably omits FC_BUS/BT_BUS link-drop energy, and “billed implicitly through `r_fix`” is false; the fixed 35 mV belongs in the fitted intercept. I accept the verified impact: 0.250–0.362% absolute under-bill, while tables and runs share the same N_BUS boundary so matched deviations cancel. The minimal fix is documentation plus an intercept invariant, not wholesale source-current rerouting. The remaining share-dependent \(i^2R\) residue should be disclosed for single-source comparisons.
F5 — **REFINE.** Reduce to minor. I withdraw the \(i\sqrt{L/C}\) proposal: that is the wrong loop for Death-5 and understates the recorded hot-loop mechanism at high current. The defensible non-integrated estimate is current-scaled from the recorded 15 A commutation: \(ΔV=(1.95/15)i_{cut}=0.130\,i_{cut}\) V. That gives 8.5 mV at 65 mA, 65 mV at 0.5 A, and 0.829 V at 6.38 A. The 0.5 A classification gate is currently non-exposing; none of 1,028 events yields a verdict under fixed, scaled, or LC forms. Still change “boost-death signature” to a non-certifying bound and remove the three incorrect “physically correct” LC-fix claims.
F6 — **REFINE.** Retain minor. The semantic issue is real, but I withdraw “too late for a reader”: the initial discussion already says not to read a run as beating the optimum, and the generated comparison note is unconditional. The needed correction is smaller: rename “lower-bound leg/reference” to model-optimum reference and put the observed signed ±0.8% dynamic/DC bias in the table caption and standard note.
F7 — **REFINE.** Retain minor, narrowed to scope. I withdraw the claim that `TAU_R` drives the share transient; the algebraic droop re-split is the relevant zero-lag limit. The fw-v26 transient result is conditioned on that plant realization, but the OC outcome is robust over the verified bracket of roughly 1.49–1.68 A because a single raw sample above 1.40 A latches. Add that condition to §11 and the transient section. Also add campaign-F’s 24–45 ms single-source cut deferral to §9.4.3; the document already correctly identifies the bus-law probe as hi-fi-engine-derived.
F8 — **CONCEDE.** Retain minor. The stale prose and misplaced §9.4.3 are confirmed. I withdraw any implication that `:1798`’s [0.25, 0.75] is wrong—it is the minority-current clip band, not the DP grid.
## Ranked final adjustment list
1. Fix `GovernorModel` with \(\rho\) and the 0.033 Ω common floor; implement its closed-form inverse and re-pin all walk MDAC outputs.
2. Fix simple-mode asymmetry: it must not use M2 ΔV0 with \(\rho=1\); add asymmetry-off regression coverage.
3. Correct §4.8’s 58.2 s/95.15% N_MOT decay, classify plant outputs versus firmware responses, and show the latch-shift node arithmetic.
4. Perform the physical dark-node decay capture; do not promote 30/60 kΩ beyond `TODO(calibrate)` before it.
5. Replace ring language and the three LC-fix claims with the hot-loop current-scaled, non-certifying bound.
6. Remove “manager ran” wording from HIL_PLANT, HIL_SCENARIOS, and HIL_MODE; repair stale firmware citations.
7. Add a bench test that holds `REGEN_ENABLE` high during MPPT-manager OBSERVE to pin the exclusion.
8. Rewrite the diode-drop section as an accounting-boundary statement; add the V0/intercept invariant and disclose the 0.25–0.36% absolute bias.
9. Rename the DP item as a model-optimum reference and carry signed ±0.8% bias in labels/captions.
10. Add §11 scope for zero-lag transient realization and §9.4.3’s 24–45 ms cut deferral.
11. Date-scope stale regen claims, update the grid prose, and move §9.4.3 before §10.
12. Correct the printed race formula in `fw26_current_ceiling_governor.md`.
## New defensible positions
- **N1/N2 are one major model-family defect:** the governor law needs the common 33 mΩ floor even with asymmetry off, and simple mode inherits the same conceptual error. Campaign-F inversion matching to ≤0.05% is direct evidence for the full law.
- **N3 is a documentation boundary:** the loss map’s ±0.9% statement is only interior; the true +4.16% rail dependence must be stated wherever the map claims control-independence.
- **N7 is a minor observer-accounting inconsistency:** `p_bal_w` bills MOT_PWR but not boost-link drops. It should be fixed or explicitly scoped as observer-only; it does not reopen the settled published-metric issue.
- **N8 remains unverified.** The one-tick `V_bus=0` while N_MOT remains 15.78 V could be report-side zeroing or engine behavior; no defensible conclusion without inspecting the raw trace and frame-generation path.

## Adjudication (orchestrator, 2026-09-03)

Severities are the verification round's, as modified by conceded round-2 arguments. Codex conceded
or refined every finding; no disagreement survived round 2. The review is review-only; fixes go
through the normal fix discipline (below).

| ID | Source | Final severity | Status | Finding | Agreed fix | Rejected parts |
|---|---|---|---|---|---|---|
| PLANT-R2-F3 | codex + verifier (opus) | **major** (low end) | accepted | `governor_model`'s static split law carries only the ΔV0 half of the two-parameter asymmetry fit and omits the 0.033 Ω common series floor; the walk-derived MDAC pins are wrong by up to +10.5 % (region 12 pin 5339/5293 vs measured 5377/5259, an adjudicated FAIL) and the MPC's delivered-share predictions err 0.004–0.019 against its 5e-3 Gate-1 acceptance. The full law (R_FC = ρ·k_d/r + R_f, R_BT = k_d/(1−r) + R_f, α = (ΔV0/I_tot + R_BT)/(R_FC + R_BT)) reproduces the board to ≤ 0.05 % on all nine campaign-F windows and CAL-1 at RMS 0.0064 | Two constructor parameters (`droop_scale_fc`, `r_series_ohm`; defaults bit-identical) + the law with its closed-form inverse, wired from `hil_electrical.ASYM_DROOP_SCALE_FC` and `DROOP_FIXED_SERIES_OHM` the way `dv0_v` is; re-walk; re-pin sweep region 12 to (5377, 5259) and add region-10 pins; tests: delivered-share section, CAL-1 RMS ≤ 0.0064, equality vs `hil_electrical`'s DC solve at three ratios. Same fix in the simple-mode plant (`_apply_simple_asymmetry`, N1) and asymmetry-off coverage (N2) | Codex's "K_G and the loss-map bound must be re-derived": K_G was fitted on the hi-fi engine with the asymmetry on; bound bias ≤ 0.05 % (interior) / ≤ 0.2 % (rail) inside the ±0.8 % tolerance. Codex's "add ρ only": insufficient (+5.4 % residual at r ≈ 0.18) |
| PLANT-R2-F2 | codex + verifier (fable) | minor | accepted | §4.8 presents hi-fi-plant outputs as "measured"/"confirmed"; the 92 % / 28.2 s retention figures use 470 µF where the plant stamps 970 µF (trace: 95.15 % / 58.20 s; pre-era τ 1.94 s); "65 of 65" is a B/C carry-over (70 of 70); the +1.5 s latch-shift figure was an undocumented two-node hand estimate (three nodes → +2.24 s vs measured +2.62 s) | §4.8 header and a per-row plant-output / firmware-response classification; corrected 95 % / 58.2 s at five sites (HIL_PLANT :1699-1703, `run_hil_suite.py:545-546`, the D ledger, D's HIL_SUMMARY, the CLAUDE.md 2026-09-03 addendum); the node arithmetic at :1688; "70 of 70" | "the document claims the 30/60 kΩ values were validated" — it keeps `TODO(calibrate)` at :1637/:1675 |
| PLANT-R2-F5 | codex + verifier (opus) | minor | accepted | :2319 states `over_absmax` "is the boost-death signature" unhedged; the fixed 1.95 V allowance is the record's own FC number at the 15 A cycle-limit commutation (`boost-bringup-debug.md:1572-1573`); three repo sites name i·√(L/C) as "the physically correct fix", which is the node-ring loop, not the recorded boost hot-loop mechanism (understates it 10.6× at 6.38 A); the code comment "no scaling law documented" is falsified by :195. Verdict invariance: 0 → 0 → 0 over 1028 events under fixed / current-scaled / LC forms; no masked kill through the 0.5 A gate (class maximum node 15.54 V vs the 18.05 V needed) | Reword :2319 ("a bound, not a computed ring; non-certifying"); replace the three i·√(L/C) sites with the hot-loop current-scaled form peak = v_node + 0.130 V/A·i_cut and its provenance; correct the :644-646 comment; say the boost hot-loop L is stamped on the RT1987 node; the 0.5 A gate stands | Codex's i·√(L/C) estimator and "2,450× too large" (wrong loop); "gate is not physics" as a defect (it is class membership, non-exposing) |
| PLANT-R2-F4 | codex + verifier (opus) | minor | accepted | The boost-link drop (0.035 V + i·0.023 Ω) is billed to neither source (`_source_current` refers stack currents at N_BUS); "billed implicitly through r_fix" is false as an energy statement (the fixed part is the fitted intercept: V0_NOLOAD − RT_V_FWD = 15.871716 vs V0_EFF 15.871722). Absolute under-bill 0.25–0.36 %; cancels in every table-vs-run deviation (same boundary in DP/walk/MPC); share-dependent residue 0.087 % of source power in favour of single-source at 1.15 A | Rewrite :1725-1731 as an accounting-boundary statement with the numbers; pin `abs(DP_BUS_V0_EFF − (V0_NOLOAD − RT_V_FWD)) < 1e-4`; caveat the 0.09 % single-source residue wherever the `ems-mpc-single` gain is quoted; scope `p_bal_w` item 4 (N7) | Codex's "refer source current to the boost-output node and recalibrate the tables" (moves every anchor ~0.3 %, breaks the bleed-era pins, opens a plant-vs-DP gap) |
| PLANT-R2-F7 | codex + verifier (fable) | minor | accepted | §11.1 does not state that the fw v26 transient figures (:1055-1072: 25.6 % under-read, the 4.3-vs-25-tick race, the −0.191 A plant term) are conditioned on the plant's load-rise and its algebraic droop re-split (the zero-lag limit; a real converter with τ_r 7–300 µs delivers ≤ the plant's peak); the board's peak on the E stimulus brackets [≈ 1.49, ≤ 1.68] A and the OC_FC outcome is plant-invariant; the 24–45 ms cut deferral is absent from §9.4.3 | §11.1 "not trustworthy for" gains the transient-realization exclusion; :1057 "against the hi-fi plant; the delivered peak is an upper bound for a lagging converter"; §11.2 bench row (τ_r step test + a `W 4.0 0.15` joint-step run); §9.4.3 gains the deferral; fix the printed race formula's spurious "1 −" in `fw26_current_ceiling_governor.md:456-457` and the E ledger (N5) | Codex's `TAU_R = 100 µs` mechanism (not in the share-step path; 0.012 A of a 0.239 A error even if it were); "the single-source law is presented as a board measurement" (the doc says "probing the hi-fi engine") |
| PLANT-R2-F6 | codex + verifier (sonnet) | minor | accepted | "Lower-bound reference" at first mention; runs land 0.09–0.80 % below the bound (the DC-Gfc vs dynamic-integrator bias); the ±0.8 % number is absent from `MATCHED_DP_GFC_NOTE` and the table caption; the "lower-bound leg" label is unqualified | Rename at :2749 to "offline model-optimum reference (a lower bound on the Gfc DC-gain stage cost, not on the logged h2)"; carry the signed magnitude in `MATCHED_DP_GFC_NOTE`, the Table caption (`hil_ems_comparison.py:698-699`) and the "lower-bound leg" label (:677); update the note's test | "too late for a reader" (the first mention already warns; the note is appended unconditionally to every generated report) |
| PLANT-R2-F1 | codex + verifier (fable) | minor | accepted | One surviving pre-R1-F1 sentence: :1795 "the `mppt_thresh_cnt` column is the positive evidence the manager ran" (the run-001 fix landed at :1297-1320, :1613, :1746); same phrasing at `HIL_SCENARIOS.md:333` and `HIL_MODE.md:397`; three stale `.ino` citations in `run_hil_suite.py`; no bench test drives the MPPT manager's OBSERVE with `REGEN_ENABLE` high (N9) | One sentence at :1795 (text in the verifier report); the two sibling sites; the citations; a `-DBENCH_TEST=0/1` test raising `REGEN_ENABLE` mid-OBSERVE and asserting the manager abandons (`.ino:11533`) | Re-opening PLANT-R1-F1 (no new evidence: the carry was documented before campaign D); "E moves the window to hide it" (derived edge; artefact pinned) |
| PLANT-R2-F8 | codex + verifier (sonnet) | minor | accepted | Stale current-state prose: :589 "validates the regen model end to end" (measured 0.63 vs 0.707); :643-651 "No campaign has run any of this"; :2794 "the share control grid is [0.25, 0.75]"; §9.4.3 placed after §11 | Date-scope :589 → §4.8; SUPERSEDED banner at :643; rewrite :2794; move §9.4.3 before §10 | Any implication that :1798's "[0.25, 0.75]" (the minority-current clip band) is wrong |
| PLANT-R2-N1 | verifier (F3 adjacent) | minor | accepted | `hil_plant_sim._apply_simple_asymmetry` (simple-mode PLANT) is "M1 with ρ = 1" fed M2's ΔV0 — the pairing the fit document rejects; simple-mode runs mis-split by up to 0.019 | Fixed with F3 (same law); note in §4.4a that the simple engine and the offline controller models carried only half the fit | — |
| PLANT-R2-N2 | verifier (F3 adjacent) | minor | accepted | The governor map is wrong with `--asymmetry off` too (±0.0096 of share at the band rails from the missing 0.033 Ω floor) | Covered by F3's `r_series_ohm`; asymmetry-off regression test | — |
| PLANT-R2-N3 | verifier (F3 adjacent) | minor | accepted | The loss-map separability banner's "±0.9 % share dependence" is band-interior; true K_eff deviation +4.16 % at r 0.15 / −0.47 % at r 0.68 vs 0.308502 Ω; h2 consequence ≤ 0.2 % on a rail-pinned leg | Restate the banner (`hil_plant_sim.py:4271-4277`) and §9.4.3 with the rail figure | — |
| PLANT-R2-N5 | verifier (F7 adjacent) | minor | accepted | The race formula as printed (`fw26_current_ceiling_governor.md:456-457`, campaign E ledger) has a spurious leading "1 −" (evaluates to 6.4 ticks, not the quoted 25); correct form ln((I_new − I_new·CEIL/LIMIT)/(I_new − I_old))/ln(1 − α) = 24.9 | Correct both sites | — |
| PLANT-R2-N6 | verifier (F2 adjacent) | nit | accepted | τ figures have mixed node capacitances across engines (0.94 s = simple 2 kΩ × 470 µF; hi-fi N_MOT is 970 µF) | Quote one capacitance per engine in §4.8 | — |
| PLANT-R2-N7 | verifier (F4 adjacent) | nit | settled-caveat | `p_bal_w` item 4 bills the MOT_PWR drop but the two boost-link drops have no item (observer column only) | Scope as observer-only or add the item | — |
| PLANT-R2-N8 | verifier (F2 adjacent) | open (unverified) | open | The comm-loss trace reports V_bus 0.0000 one tick after the State-99 latch while N_MOT holds 15.78 V; C_VBUS should decay at τ ≈ 30 kΩ × 35 µF ≈ 1 s. Report-side zeroing (§4.1 source-liveness rule) or engine behaviour — not settled here | Inspect `hil_plant_sim.py` liveness reporting (:1467, :1749, §4.1) against the hi-fi node solve on the raw trace before the next physics change | — |
| PLANT-R2-N4 | verifier (F5 adjacent) | minor | accepted | Three sites name i·√(L/C) as "the physically correct fix" (wrong loop) | Folded into F5 | — |
| PLANT-R2-N9 | verifier (F1 adjacent) | minor | accepted | No bench test pins the MPPT manager's regen exclusion | Folded into F1 (test follow-on) | — |
| PLANT-R2-N10 | verifier (F1 adjacent) | nit | accepted | `HIL_SCENARIOS.md:333` / `HIL_MODE.md:397` carry the "manager ran" phrasing | Folded into F1 | — |

**Rejected recommendations, with rationale:** re-referencing source current to the boost-output
nodes (F4: a 0.3 % level effect that cancels in every comparison; would break the bleed-era pins);
the i·√(L/C) ring estimator (F5: wrong loop against the bench record; understates the recorded
mechanism 10.6× at the largest cut); re-deriving K_G / the DP tables for the asymmetry term (F3:
the map was fitted through the hi-fi engine; bias ≤ 0.2 %); re-opening PLANT-R1-F1 (F1: no new
evidence); the "hide the artefact" reading of the mppt window (F1: derived edge, pinned artefact);
"too late for a reader" (F6: the caveat is at first mention and in every generated report); the
`TAU_R` mechanism (F7: not in the share-step path).

**Verdict on the document:** an honest physics record with one substantive model defect outside it
(the offline controller map's split law, F3, which the document did not describe as incomplete) and
a set of documentation-accuracy corrections in §4.8, §8.5, §9.4 and §11. Nothing found changes a
safety verdict, a campaign verdict, or a published comparison number beyond its stated band.

**Follow-on discipline:** F3/N1/N2 are tooling-model changes → implementation with tests
(`tools/test_governor_model.py`, `tools/test_hil_plant_sim.py`), then re-walk and re-pin; the
firmware is untouched (no `pinmap-audit`, `protocol-bump` or `regen-coeffs` — the firmware's droop
math is correct and `share_controller_coeffs.h` is not involved). N9 is a firmware TEST addition →
`test` skill, both `BENCH_TEST` builds. The dark-node decay capture (F2, ranked 4 by Codex) is a
bench item → `bench-incident` when it runs. Everything else is documentation.

## Fix round (2026-09-03, operator-approved, order F3/N1/N2 -> docs -> N9)

- **F3/N1/N2 shipped.** `GovernorModel` carries the full split law (rho + the 0.033 ohm floor) with a
  closed-form inverse; `_apply_simple_asymmetry` uses the same law; both parameters are resolved from the
  live engine beside `dv0_v`. Validation: board windows <= 1.1e-4, CAL-1 RMS 0.006414, engine equality
  9.3e-5, inverse 2.2e-16. Re-pins (all on the old law's path): sweep regions 1/8 (4917, 6464), 12
  (5378, 5259), new region 10 (5376, 5261) at a declared 5 % band, clamped region 2 ((5100, 5648),
  (4822, 7895)), cruise phase-A r_applied 0.6125, unbridged region-6 peak 1.7120 A. DP tables and K_G
  byte-identical. Gate 1: mpc-det 0.000740 PASS (was 0.008927 under the dV0-only map); mpc-sto 0.0076
  FAIL for the pre-existing forecast reason. Design note `docs/modeling/governor_split_law_20260903.md`.
  Review (Opus): SHIP WITH FIXES, all applied (MPC-side wiring tests; split resolved from the engine;
  `--droop measured` runtime warning; rho = scale_fc/scale_bt; public `map_is_identity`).
  **Open (operator ruling):** the law under `--droop measured` (engine alpha 0.2571 vs model 0.2208 at
  r 0.2 / 1.5 A; resolution in the design note section 6).
- **Docs shipped:** F1/N10, F2/N6, F4/N7, F5/N4, F6, F7/N5, F8, N3 as adjudicated; the CLAUDE.md
  addendum and the two local ledgers corrected (95.15 % / 58.20 s on 970 uF; the race formula 24.9 ticks).
- **N9 shipped:** `test_mppt_regen_excluded_from_window` (35 checks; 3961 / 175 / 4443, 0 warnings).
  The rule as implemented: `fcChargePathIsPowering()` = FC_CHARGE high AND REGEN low, sampled at the
  manager's 50 Hz tick; OBSERVE abandons to IDLE and zeroes the sample count, a fresh settle wait applies
  on re-entry, no budget counter moves. Two residuals recorded, no firmware change: (a) a level test, so
  a regen pulse that opens and closes between two `pollAg105()` ticks would fold its sample; (b) the
  abandoned window's stale minimum stays visible in the State-98 diagnostics until the next re-seed.
- **N8 remains open** (report-side liveness zeroing vs engine behaviour).
- **Bench items:** the dark-node decay capture (F2), the tau_r step test and `W 4.0 0.15` joint step (F7).

## N8 settled (2026-09-03 afternoon, trace inspection)

Verdict (b), high confidence: the hi-fi engine discharges N_BUS in four ticks, and the mechanism is
the documented 0.15 A `I_AUX_A` housekeeping sink stamped unconditionally on `C_VBUS` 35 uF
(4.2857 V per tick predicted, 4.2947 / 4.2906 measured; the 30 kOhm bleed contributes 0.53 mA, a
factor 285 smaller). The reported `V_bus` is the solved node (`hil_electrical.py` results block);
the section 4.1 liveness gate is simple-engine only; the injected frame carries the same value. N_MOT
holds 15.78 V because MOT_PWR reverse-blocks (`reverse_block` event at the latch). Energy closes
(4.383 mJ). No pinned anchor reads the post-latch value. Doc fix applied to HIL_PLANT.md sections
4.2 and 4.3. Open: a dropout floor for `I_AUX_A` below ~1 V (model ruling; cannot move a loaded
anchor) and `neg_clamp_count` plumbing into the sidecar.
