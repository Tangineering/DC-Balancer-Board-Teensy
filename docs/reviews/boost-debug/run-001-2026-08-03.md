# boost-debug run 001 — 2026-08-03

**Target:** the 2026-07-31/08-01 bus-overvoltage diagnosis chain in
`docs/boost-bringup-debug.md` (recurring `FAULT_OV_BUS` 0x8004 on State-98 `G`): the parked
load-dump overshoot hypothesis, its supporting measurements (16.0 V regulation, ΔY = 1.4 V
parks, 7–9 A INA253 shunt peaks, tD_ON > BUS_SETTLE_MS, no body-diode bus pre-charge), the
overshoot arithmetic, and the proposed fixes (CSS 100 nF, settle 20 ms/ADC gate, windowed OV,
bleed, no compensation change). Reviewer explicitly tasked with constructing stronger
alternative hypotheses.

**Process:** Codex task-class adversarial round 1 → finding-verifier fan-out
(opus = quantitative, fable = adjudication, sonnet = mechanical) → Codex counter-review
(resume) → adjudication → ledger.

## Round 1 (Codex)

Thread: `019fc98c-f292-7850-88b9-8a3f17a565a0` (task-class, resumable)

```
VERDICT: materially flawed — the record proves a real, current-coupled overvoltage event, but...
[codex] Turn completed.
VERDICT: materially flawed — the record proves a real, current-coupled overvoltage event, but not the asserted RT1987-SCP causality or the numerical guarantees used to justify the fixes.

FINDINGS

F1

- Severity: critical
- Location: docs/boost-bringup-debug.md:311-354; teensy_controller/teensy_controller.ino:2639-2649
- Attack: A fixed 20 ms delay cannot prove the two-stage precharge occurred. RT1987 turn-on requires VIN above UVLO; `D-MT-EN` has VIN = VBUS, so it cannot precharge until `D-BT-EN` has first raised VBUS. `tD_ON=8 ms` is typical only, with no maximum. Even at typical values, the chain is roughly 8 ms (`D-BT`) + 8 ms (`D-MT`) + ramps, leaving little margin in 20 ms; the latest capture’s VBUS=0 observation means the prerequisite low-voltage path was not demonstrated.
- Consequence: Boosts can again meet an uncharged motor node at ~16 V—the known hardware-destruction path.
- Recommendation: Gate boost enable on measured precharge of both VBUS and V-MOT/V_rgn, with timeout-to-fault; do not use a delay as evidence of completion. :codex-file-citation{path="C:\Life Ops\School\Thesis\DC-Balancer-Board-Teensy\references\Datasheets\RT1987_DS-00.pdf" purpose="source"}

F2

- Severity: major
- Location: docs/boost-bringup-debug.md:341-354
- Attack: 100 nF is a plausible candidate, not an all-corner guarantee. The stated formula gives `tON=19.83 ms` at 16 V and 4.71 ms at 3.8 V. For the 10–90% ramp, `I≈0.8CV/t`; this is 0.646 A per 1 mF. The 2.5 A SCP floor is typ-only, not a guaranteed minimum, and it is crossed at `C≈3.87 mF`. The VESC capacitance is unknown. Also, `½CV²` is 128 mJ at 1 mF but 495 mJ at 3.87 mF—not the claimed 100–130 mJ—and no board-specific SOA proof is supplied.
- Consequence: A longer soft-start can move the failure into RT1987 pass-FET stress/OTP while falsely appearing “self-limiting.”
- Recommendation: Measure effective V-MOT capacitance and current waveform, including VESC attached; qualify CSS tolerance and repeated-start thermal stress before fitting it.

F3

- Severity: major
- Location: docs/boost-bringup-debug.md:398-405; 472-502
- Attack: The `0.24–0.39 V/A` result is only a small-signal substitution of the datasheet crossover equation: `ΔV≈ΔI/(2πC f_c)`, where C cancels only because `f_c∝1/C`. It is invalid once COMP/current limits saturate. It also uses an irrelevant 12 V corner: battery-only BT operation at 7.4–8.4 V gives 0.388–0.342 V/A, hence 1.4 V implies 3.6–4.1 A, not 3.6–5.9 A. The later triple-channel entry explicitly reports 1.5–1.9 A and calls the linear inference falsified.
- Consequence: “Foldback-band confirmation” and the no-compensation conclusion rest on a disproven current inference.
- Recommendation: Remove the current-back-calculation as evidence; model the event from measured COMP/inductor-current trajectories. :codex-file-citation{path="C:\Life Ops\School\Thesis\DC-Balancer-Board-Teensy\references\Datasheets\TPS61288LRQQR.pdf" purpose="source"}

F4

- Severity: major
- Location: docs/boost-bringup-debug.md:426-442; 472-502
- Attack: `700–900 mV ÷ 0.1 V/A = 7–9 A` holds only if that is the INA output’s absolute excursion from its zero-current baseline. The described scope trace was vertically offset; no retained waveform, baseline cursor, probe attenuation, or bandwidth record establishes that conversion. The same document later reports 150–190 mV, or 1.5–1.9 A, for the same dips. That is not a tolerable rounding difference.
- Consequence: The record overstates stress by roughly 4× and incorrectly presents dip #1’s clamp amplitude as settled thesis-grade evidence.
- Recommendation: Preserve a raw capture/CSV showing INA zero, channel offsets, probes, and cursor deltas; report pulse charge `∫I dt`, not a visually inferred peak.

F5

- Severity: major
- Location: docs/boost-bringup-debug.md:311-340; 356-384
- Attack: The evidence is consistent with D-MT SCP, but does not force it. SCP requires output current continuously clamped above ISCP for 250 µs; neither D-MT SS nor a clamp-status waveform was captured. A 0.5–1 ms dip and a bus-arrival correlation can also arise from normal soft-start completion, RT1987 reverse blocking, or TPS61288 current-control recovery. The absence of another event inside a <64 ms window is compatible with SCP, not confirmation.
- Consequence: The document promotes an explicitly UNCONFIRMED mechanism to “confirmed on paper,” steering hardware changes and thesis claims without its decisive observable.
- Recommendation: Capture D-MT SS, VBUS, V-MOT, and the shunt simultaneously; use FLTB only as supporting evidence, not a substitute for SS/current timing.

F6

- Severity: major
- Location: docs/boost-bringup-debug.md:356-377; 120-124
- Attack: “No body-diode precharge when disabled” agrees with the RT1987’s back-to-back FETs. But it does not reconcile the earlier explicitly labeled `2-VBUS.jpg` observation of VBUS stepping 0→8.3 V. Calling that earlier VBUS trace “the local upstream node” contradicts its own label. Both can be true only if the earlier RT1987 was enabled/conducting, or the capture/channel was mislabeled.
- Consequence: The historical Death-4 sequencing evidence is not yet normalized; claims that an old run was precharged or a new run was necessarily uncharged remain unsafe.
- Recommendation: Append a correction: disabled RT1987 has no passive precharge; classify the historic 8.3 V event as live-path conduction or unresolved capture identity until remeasured.

F7

- Severity: critical
- Location: docs/boost-bringup-debug.md:462-470; teensy_controller/teensy_controller.ino:1111, 283
- Attack: An 18.0 V grace threshold is not a sound protection ladder. TPS61288’s OVP minimum is 18.3 V, leaving only 0.3 V; its recommended VOUT maximum is 18 V and absolute maximum is 20 V. Divider tolerance alone is about ±30 mV at 18 V; ADC-reference error is uncalibrated. OVP stops switching; it does not sink a peak-held bus. The proposal also specifies neither window arming/re-arm nor State-99 interaction, while `detectFaults()` presently has an unconditional comparator.
- Consequence: If implemented, a repeated load-dump can be deliberately masked until hardware protection is already acting or an unobserved SW/VOUT peak damages a boost.
- Recommendation: Do not add the window before eliminating the event. If retained, specify an explicit tested state machine, calibrated threshold, hard expiry, and re-arm only after all paths are off and both nodes discharge.

F8

- Severity: major
- Location: docs/boost-bringup-debug.md:453-460; controller_design/system_model.md:281-333
- Attack: Section 6e validates a 5–10 Hz share-loop simplification; it does not validate microsecond startup overshoot. The blanket “more C_OUT does not help” follows only from the linear crossover substitution. The later entry’s saturated-EA model is instead `ΔV≈I_dump·t_slew/C_node`, where more local capacitance does reduce peak voltage. Likewise, reducing the 2 nF COMP capacitor could reduce slew-limited overshoot, though it requires loop/RHPZ revalidation.
- Consequence: “No compensation change” is presented as settled while a potentially cheap mitigation is dismissed using an inapplicable model.
- Recommendation: State that CSS is the preferred root fix, but retain COMP/COUT changes as experimentally testable mitigations after a safe load-step and loop-stability review.

F9

- Severity: minor
- Location: docs/boost-bringup-debug.md:297-300; 468-470
- Attack: The divider-only decay arithmetic is reasonable: `17.4/56.4 kΩ=309 µA`; `40 µF·0.4 V/309 µA=51.9 ms`. But a 2.7 kΩ bleed gives 6.44 mA at 17.4 V and crosses 17.4→17.0 V in only `40 µF·0.4/6.44 mA=2.48 ms`, not ~15 ms. It dissipates 95 mW at 16 V and 113 mW at 17.5 V. Its 1.8 mV droop is small, but firmware’s “zero current” gate is 1 µA, so 6 mA is not ignored.
- Consequence: The bleed can perturb no-load share behavior and continuously consume power in State 3, which intentionally leaves the bus energized.
- Recommendation: Document those effects; add a realistic no-load current threshold before calling droop impact nil.

F10

- Severity: minor
- Location: teensy_controller/teensy_controller.ino:1014-1027, 1088-1112; docs/boost-bringup-debug.md:219-229
- Attack: `0x8004` proves that one scaled ADC sample exceeded `LIMIT_V_BUS_MAX`; it does not independently prove each fault was the long parked plateau. The initial 17.4 V bench observation supports a real event against the then-17.0 V threshold. But `analogRead()` is unfiltered, uses an assumed 3.3 V reference, and logs neither raw count nor time correlation. After the 17.5 V mitigation, the fault-rate/peak relation is undocumented.
- Consequence: The diagnosis risks turning a valid fault bit into an overprecise physical-voltage claim in published work.
- Recommendation: Log raw BUS ADC count and timestamp around `G`, then calibrate against a DMM/scope at 16.0, 17.5, and 18.0 V.

F11

- Severity: info
- Location: docs/boost-bringup-debug.md, “UPDATE,” “Datasheet reconciliation,” “Dual-channel capture,” and “Research round”
- Attack: UNCONFIRMED boundaries leak downstream: the revised mechanism is declared “confirmed on paper”; dip #1 remains unidentified but is used in the timing narrative; D-MT’s UVLO/tD behavior is inferred without SS; a 4–9 ms apparent retry conflicts with 64 ms SCP yet is folded into the mechanism; the linear current inference is used before its later falsification; CSS is called immune/all-scenario; SOA acceptability is asserted; and the compensation/bleed verdicts inherit those assumptions.
- Consequence: The document reads more settled than its evidence permits, risking silently wrong thesis numbers.
- Recommendation: Label each item as measured, datasheet-guaranteed, inferred, or proposed; demote every downstream conclusion that depends on an inferred item.

ALTERNATIVES

A1

- Mechanism: D-MT completes a non-SCP soft-start; as V-MOT approaches VBUS, RT1987 reverse blocking abruptly removes the boost load. TPS61288 inductor energy/EA recovery then parks both rails high.
- Existing evidence: Explains VBUS-arrival timing, real forward shunt current, the VOUT dip, and a 17.4 V parked rail without requiring a 64 ms retry.
- Different prediction: D-MT SS completes normally; no 250 µs continuously-clamped interval precedes the release.
- Discriminator: One synchronized capture of D-MT SS, VBUS, V-MOT, and shunt current.

A2

- Mechanism: The TPS61288 itself enters peak-current/VCC-control recovery during the capacitor transfer; the output shunt sees only 1.5–1.9 A average while switch/inductor current reaches its 15 A-class limit. Load release then overshoots the local rail.
- Existing evidence: Explains a deep local VOUT dip plus recovery overshoot and does not conflict with the missing 64 ms D-BT retry.
- Different prediction: TPS SW/inductor current and COMP show converter limiting while D-MT SS proceeds without SCP shutdown.
- Discriminator: Capture TPS SW/inductor current and COMP referenced to IC AGND during the deep dip.

A3

- Mechanism: Dip #1 is a D-BT/D-MT UVLO handoff race. A brief D-BT pulse charges 40 µF VBUS to D-MT’s ~3.3 V rising threshold: `Q=40 µF·3.35 V=134 µC`, requiring ~89 µs at 1.5 A. D-MT then drains into 470 µF+VESC and VBUS falls below UVLO, leaving little retained bus charge.
- Existing evidence: Explains tD phase-locking, real shunt current, the missing visible retained VBUS rise, and the non-64 ms follow-on event.
- Different prediction: Fine-resolution VBUS crosses both UVLO thresholds while V-MOT begins to rise.
- Discriminator: Capture VBUS and V-MOT at 1–2 V/div through dip #1.
```

## Verification round

**Operator ruling (2026-08-03, mid-verification):** the "1.5–1.9 A" figures in the doc's
2026-08-02 triple-channel subsection were an artifact of an **abandoned/rewound chat branch**
(the operator computed the current wrong, the doc was updated, the chat was rewound and the
number corrected to 700–900 mV = 7–9 A — but the doc write survived the rewind). The operator
directed removal; the subsection was rewritten 2026-08-03 with the corrected amplitudes
(7.2 ± 0.4 A needle / 6.3 A hump centre, per the F4 verifier's photometric read) and a
provenance note. This settles the amplitude dispute at the center of F3/F4 with operator
authority, in agreement with all three independent photometric reads. Codex's round-1 citations
of 1.5–1.9 A were accurate quotations of the then-current doc — not fabrications; the
orchestrator's "fabrication check" steer in the F3/F4 briefs was based on a stale read of the
doc and was wrong.

**Verdict summary** (11 agents: 8 Opus, 3 Sonnet; severities re-assigned by verification):

| ID | Verdict on finding as filed | Underlying defect | Key quantitative outcome |
|---|---|---|---|
| F1 | PARTIAL (as-written minor) | major | 8+8 serial-tD_ON refuted (tD_ON doesn't re-run on VIN-good); measured EN→precharge chain 19.3 ms lower-bound vs 20 ms → 3.5% margin → **open-loop settle rejected, ADC gate only** |
| F2 | PARTIAL | major | All Codex arithmetic exact; node measured 0.4–0.6 mF (bare) → 6–8× margin; "any scenario" bounded ≤2.9 mF worst-case CSS; **DS §18 contains no SOA curves** — "acceptable" unsupported; OTP-destruction consequence refuted |
| F3 | PARTIAL (as-written minor) | major | Citation real (operator: rewound-branch artifact, corrected to 7–9 A); methodological core confirmed; replacement 3.6–4.1 A refuted (VIN readable in capture: 8.2–8.7 V); foldback + no-comp conclusions survive |
| F4 | REFUTED as filed | major (defect) | Record error UNDERSTATED stress 3.9× (unit slip: A3 scale used for fitted A1); photometric 7.2±0.4 A needle / 6.3 A hump / 7.4 mC; process residue minor (BW-limit, ∫I dt convention) |
| F5 | CONFIRMED, understated | major | "SCP guaranteed" falsified (completed 1.77 ms conduction, no cut/retry; applicable ISCP ≈5.3 A at ΔV=16); doc's VIN-UVLO replacement ALSO refuted (VBUS ramps DURING deep dip; VOUT floor 9.68 > 8.1 V passthrough); **deep dip = A1 completion-release, A2 complement**; A3 refuted by charge conservation (0.8–1.3 mC vs ≤4 µC bus) |
| F6 | PARTIAL | minor | 2-VBUS.jpg identity proven (0 V baseline = bus); git: pre-Death-5 bringUpBus had no MOT_PWR → historic 40 µF-only connect completed in 44 µs → history reconciled; wording fixes only |
| F7 | CONFIRMED | major | Tolerance stack (±0.1% dividers, ±2% ADC ref): reading 18.0 → true ≤18.47 V > 18.3 OVP min & 18 V rec-max → **18.0 V rung withdrawn; 17.5 V max legal window; no window rides out the parks** |
| F8 | CONFIRMED | major | C_OUT-independence linear-only; C_C carries NO share-plant collateral (f_c <0.1%, τ_r <0.1 µs; cost 5–11° PM) → both cap levers re-opened; CSS-first decision survives |
| F9 | CONFIRMED | minor | Both decay figures right under different unstated assumptions; real gaps: standing 95–113 mW dissipation omitted; share-nil mechanism mis-attributed (6.4 mA ≈ 0.8 ADC LSB is the real reason) |
| F10 | CONFIRMED | minor | All predicates true; ±0.26 V exposure computed but unrealized (all precise voltages scope-sourced) → TODO adopted |
| F11 | mostly ALREADY-HEDGED | minor | 5/8 hedged by supersession convention; 2 narrow leaks; (e) mooted by operator ruling; largely redundant with F2/3/5/8 |

**New verifier-raised items:** N1 park decay 113 V/s → ~1.5 ms above 17.0 → persistence
filter viable (flips doc trade) [major]; N2 OV-ADC node (VBUS, peaked 16.85 V) vs cursor
node (boost-local, 17.23 V) discrepancy — one confirming run + raw-count logging [major];
N3 dip #1 charge-conservation violation → half-rectified-ring / CM-artifact candidates;
zoomed 20–50 µs/div single-shot discriminates [major]; N4 settle scales with fitted CSS
(~33 ms at 100 nF) — ADC gate mandatory [major]; N5 historic connects already ISCP-clamped
(edge 4.5× faster than gate-ramp) [minor, UNCONFIRMED]; N6 metrology conventions
(div×scale recording, trace-centre levels, chronological ordering) adopted [minor].

**Cross-verifier disagreements carried to adjudication:** park amplitude convention
(cursor edge-to-edge 1.4–1.5 V vs centre-to-centre 0.65–0.72 V — F5's metrology preferred,
convention now in bench rules); overshoot mechanism (linear vs EA-slew — both over-predict
the centre-to-centre parks; adjudicated OPEN pending COMP-pin probe); F4's "linear
confirmed at 0.24 V/A" rested on the 12 V corner F3 rejected — superseded by F5's
0.10–0.19 V/A empirical band.

## Round 2 (Codex counter-review)

```
F1 — REFINE (major): I concede the incorrect serial `8 + 8 ms` model; D-MT’s delay did not re-run on VIN arrival. The core point strengthens: a 19.3 ms measured lower bound leaves only 0.7 ms (3.5%) in a 20 ms typ-only/no-max delay. The only acceptable fix is ADC-confirmed VBUS and V-MOT precharge; this was already the document’s preferred option.

F2 — REFINE (major): I retain the unmeasured VESC-capacitance and unsupported-SOA findings. I concede the claimed new destruction/OTP mode: the measured node gives about 30 °C junction rise and OTP retries. With 0.4–0.6 mF bare-board, 100 nF has 6–8× margin; its guarantee boundary is 2.9 mF with tolerance. Do not claim “any scenario” until VESC-attached capacitance is measured.

F3 — REFINE (major): I concede reliance on the abandoned 1.5–1.9 A branch and withdraw my 3.6–4.1 A replacement. I retain the validity-boundary critique: at captured VIN 8.2–8.7 V, the linear coefficient is 0.32–0.35 V/A, but corrected centre parks are only 0.65–0.72 V. Empirical ratios are roughly 0.10–0.19 V/A, so neither linear nor EA-slew amplitude model is established. COMP probing is decisive.

F4 — CONCEDE: Withdrawn as a major finding. The filed capture, 1× probes, CH2 zero marker, offsets, and independent pixel calibration establish 7–9 A; my reliance on the abandoned branch reversed the error. Residual process issue is minor: record CH2 bandwidth and raw CSV/envelope-integration convention.

F5 — DEFEND (major, stronger): The SCP-specific claim is falsified. Deep-dip conduction lasts 1.77 ms, or 7.1× the 250 µs timer, yet VBUS rises monotonically 0.91→16.85 V and no 64 ms retry follows. This is consistent with current dropping out as ΔV vanishes, resetting SCP timing—not with a timer cut. I withdraw A3: `0.8–1.3 mC` versus at most `~4 µC` retained VBUS charge rules out that UVLO-race explanation. A1 completion-release remains primary; A2 converter/source limiting remains complementary.

F6 — CONCEDE, with minor wording refinement: Capture identity is settled. Historic firmware did not enable MOT_PWR, so only ~40 µF needed charging: `0.33 mC / 7.5 A ≈ 44 µs`, safely inside 250 µs. Current firmware exposes 470 µF+VESC at a UVLO-straddling precharge and returns VBUS to ~0.9 V. The difference is configuration, not a contradiction in RT1987 isolation.

F7 — DEFEND (major): This remains the strongest safety finding. With ±2% ADC-reference uncertainty dominating the ±0.1% divider stack, an 18.0 V firmware reading can mean 18.47 V true—0.17 V above TPS61288’s 18.3 V OVP minimum and above its 18 V recommended operating limit. The 18.0 V window is unsafe and should remain withdrawn. No legal window rides out the park.

F8 — DEFEND, corrected (major): I concede my claim that halving C_C required re-synthesis. DS Eq. 12 has no C_C; 2 nF→1 nF changes crossover <0.1% and share-plant lag <0.1 µs, costing instead 5–11° from a 76–79° phase margin. The categorical dismissal remains wrong. CSS stays first-line; C_C is a valid secondary mitigation, while C_OUT depends on the pending COMP result.

F9 — REFINE (minor): My 2.48 ms result is correct for 40 µF and a 0.4 V drop; the document’s ~15 ms can describe a 1.2 V combined-node case, but it failed to state that assumption. The measured 113 V/s decay supersedes both: a typical excess above 17.0 V lasts ~1.5 ms, making a 2–3-sample persistence filter viable. I retain the 95–113 mW Idle/State-3 bleed cost; share impact is negligible because 6.4 mA is ~0.8 ADC LSB, not because of the 1 µA divide-by-zero guard.

F10 — REFINE (minor): The generic ADC-rigor finding stands: one unfiltered sample, hardcoded 3.3 V, no raw count, and ±0.26 V exposure at 17.5 V. I accept that it did not create a published-number error. The VBUS-cursor discrepancy is separately major pending one confirming run.

F11 — CONCEDE/REFINE (minor): Most boundary issues were already hedged and my list was redundant. Retain only two leaks: the SS-inferred statement under a “Confirmed” heading and the unbounded “any scenario” CSS wording. Evidence-class tags are the correct fix.

N1 — Agree, major. The 113 V/s measurement overturns the “persistence cannot work” claim. A 0.17 V residual takes `0.17/113 ≈ 1.5 ms`; a short, bounded persistence filter can reject this event, provided it is tested against genuine overvoltage response.

N2 — Agree, major pending one run. A 16.85 V VBUS cursor is 0.15 V below the armed 17.0 V limit, while ADC-scale uncertainty alone is ±0.26 V near 17.5 V. The mismatch could be calibration, pickup, capture mismatch, or node identity; each materially changes trust in the firmware safety monitor.

N3 — Agree, major. Dip #1 cannot be the proposed UVLO charge transfer: `0.8–1.3 mC` versus ≤4 µC observable bus charge is a 200×-class deficit. Prioritize a 20–50 µs/div single-shot to distinguish a half-rectified shunt-ring event from CH2 common-mode artifact.

N4 — Agree, major. A fixed settle must scale with fitted CSS; at 100 nF the observed sequence requires about 33 ms. Since both delay and capacitance are corner-dependent, this is an independent proof that ADC gating—not a longer constant—is mandatory.

N5 — Agree, minor and unconfirmed. A 4.5× faster historic connect edge is compatible with an ISCP-clamped amps-class event, but does not prove it without a timebase-calibrated current/SS capture. Preserve it as a historical hypothesis only.

N6 — Agree, minor. Trace-centre cursor rules, scale/attenuation recording, and chronological section ordering directly prevent the errors corrected in this round.

RANKED final adjustment list

1. Replace all fixed-settle proposals with ADC-gated confirmation of VBUS and V-MOT precharge; 5 ms is defective, 20 ms is rejected, and 100 nF implies roughly 33 ms if a fixed delay were attempted.

2. Withdraw “SCP trip guaranteed” and “mechanism confirmed” for the deep dip. Completion-release plus converter/source limiting is the leading explanation; capture D-MT SS, COMP, VBT, VBUS, V-MOT, and shunt current.

3. Keep the 18.0 V overvoltage window withdrawn. Restore a normal 17.0 V threshold after the root event is removed; resolve the ADC-node discrepancy before relying on any firmware voltage limit.

4. Fit CSS as the primary mitigation, but state its guarantee only for measured capacitance. Measure VESC-attached capacitance and qualify the RT1987 transient-energy/SOA claim before declaring 100 nF universal.

5. Re-open C_C as a secondary mitigation and condition C_OUT decisions on the COMP capture; CSS-first remains the current design choice, not proof that compensation is irrelevant.

6. Remove the dip-#1 UVLO-race claim unless the high-resolution capture shows the required bus excursion. Ring/common-mode artifact is now the leading unresolved class.

7. Adopt a short, bounded persistence filter only after confirming the 113 V/s decay and verifying that it does not mask a real sustained fault.

8. Correct the record’s metrology: 0.65–0.72 V trace-centre parks, corrected 7–9 A currents, explicit capture provenance, raw ADC logging, and evidence-class labels.
```

## Adjudication

**Overall verdict on the diagnosis chain: sound-with-major-corrections.** The events, the
0x8004 fault reality, the tD_ON > BUS_SETTLE_MS timing bug, the CSS-first root-fix choice,
and the no-compensation-change decision all survive. Three mechanism links are rewritten
(deep dip = completing soft-start with convergence release, not an SCP cut; dip #1 =
non-charge-delivering event of unknown class, not an aborted clamp; parks are 0.65–0.72 V
centre-to-centre decaying at ~113 V/s, not 1.4–1.5 V for 50–400 ms), and several fix
parameters change (open-loop settle rejected; 18.0 V window withdrawn; persistence filter
re-opened; CSS guarantee bounded).

| ID | Source | Final severity | Status | Finding (adjudicated form) | Agreed fix | Rejected parts |
|---|---|---|---|---|---|---|
| BOOST-R1-F1 | codex | major | accepted | Fixed settle (5 or 20 ms) cannot evidence precharge completion; measured chain 19.3 ms lower-bound vs 20 ms; scales to ~33 ms with 100 nF CSS (N4) | Reject open-loop settle; ADC gate on V_bus AND V_rgn with timeout→FAULT_INIT_FAIL; new low-voltage threshold constants (V_BUS_CHARGED_THRESH is post-boost) | Codex's 8+8 serial-tD_ON arithmetic (refuted; tD_ON doesn't re-run on VIN-good) |
| BOOST-R1-F5 | codex | major | accepted | "SCP trip guaranteed / mechanism confirmed" falsified; deep dip = A1 completion-release (+A2 depth complement); doc's VIN-UVLO replacement also refuted; dip #1 charge-conservation violation → ring / CH2 CM-artifact candidates | Retitle DS-reconciliation header; rewrite SCP bullet (foldback ≈5.3 A at ΔV=16, connect completed); rewrite capture-5 VBUS bullet (bus ramps DURING dip); add conservation bound + surviving candidates; queue zoomed 20–50 µs/div dip-#1 single-shot + COMP + VBT probes | A3 (UVLO race — withdrawn by Codex; refuted by charge conservation) |
| BOOST-R1-F7 | codex | major | accepted | 18.0 V windowed threshold fails tolerance stack (true ≤18.47 V > 18.3 OVP min, > 18 V rec-max); no legal window rides out the parks | Withdraw 18.0 V; 17.5 V is the max legal window; correct ladder (add 18 V rec-max rung; fix L54 OVP shorthand); ADC calibration precondition (F10 TODO); persistence filter (N1) may obsolete the window entirely | Codex's design-incompleteness half capped at minor (open sketch, not an implementation) |
| BOOST-R1-F8 | codex | major | accepted | C_OUT-independence is linear-regime-only (all measured events saturated on the dip side); C_C dismissal rationale wrong (no share-plant collateral; cost 5–11° of 76–79° PM) | Scope the comp verdict; re-open C_C and C_OUT as testable secondary mitigations conditional on the COMP-probe outcome; CSS-first decision unchanged | None (Codex's weaker "requires RHPZ revalidation" hedge superseded by the stronger truth) |
| BOOST-R1-F2 | codex | major | accepted | CSS 100 nF guarantee is bounded (≤2.9 mF worst-case CSS), not "any scenario"; VESC-attached capacitance unmeasured; SOA "acceptable" cites a section with no SOA data; 100–130 mJ wrong for the doc's own 470 µF | Bounded-guarantee rewrite with measured 0.4–0.6 mF node (6–8× margin); VESC capacitance measurement queued; θJC-estimate framing for dissipation; CSS-tolerance note | Codex's OTP/pass-FET destruction consequence (OTP auto-restarts; ΔTJ≈30 °C measured) |
| BOOST-R1-F3 | codex | major (defect) / minor (as filed) | settled-caveat | Overshoot-arithmetic corners unsupported (12 V never occurs; VIN readable in-capture at 8.2–8.7 V); linear-validity boundary real; empirical coefficient 0.10–0.19 V/A — both models over-predict; mechanism OPEN | Scope-of-validity rewrite of the research-round arithmetic paragraph; COMP-pin probe queued as discriminator | Codex's 3.6–4.1 A replacement (withdrawn); "conclusions collapse" consequence (foldback + no-comp survive) |
| BOOST-R1-F6 | codex | minor | accepted | Historic "body diode pre-charge" is a mechanism-name error (was live D-BT-EN conduction into a 40 µF-only node — pre-Death-5 firmware had no MOT_PWR in bringUpBus); "no pre-charge" claim needs scoping to current config | Verifier's five wording edits (Death-4 row, captions, scoping); history reconciled, Death-4 conclusion strengthened | Codex's "unresolved capture identity" and "history unsafe" branches (refuted by pixel + git evidence) |
| BOOST-R1-F9 | codex | minor | accepted | Bleed bullet: unstated node/ΔV assumptions; wrong mechanism cited for share-impact-nil (real reason: 6.4 mA ≈ 0.8 ADC LSB); standing 95–113 mW dissipation omitted | Corrected bullet; superseded in practice by N1 (persistence filter likely obsoletes the bleed) | Codex's "not ignored by firmware" concern (sub-LSB, unresolvable) |
| BOOST-R1-F10 | codex | minor | accepted | ADC path unfiltered/uncalibrated (±0.26 V at 17.5 V) — real but unrealized (all precise voltages scope-sourced) | TODO line: raw-count + timestamp logging around G; 3-point DMM calibration; ties to N2 | Major framing (no wrong published number exists) |
| BOOST-R1-F11 | codex | minor | settled-caveat | 2 of 8 boundary leaks real ("Confirmed:" header over an SS-inferred bullet; "any scenario") | Header split + evidence-class tag (folded into F2/F5 fixes) | The other 6 sub-items (already hedged / mooted); the "immune to every corner" misquote |
| BOOST-R1-F4 | codex | rejected (minor process residue) | rejected | "Record overstates stress 4×" was backwards — the 7–9 A branch is the measured truth; the low figure was the rewound-branch artifact (operator-confirmed) | Process residue adopted: record CH2 BW-limit; ∫I dt-with-envelope convention (bench-incident rules, N6) | The finding's consequence, its "150–190 mV" pseudo-quote, and its stale no-retained-waveform premises |
| BOOST-R1-N1 | verifier (F5) | major | accepted | Park decays at ~113 V/s → ~1.5 ms above 17.0 V → a 2–3-sample persistence filter rides out the events (doc said the opposite) | Correct the decay figures; add persistence filter to the fix menu, gated on one decay-confirmation run + a masked-real-fault test | — |
| BOOST-R1-N2 | verifier (F5) | major (pending one run) | accepted | OV-ADC node (VBUS, peak 16.85 V) vs cursor node (boost-local 17.23 V) discrepancy — the trip margin may be misjudged | One confirming run with raw ADC counts logged (F10 TODO) before relying on any firmware voltage limit | — |
| BOOST-R1-N3 | verifier (F5) | major | accepted | Dip #1 charge-conservation violation (0.8–1.3 mC vs ≤4 µC) excludes all delivered-charge mechanisms | Zoomed 20–50 µs/div single-shot on dip #1 (trigger CH2 rising) — top-priority bench measurement | — |
| BOOST-R1-N4 | verifier (F2/F1) | major | accepted (merged into F1) | Fixed settle scales with fitted CSS (~33 ms at 100 nF) | ADC gate (same fix as F1) | — |
| BOOST-R1-N5 | verifier (F6) | minor, UNCONFIRMED | accepted | Historic connects were plausibly already ISCP-clamped amps-class events (edge 4.5× faster than gate-ramp) | Historical-hypothesis note only | — |
| BOOST-R1-N6 | verifier (F4/F3/F5) | minor | accepted | Metrology conventions (div×scale recording, trace-centre cursor levels, chronological ordering, calibration-chain notes) | Add to bench-incident conventions + skill | — |

**Cross-verifier disagreements, adjudicated:** (1) Park amplitude — F5's centre-to-centre
0.65–0.72 V metrology adopted over the 1.4–1.5 V cursor readings (edge-to-edge over-read;
convention now in the rules). (2) Overshoot mechanism — F4's "linear confirmed" rested on
the rejected 12 V corner; F3/F8's "saturated" applies to the dip side but the recovery
regime is unproven; adjudicated OPEN, COMP probe decides (and with it the C_OUT lever).
(3) The orchestrator's fabrication steer in the F3/F4 briefs was wrong (operator-confirmed
rewound-branch artifact); recorded above.

**Bench measurement queue produced by this run (priority order):** (1) zoomed dip-#1
single-shot (N3); (2) one `G` with raw ADC counts logged + COMP pin on a spare channel
(N2 + mechanism); (3) VBT channel through one `G` (deep-dip source-vs-converter limiting);
(4) VESC-attached V-MOT capacitance measurement (F2); (5) decay-rate confirmation for the
persistence filter (N1).
