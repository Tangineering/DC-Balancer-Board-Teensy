# Firmware version ledger

`FW_VERSION` is a monotonic u16 defined near the top of
`teensy_controller/teensy_controller.ino`. **Bump it on every flash-worthy
behavioral change** â€” control law, pin/sequencing, scaling, logging format â€”
and add a row here in the same commit. Comment/doc-only changes do not bump.

The version is:
- stamped into every `.BLG` bench-log header (format v2 and later, u16 at offset 18),
  so every logged dataset is attributable to the firmware that produced it
  (`decode_benchlog` prints it; it lands in each run's `decode_report.txt`);
- printed at boot (`[BOOT] DC balancer firmware vN`);
- printed by the State-98 `S` status dump (`FW_VERSION: N`).

Version **0 / "pre-versioning"** is reserved for all firmware before this
ledger existed. Format-v1 `.BLG` files (no fw_version field) decode as
`fw_version=pre-versioning`; that covers `PS0001`â€“`PS0002` (pre-averaging)
and `PS0003`â€“`TP0005` (`analogReadAveraging(8)`).

| FW | Date | Flashed changes (vs previous) | Logs produced |
|----|------|-------------------------------|---------------|
| 0 | â€” | Everything before versioning. Notable sub-states: PS0001/PS0002 ran the pre-averaging build (Teensyduino default averaging 4); PS0003/TP0004/TP0005 ran the `analogReadAveraging(8)` build; the 2026-08-11 share-setpoint sweep TP0007â€“TP0013 also decodes as fw 0 (format-v1 headers). | PS0001â€“TP0005, TP0007â€“TP0013 |
| 1 | 2026-08-11 | `analogReadAveraging(16)` (was 8). Share-integrator minimum-load hold at `SHARE_I_TOT_MIN_A` = 75 mA. Full-span share command semantics: setpoints/ratios valid over [0,1]; `applyShareRatio()` actuation layer â€” droop-band clip [0.15, 0.85] moved inside it, out-of-band ratios cut the starved channel off the bus via its RT1987 bus switch (never the boost enable) with 0.01 re-entry hysteresis and a charged-bus re-entry guard; `O`/`P` commands unclamped to [0,1]; Youla anti-windup authority span widened to [0,1]. BLG header format v2 (fw_version stamp). State-98 combined profiles: `Y` (velocity + share) and `W` (commanded current + share) sweep both axes from one shared 16-region table via `advanceComboRegion()`, logged to `YPnnnn`/`WPnnnn`; **the VESC watch moved from `W` to `U`**; both profiles warn when the committed share band reaches the cutoff region and re-close a latched channel cutoff on natural completion; `pollVescWatch()` suppressed during the staged bring-up. | *(never flashed â€” superseded by v2 before first flash)* |
| 2 | 2026-08-11 | Share-loop limit-cycle mitigation (TP0010/TP0013 sweep + capture 12; whitepaper `docs/share_sweep_whitepaper`): setpoint governor clips the effective in-band share setpoint so the commanded minority-channel current stays â‰¥ `SHARE_MINORITY_I_MIN_A` = 0.20 A (collapses to 0.5 below 2Ã—; out-of-band setpoints incl. 0/1 bypass â€” the cutoff path owns them); droop-ratio slew limit `DROOP_RATIO_SLEW_PER_TICK` = 0.02/tick in `applyShareRatio()` (full band in 35 ms, no rail-to-rail MDAC slams); `resetShareControlState()` at every profile start (`R`/`D`/`T`/`Y`/`W`) â€” runs no longer inherit the prior run's controller state. Î”Vâ‚€ CAL-1 recorded in the model (+0.05 V, bench supplies); shipped Youla coefficients unchanged. | *(never flashed â€” superseded by v3 before first flash)* |
| 3 | 2026-08-11 | State-98 `T` sweep extension: `T <Imax> <hold> <rate> [t,r1,...,rn]` runs one trapezoid per closed-loop share setpoint `r_i` (max 16), each to its own `TPnnnn.BLG`, with the next run gated on the logger being fully idle (`!logActive && !logCloseRequested` â€” an early start would silently lose that run's dataset) and separated by a `t`-second motor cool-off dwell. Share setpoint applied via `setPowerShareSetpointLive()` BEFORE each run opens its log; setpoint restored to 0.5 / `powerBalanceLive` cleared on completion and on every cancel path (`T`-stop, `X`, `Q`, a new `T` line, or any other profile start). Sweep list refused under plot mode (`L`); the grammar's 4th field ends the old trailing-junk tolerance (any unparsable tail now rejects the whole line). `inputBuf` 32 â†’ 96 B, and `[`/`,`/`]` accepted at the trapezoid prompt only. Automates the hand-run TP0007â€“TP0013 sweep. | TP0014â€“TP0038, WP0039â€“WP0040 |
| 4 | 2026-08-12 | Three changes from the fw v3 validation sweep (TP0014â€“TP0038, WP0039â€“WP0040; whitepaper `docs/share_sweep_whitepaper`). (a) `SHARE_MINORITY_I_MIN_A` 0.20 â†’ **0.30 A**: the sweep bracketed the light-load conduction floor â€” 0.245 A commanded minority still collapsed the bus to 8.2 V (TP0016, sp = 0.15), 0.29 A is clean (TP0017, sp = 0.18) â€” so the shipped 0.20 A sat below the whole bracket and governed nothing at the setpoints that failed; the governor's collapse-to-0.5 threshold rises to 0.60 A automatically. (b) **Setpoint-latched channel cutoff** (`updateShareSetpointCutoff()`, "one owner per setpoint"): the governor bypassed out-of-band *setpoints* while the cutoff fired on the controller *output* `r`, leaving two gaps â€” sp = 0.87 settles at r â‰ˆ 0.84 (in band), engaging neither mitigation and running a 19.5 Hz limit cycle (TP0037), and sp = 0.12 fired the cutoff but the standing share error wound `r` back over the 0.01 re-entry hysteresis, hunting at 20 Hz with ~190 `FC_BUS_ENABLE` cycles/run (TP0015). A setpoint outside `[DROOP_R_MIN, DROOP_R_MAX]` now latches the starved channel off the bus (same last-source guard; guard-blocked â†’ no latch, normal governed control), **freezes** the share controller (no governor, no step, no MDAC write), and disables ratio-hysteresis re-entry for that channel; release only on the setpoint returning in-band, gated on `V_bus â‰¥ V_BUS_CHARGED_THRESH`, then `resetShareControlState()`. State-98 `'1'`/`'2'`, `assertFcChargeEnable()`, `safeAllSwitches()` and the completion restore all clear the latch (operator/state ownership). The r-based cutoff remains as the in-band backstop. (c) **`FAULT_UV_BUS` reworked**: armed by the bus (`V_bus â‰¥ V_BUS_CHARGED_THRESH` with a source switch closed; disarmed when both source switches are LOW) instead of gated on State 2, persistence-filtered like `FAULT_OV_BUS` (`UV_BUS_PERSIST_MS` = 10, 3 samples, 5 ms gap guard), and **armed under `BENCH_TEST`** â€” WP0039 sagged to 7.6 V through 89 dropout cycles with `fault_flags = 0` and ended in an MCU brownout. `LIMIT_V_BUS_MIN` stays 12.0 V. `'S'` gains UV-transient/armed and setpoint-latch lines. No telemetry layout change (v4/58 bytes). | TP0041â€“TP0068, WP0069â€“WP0073 |
| 5 | 2026-08-12 | Three changes from the fw v4 validation sweep (TP0041â€“TP0068, WP0069â€“WP0073). (a) **Governor open-loop low-current fallback**: the fw v4 governor collapsed the effective setpoint to 0.5 below `2Â·SHARE_MINORITY_I_MIN_A`, which at 0.075â€“0.60 A of filtered total commands 0.038â€“0.30 A per channel â€” at or below the 0.30 A conduction floor it exists to enforce, against ~20 mV of droop authority â€” and that fallback *ignited* the source-commutation relay limit cycle (six runs collapsed the bus to 7â€“9 V and latched `ERR_UV_BUS`). `powerBalance()` now has two modes, hysteretic on the filtered total (enter closed loop above 0.60 A, fall back below 0.60 âˆ’ `SHARE_GOV_OL_HYST_A` = 0.55 A): **closed-loop** runs the Youla controller + the now always-relaxing governor clip (the collapse-to-0.5 branch is deleted as unreachable); **open-loop** never steps the controller â€” it feeds the raw setpoint forward through the same `DROOP_RATIO_SLEW_PER_TICK` limiter until the closed loop has run once (the sweep showed the commanded hold ratio tracks the setpoint within ~0.01â€“0.02), and **holds** the last applied ratio thereafter. The openâ†’closed transition reseeds the controller from `droopSlew_prev` via the new `resetShareControllerCore()`, which deliberately does not touch `share_govTotAFilt` (the mode decision variable); `resetShareControlState()` clears both new mode flags, so every profile start (`R`/`D`/`T`/`Y`/`W`) and the setpoint-latch release begin open-loop. (b) **`FAULT_UV_BUS` leaky-dwell filter**: the fw v4 wall-clock window (10 ms + 3 samples + 5 ms gap guard) is evaded by duty cycle â€” the relay cycle's 9 ms-under / 51 ms-over period reset it every cycle, so runs endured 1.0â€“1.3 s and up to 24 excursions to 7 V before latching. `UV_BUS_PERSIST_*` is replaced by `UV_BUS_DWELL_LATCH_MS` = 20, `UV_BUS_DWELL_LEAK` = 0.05, `UV_BUS_DWELL_DT_CAP_MS` = 5: dwell accumulates `+dt` under the limit and leaks `âˆ’0.05Â·dt` above it, latching at 20 ms net (TP0053 nets +6.45 ms/cycle â†’ latch â‰ˆ180 ms; WP0069's ~19 ms of sparse dips over 208 ms nets ~9.6 ms â†’ correctly no latch). Arming/disarming unchanged; `'S'` reports the live dwell. (c) **BLG format v3** (`hdr[4]` = 3, 68 B/record): record gains `V_fc`, `V_batt`, `V_chg`, `V_rgn` after `I_cmd` â€” WP0072/WP0073 ended in MCU brownouts with the *bus* in regulation, and the board-powering `V_batt` rail was unlogged. `tools/decode_benchlog.py` updated in lockstep. A source-rail UV *fault* is deliberately **deferred** until logged `V_batt` from the next brownout sets its threshold. `'S'` also reports the share-loop mode; log `flags` gains bit2 (`shareClosedLoopMode`) and bit3 (`shareClosedLoopRun`). **Scope of the dwell latch:** the 20 ms threshold is validated against BUS-referenced collapses only (TP0053-class relay cycles, WP0069-class transients); the WP0072/WP0073 source-rail brownouts are invisible to any bus-referenced fault by construction â€” that is exactly the deferred source-rail item. Review-round hardening (same version): HOLD no longer strands an outstanding `shareIso*` cutoff nor swallows a commanded setpoint change; orphaned `shareIso*` claims self-heal; `setPowerShareSetpointLive()` resets the share-control state; the controller reset seeds the integrator (not the dead held output); `'R'` and the `'T'` sweep restore a channel cutoff on natural completion like `'Y'`/`'W'`; `FAULT_UV_BUS` arming requires a matched bus-switch + boost pair. | *(pending first flash)* |
| 6 | 2026-08-12 | Five changes from the fw v5 validation sweep (TP0074â€“TP0094 clean, WP0095â€“WP0101). (a) **Load-aware handoff guard** on the setpoint-latched cutoff entry (`SHARE_CUT_MAX_HANDOFF_A` = 0.5 A): the cut hands the doomed channel's entire current to the survivor in one tick â€” WP0097/WP0101 fired it with 1.3â€“1.5 A on BT, pushing solo FC past this bench's ~2.1 A source knee and collapsing the bus in ~40 ms (`ERR_UV_BUS`), while the same cut at ~0 A is validated clean (TP0074/85/86/87 run-start latches). Entry now also requires the doomed channel's |I| â‰¤ 0.5 A; blocked â†’ no latch, and the tick raises a per-tick **deferral** (`shareCutDeferredFC/BT`) which clips the closed-loop *reference* from the out-of-band setpoint onto the doomed side's band edge (so the loop actively migrates load toward the survivor) and **suppresses** `applyShareRatio()`'s r-based cutoff on that side â€” that cutoff has no current guard and would otherwise execute the refused handoff a few ticks later under the wrong ownership flag (`shareIso*`, invisible to the `!shareSpCut*`-gated re-closers â†’ re-close/re-cut cycling). The cut fires on a later tick once the migration has pulled the current under the threshold. *Residual:* at high total current the migration may never get there â€” the band edge is the maximum droop authority available â€” and the loop sits at the edge running the self-limiting rail-saturated dropout cycle instead â€” accepted until the floor-law rework. (b) **Source-rail UV fault on `V_fc`** (`UV_FC_DWELL_LATCH_MS` = 20 ms, reusing `UV_BUS_DWELL_LEAK`/`UV_BUS_DWELL_DT_CAP_MS` as filter-shape parameters): the fault fw v5 deferred, taken now that three fw v5 runs ended in an MCU stop with `V_fc` under 5 V while the **bus still read 15.7 V** â€” invisible to any bus-referenced fault, and leading the bus event by ~7 ms. Armed under `BENCH_TEST` and in every state; the old State-2-gated single-sample check is removed. Boot-lock is prevented by **arming**, not a state gate: `FC_REG_ENABLE` + `FC_BUS_ENABLE` both HIGH (matched pair) **and** `V_fc` observed â‰¥ `V_FC_ARM_THRESH` = 7.0 V while so routed â€” deliberately 1.0 V *above* the 6.0 V trip limit (mirroring `V_BUS_CHARGED_THRESH` vs `LIMIT_V_BUS_MIN`) so a fuel-cell ramp cannot arm the filter inside its own trip band â€” so a bench with no fuel cell never arms. Evaluated **before** the bus UV block so a same-tick double-cross latches the *cause* (`ERR_UV_FC`) rather than the consequence (`ERR_UV_BUS`). `V_batt` counterpart still deferred (threshold blocked on the LM1084-input capture). `'S'` gains a `UV_FC` line. (c) **Governor handover continuity** (`share_spEffPrev`): open loop feeds the raw setpoint forward while the first closed-loop tick handed the controller the floor-clipped value â€” a reference step of up to 0.35 share at the 0.60 A crossing (raw 0.15 â†’ 0.50). The clipped target is now approached through a `DROOP_RATIO_SLEW_PER_TICK`-limited **reference**, seeded at every controller reset/seed from the same value the core is seeded with (`droopSlew_prev` at the OLâ†’CL transition, 0.5 on a full reset), band-clipped. Converged behaviour is bit-identical to fw v5 â€” transients only; the Youla/PI internals are untouched. (d) **Combined-profile table rows 5/6/7** (`W`/`Y`): all five fw v5 `W` failures were **R6-entry** events, R6 being the only region combining peak load (v = 1.0) with an extreme share (s = 1.0). The excursion is kept but **de-rated** â€” R5 ramps v down to 0.3, R6 runs s = 1.0 at 0.3Â·Imax (â‰ˆ0.6 A total, under the source knee), R7 takes the s step-down at that low load then ramps v back to 1.0 so R8 keeps its 1.0 â†’ 0.5 down-step. Region count, durations and the 40 s total unchanged; rows 0â€“4 and 8â€“15 byte-identical. (e) **BLG header format v4** (`hdr[4]` = 4; **record format unchanged**, 68 B): header gains the committed per-run profile parameters â€” `hdr[7]` param-valid flags (bit0 amp, bit1 b), float `profileAmp` at 20â€“23 (`T`/`W` Imax, `Y` Vmax), float `profileB` at 24â€“27 (`W`/`Y` clip bound b); bytes 28â€“31 reserved zero (byte 19 is the high byte of the 2-byte `fwVersion` at 18â€“19, not a reserved byte). Derived inside `logOpenForProfile()` from `typeMask` plus the already-committed profile globals, so no call site can omit them. `tools/decode_benchlog.py` updated in lockstep. No UDP telemetry change (v4/58 bytes). | *(pending first flash)* |
| 7 | 2026-08-13 | Velocity-chain calibration landed. (a) **Encoder geometry measured** (operator, bench): hand-turning the flywheel exactly one revolution accumulates 120 `encoderPos` counts, so at the ISR-verified x2 quadrature decode the disc carries 60 slots â€” `ENCODER_SLOTS_PER_REV` 512.0f â†’ **60.0f** (the 512 was unsourced and not credible behind a 3.18 mm beam gap). âš ï¸ **The 60 is SUPERSEDED by fw v8's 120** â€” the "120" measured here was 120 *slots*, not 120 counts, so the Ã—2 decode was applied backwards; the fw v8 row has the full reconstruction. The Ã—19.70 discontinuity stated at the end of this row is the fw 6â†’7 figure and remains correct for fw 7 traces. Flywheel radius measured directly: `FLYWHEEL_RADIUS_M` 0.033f â†’ **0.0762f** (3.00 in), superseding the nominal tire-OD/2 estimate and the reasoning behind it â€” the encoder disc is mounted on the flywheel, so the flywheel radius is what converts its angular rate to m/s. `RPM_TO_MPS` follows both automatically. (b) **`VELOCITY_CHAIN_CALIBRATED` default 0 â†’ 1**: State-98's velocity entry points (`V` manual velocity, `D` drive cycle) and the `Y` profile's velocity axis are open. The interlock machinery (compile-time `#ifndef` override + runtime `velocityChainCalibratedFlag`) is retained unchanged â€” the over-drive hazard it guards (under-reading `v_actual` â†’ PI adds current chasing a speed already passed; `commandMotorCurrent()` bounds amps, not speed) belongs to the mechanics, so a new disc or flywheel means rebuilding with `-DVELOCITY_CHAIN_CALIBRATED=0` until re-measured. `T`/`W` still deliberately bypass the gate (they never close the velocity loop). (c) **`L` Serial-Plotter stream: six fields â†’ eight** â€” `sp`/`act` renamed to `share_sp`/`share_act`, and `v_sp` (`v_setpoint`) + `v_act` (`v_actual`) appended; order `share_sp,share_act,gFC,gBT,ifc,ibt,v_sp,v_act`. Fixed-field-count rule unchanged; the `plotTick()` backpressure guard rises `Serial.availableForWrite() < 80` â†’ `< 110` for the longer line. Help text and the `[PLOT]` banner list the new fields. (d) **`MOTOR_I_CMD_MAX` 5.0 â†’ 10.0 A** (operator decision): the 5 A figure came from the ~67â€“87 W source power budget, which is a *bus*-current budget, but this constant clamps what `setCurrent()` sends the VESC â€” three-*phase* motor current, related to bus current only through the duty ratio (`I_bus â‰ˆ DÂ·I_mot/Î·_esc`). Bounding bus current is therefore not this constant's job â€” it belongs in the VESC's own Battery Current Max setting (â‰ˆ 4.2 A). âš ï¸ **That is a PRECONDITION, not a statement of fact:** `docs/VESC_MOTOR_INTEGRATION.md` Â§4 still records Battery Current Max and Battery Current Regen Max as *not set / not tracked*. Configure them in VESC Tool (â‰ˆ 4.2 A / â‰ˆ 1.5 A) and tick Â§4 **before any velocity-path run at the 10 A ceiling** â€” until then there is no bus-current bound anywhere: not in this constant (phase current, duty-dependent), not in the VESC (unset), and not in this firmware on a `BENCH_TEST` flash, where the OC faults are compiled out. The old derivation is retained at the constant as the previous bench value; the 15.0 A vehicle `TODO(calibrate)` stands. This is also the State-98 manual-current ceiling (`setManualMotorCurrent()` clamps to Â±`MOTOR_I_CMD_MAX`, so `A` now accepts Â±10 A). **Amended 2026-08-15 (pre-flash): raised again 10.0 â†’ 12.0 A** with the Castle 1406 1900KV motor fitted, for the Youla-H drive-controller bring-up; same phase-current rationale, Â§4 precondition unchanged and still open. Deliberately NOT following it: `TRAP_I_ABS_MAX` (25 A ESC rating â€” what `T`/`W` bound against), `W_IMAX_DEFAULT` (5 A, an independently conservative profile default), `MANUAL_MOTOR_V_MAX` (5.0 m/s, a velocity). The motor PI's anti-windup `integMax` derives from `MOTOR_I_CMD_MAX` and rescales automatically. **No BLG record or header change** (the 68 B record already carried `v_sp`/`v_act`, and `flags` bit1 already reported `velocityChainCalibrated()`) and **no UDP telemetry change** (v4 / 58 bytes). **Scale discontinuity (review S7):** `v_actual` (and the BLG `v_act` column) changed by **Ã—19.70** for identical motion â€” (1024/120) counts Ã— (0.0762/0.033) m â€” and BLG `flags` bit1 is now set by default, so fw â‰¤ 6 and fw 7 velocity traces are **not comparable**; the header `fwVersion` field disambiguates. **Next bench (advisory):** run the first velocity tests scope-armed; use `V` with small setpoints before `D` (the drive cycle's regen phase steps a âˆ’3.5 m/s error, which at the new ceiling commands âˆ’10 A onto the regen path); consider a temporarily reduced `MOTOR_I_CMD_MAX` for the first run. | *(pending first flash)* |
| 8 | 2026-08-16 | **Encoder pin move (the root cause), observability, and a slot-count correction. No control-path, telemetry, or log-format change; the pin map and one scale constant change.** Raised from a bench report that `v_actual` stayed at 0.000 in the `L` stream while the encoder was visibly producing a signal. **(0) PIN MAP â€” ROOT CAUSE: `ENC_A` 2 â†’ 14, `ENC_B` 8 â†’ 15, `ENC_ENABLE` (7) DELETED.** Bodge work had relocated both encoder channels and hardwired the optical sensors to power, and the firmware had not been reconciled â€” it was attaching `CHANGE` interrupts to two pins the encoder was no longer wired to, so no ISR ever fired and `encoderPos` never moved. Pin 7 is now **undriven** (no `pinMode`, no `digitalWrite` anywhere â€” there is no net to drive) and pins 2/8 are free; the `initControlPeripherals()` enable write is gone, so the encoder is live from power-on rather than from State 0. Teensy 4.1 pins 14/15 are A0/A1 and Serial3 TX/RX â€” neither alternate function is used by this firmware and both are interrupt-capable, so the ISR path is otherwise unchanged. `references/Scale Car Teensy IO - IO.csv` was updated by the operator in lockstep (rows moved to 14/15, pin 7 marked "No longer in use"), so the CSV remains authoritative and the firmware follows it row-for-row; bodge recorded in `CLAUDE.md`. **No build before fw v8 can produce velocity on this board.** The velocity chain had exactly ONE observable (`v_actual`) and the x2 quadrature decoder in `doEncoderA()`/`doEncoderB()` only counts when BOTH channels transition in the correct ORDER, so a dead channel, a phototransistor swing that never crosses the Teensy's V_IL/V_IH, and two beams that are not 90Â° apart all produce an identical, silent `encoderPos == 0` â€” none of them distinguishable from a stationary flywheel. Added: `encEdgeCountA`/`encEdgeCountB` (volatile u32, incremented at the top of each ISR, read by nothing but the dumps), an `--- Encoder ---` block in the State-98 `'S'` dump (live `digitalRead(ENC_A/ENC_B)` **printed with their pin numbers** â€” this dump is the field tool for exactly the stale-pin-map fault above and must not report the wrong pins silently â€” `encoderPos`, both edge counts, `v_actual`, and the two scale constants in force), and the same `encoderPos`/edges/pin-number line in the IDLE `printSensors()` dump so a hand-turn check needs no test-mode entry. **`ENCODER_SLOTS_PER_REV` 60.0f â†’ 120.0f** (`ENCODER_COUNTS_PER_REV` 120 â†’ 240) â€” the one scale change in this version, and the observability work above is what uncovered it. The disc was counted directly and physically carries **120 slots**. fw v7's 60 was a transcription error, not a competing measurement: the 2026-08-13 bench figure of "120" was recorded as 120 `encoderPos` *counts* per hand-turned revolution and divided by the Ã—2 decode, when it was 120 *slots* and the decode multiplies. The tell is the observability gap itself â€” no build through fw v7 printed `encoderPos` anywhere, so no count could have been read. **Scale discontinuity:** `v_actual` and the BLG `v_act` column **halve** for identical motion versus fw v7, so fw 7 and fw 8 velocity traces are not comparable (header `fwVersion` disambiguates); versus fw â‰¤ 6 the chain is (1024/240)Ã—(0.0762/0.033) = **Ã—9.85**. `VELOCITY_CHAIN_CALIBRATED` stays 1 â€” a direct slot count is a stronger source than the figure it replaces, and needs no working decoder to obtain. **Drive-direction consequence:** fw v7 over-read `v_actual` by 2Ã—, shrinking the velocity error so the PI under-drove; correcting it restores the true error, so fw v8 commands up to **2Ã— the current fw v7 would have at the same `v_setpoint`** â€” toward truth, but the more aggressive direction on a bench where the fw v7 Â§4 precondition (VESC Battery Current Max / Regen Max) is still open and `BENCH_TEST` compiles the OC faults out. **Disc coupling RESOLVED 2026-08-16 (operator): surface/roller** â€” the encoder is coupled to the flywheel and the flywheel's own radius IS the rolling radius, so `FLYWHEEL_RADIUS_M = 0.0762` is correct as shipped and the wheel-angular-speed alternative (which would have forced the tire radius and made `v_actual` over-read 2.31Ã—) is retired, closing the fw v7 S3 residual. No constant changes â€” a determination, not a measurement. With this the velocity SCALE chain is complete: counts/rev, decode factor, radius and coupling all settled, decoder hardware-confirmed. `v_actual` is flywheel **surface speed**, and `v_setpoint`, the `'V'`/`'D'`/`'Y'` commands and the BLG `v_sp`/`v_act` columns share that definition; no separate vehicle-speed scale exists in the firmware. Everything else â€” sequencing, faults, PI gains, droop, charger â€” is byte-identical to fw v7. Tests: the decoder had no coverage at all (every prior test wrote `encoderPos` by hand) â€” 11 new checks drive it from raw pin levels: forward/reverse Â±2 per cycle, the three silent-zero failure modes each asserted to yield zero counts *and* to remain diagnosable through the edge counters, and an ISR-driven end-to-end path to a non-zero `v_actual`. **Cross-check CLOSED on hardware (2026-08-16, same day):** one hand-turned flywheel revolution reads `encoderPos == 240`, the first reading of the counter ever taken on this board. Two independent sources now agree on the constant (physical slot count, firmware counter), and the same reading independently confirms the Ã—2 decode factor, that both optical channels are alive and genuinely in quadrature, and that the pins-14/15 bodge is correctly reconciled. Scope: counts per flywheel *revolution* only â€” `FLYWHEEL_RADIUS_M` and its coupling are untouched by a revolution count, and were resolved separately (above). Pin coverage added in the same round: `ENC_A == 14` / `ENC_B == 15` pinned literally, `ENC_ENABLE` asserted undefined, and a collision sweep of `ENC_A`/`ENC_B` against every other assigned pin â€” load-bearing because the rest of the suite drives the ISRs through these same macros, so a wrong pin number is self-consistent everywhere else and nothing else would fail. **1668 production + 175 bench pass.** | *(pending first flash)* |
| 9 | 2026-08-16 | **Manual SD logging from State 98 â€” `K` takes an argument. No control-path, sequencing, pin, UDP-telemetry, BLG record or BLG trailer change.** Until now the 1 kHz bench log could only be opened by a *profile* start (`R`/`T`/`D`/`Y`/`W`), so an operator driving the car by hand (`A`/`V` plus switch toggles) had no capture path finer than the 50 Hz `L` stream. New grammar at the `K` prompt: an **empty line** prints the logger status exactly as the pre-v9 bare `K` did; **`1`** opens a manual log and starts sampling; **`0`** requests its close; **anything else rejects the whole line** (`ERROR: K takes 0 or 1 (or empty for status)`) â€” the no-trailing-junk discipline the `T` parser adopted in fw v3, because a partially understood logging command leaves the operator believing a run is captured when it is not. Because the empty line is *meaningful* here, `PEND_K_PARAMS` is dispatched ahead of `handlePendingInputChar()`'s shared empty-line cancel, like `PEND_Y_PARAMS`/`PEND_W_PARAMS`. New `LOG_TYPE_MANUAL` = **0x08** and filename prefix **`MLnnnn.BLG`**, slotted into `logNextFileName()` after the two combined-mask tests (keeping the "combined masks first" discipline) and added to the `knownPrefix` scan so `ML` files share the session-monotonic counter. **The header's `profile_type` field is a bitmask and simply gains bit 3** â€” that is what it was for; `tools/decode_benchlog.py` passes `profile_type` through raw, so only its header docstring changed. Manual runs commit no profile parameters, so the v4 header's `hdr[7]` param-valid flags and both floats stay 0. `logSampleTick()` is untouched: with no profile flags set the phase bytes read `LOG_PHASE_NONE` and `flags` bit0 follows `powerBalanceLive`, which is correct for a hand-driven run. Ownership is one flag, `logManualActive`, cleared at **every** point the log ends â€” `logFinishFile()` (the single close bottleneck: profile force-finish, drain-complete, deadline abandon, IO-error close) and `logDrainTick()`'s `!sdAvailable` early-clear, the one path that clears the flags *without* closing. It deliberately survives `logRequestClose()` so the whole drain window still refuses a second `K 1`. `K 1` guards, each with its own message: refused while `bringupActive` (opening a file walks the directory and `preAllocate()`s 32 MB â€” a stall that must not land inside the millis()-deadline-sequenced bring-up; this is a **parse-time** guard because `K` cannot join the keypress lockout list without also killing the read-only status path), refused while any profile is active, a **`T` sweep is live in any phase** (safety review S1: between sweep runs no profile flag is set and the logger is idle, so an ML open there would be force-finished at the next run's start against a possibly-busy card â€” silently costing that sweep run its entire log, the exact loss the sweep's WAIT_LOG gate exists to prevent), or a plot arm is pending (the profile owns the logger), and refused while a log is already open or draining. A failed open (no card, scan failure, header write) prints its own diagnostic and leaves `logManualActive` false â€” ownership is claimed only if a file actually opened. A profile started over a live manual log takes the logger over through `logOpenForProfile()`'s existing double-open force-finish, which is the intended semantics. `X`, `Q` and `triggerFault()` already close whatever is open (`LOG_CLOSE_X`/`LOG_CLOSE_Q`/`LOG_CLOSE_FAULT`), so a manual log ends cleanly on every existing exit path with **no special case**; `K 0` reuses `LOG_CLOSE_STOP`, so no new trailer code and the decoder's known-reason check is untouched. Review round also added an ownership marker to `printSdStatus()`'s `active:` line â€” `(manual â€” K 0 stops)` / `(profile-owned)` while a log runs (safety S2: `K 0` refusals turn on ownership, so the status dump must show it). | *(pending first flash)* |
| 10 | 2026-08-16 | **Youla-H robust DRIVE (velocity) controller replaces the placeholder motor PI. No pin, sequencing, fault, BLG or UDP-telemetry change; `commandMotorCurrent()` remains the sole VESC writer **on the velocity path** (the `T` trapezoid and `W` profile still call `commandMotorCurrentLimited()` directly at the 25 A ESC rating, unchanged).** User-directed exception to the CLAUDE.md â€œdo not change the PI controllersâ€ rule, scoped to the velocity loop â€” the second such exception after the 2026-07-10 share-loop round. The motor PI it replaces was never tuned: its gains were `Kp = Ki = 1` placeholders, and no build before fw v8 was reading the encoder at all, so no build could have tuned them against a working `v_actual`. The new law is synthesized in `controller_design_MIMO/synthesize_drive_siso.py` (H-infinity mixed sensitivity, Î³ = 27.37, plus a Youla-H DC rescale so T(0) = 1 exactly, Tustin at Ts = 2 ms) against the **calibrated** drive plant `G22` (`controller_design_MIMO/calibration/motor_id_20260815.md`; the plant was re-identified 2026-08-16, so the synthesis is downstream of measured k_t, R_m, m_eff, r_t, Ï„_v and drag). (a) **New runtime `teensy_controller/drive_controller.h`** implementing the **Hanus self-conditioned** 5-state realization, NOT a biquad cascade: `u_unsat = CDÂ·x + DDÂ·e`, `u = clamp(u, Â±12 A)`, `x_next = ACÂ·x + BDÂ·(u/DD)` â€” the *clamped* output drives the state update, so the FULL controller state de-winds. Integrator-only back-calculation (the shipped share controller's idiom) is **measurably wrong here**: the non-integral branch has 745.5 A/(m/s) of low-frequency gain and its lag states saturate independently of the integrator, leaving a âˆ’0.48 m/s standing error and a 22.2 mm/s limit cycle on the 0â†’2 m/s step (it is clean only for steps below ~0.1 m/s). (b) **The state vector is `double`, deliberately.** The realization carries an exact integrator (eigenvalue at 1) plus an exact integrator (`AD[0][0] == 1.0`) alongside `CD` entries of order 50 (the realization's discrete max |z| is 0.9998; an earlier version of this row cited "a second mode at ~0.9999", which is stale against the fw v12 re-synthesis â€” the second section's poles are ~0.158), so perturbations are integrated rather than damped; a float32 recursion was **measured** to diverge ~1.4e-2 A at rail RELEASE on the saturated regen episode (`validate_drive_siso.py` check 4), which shortening the run does not bound. Coefficients stay the shipped float32 values (the replay reference vectors were generated from exactly those roundings) and are promoted per operation. Cost is ~35 double MACs per tick (5 for the output, 30 for the 5Ã—5 state update) at 500 Hz â€” negligible on the Teensy 4.1 FPU. (c) **New generated header `teensy_controller/drive_controller_coeffs.h`.** `synthesize_drive_siso.py` now emits the firmware copy and the study copy (`controller_design_MIMO/drive_siso_coeffs.h`) from **one emitter**, so the two cannot drift; only the top banner differs. Never hand-edit either â€” regenerate. (d) **Units: `motorConstant` is bypassed on the Youla path.** The controller maps velocity error [m/s] to motor current [A] directly, because the synthesis plant already carries the k_t/r_t/m_eff chain; dividing by `motorConstant` would rescale the loop gain by 1/`motorConstant` and void every synthesis gate. `motorConstant` (still an uncalibrated TODO) now survives only on the PI fallback. (e) **`USE_YOULA_DRIVE_CONTROLLER`** (default 1, `#ifndef`-overridable, mirroring `USE_YOULA_SHARE_CONTROLLER`) selects the law; at 0 the fw â‰¤ v9 PI path compiles verbatim and behaves identically. Independent of `BENCH_TEST` and of `velocityChainCalibrated()` â€” the Youla path sits behind the same `V`/`D`/`Y` calibration gates as before. (f) **Clamp pairing:** `DRIVE_CTRL_I_MAX` is synthesized to equal `MOTOR_I_CMD_MAX` (12.0 A), so the clamp the anti-windup conditions against is the clamp `commandMotorCurrent()` enforces downstream; the downstream clamp becomes a redundant backstop rather than the binding limit. A future `MOTOR_I_CMD_MAX` change requires **re-synthesis** (`I_CLAMP` in the script), not an edit to the constant alone â€” the anti-windup boundary scales with the clamp. This is now a **compile-time tripwire**: `MOTOR_I_CMD_MAX` became `constexpr` and a `static_assert` in the `.ino` fails the build if it ever diverges from `DRIVE_CTRL_I_MAX`/`I_MIN`. The emitter was changed to declare those two coefficients `constexpr` for the same reason â€” a namespace-scope `const float` is not a constant expression in C++17, so the guard would not compile against one. Values are unchanged; both headers were regenerated (script exit 0, ALL GATES PASSED). (g) **Reset sites.** `resetDriveControlState()` (states + held output + a back-dated Ts gate) is called from `haltMotorOutput()` â€” the single primitive that covers every velocity-path profile start (`D`/`T`/`Y`/`W` all call it immediately before their `resetShareControlState()`) and every stop, `Q` exit, `X` universal stop and natural completion; from `setManualMotorVelocity()` on the **entry edge only** (a re-typed `V` during a live manual-velocity run is a setpoint *step* the loop must answer from its operating point â€” resetting there would discard that operating point and restart the transient: the first tick is `DDÂ·e` alone, 1.81 A per m/s, but the non-integral branch's 745.5 A/(m/s) low-frequency gain then **rails the command at Â±12 A within roughly 20â€“40 ms** for any error above ~16 mm/s, so the cost is a full-rail excursion, not a gentle coast); and on **Idleâ†’Run**, where `v_setpoint` is zeroed alongside it. The Idleâ†’Run reset deliberately **diverges from the share loop**, which is pointedly not reset there: the MDAC droop ratio is physically held across Idle so the share loop resumes, whereas Idle holds the motor at `setCurrent(0)` and can last minutes while the machine coasts to a stop, making stored `i_cmd` history a live current command derived from a machine state that is no longer true. **The reset alone is not sufficient**, which is why the setpoint is zeroed with it: `doState3()` (Finish) does not clear `v_setpoint`, so the Pi's last commanded speed survives Runâ†’Finishâ†’Idle, and a freshly reset controller meeting a stale non-zero setpoint sees the full error on its first tick â€” the 745.5 A/(m/s) low-frequency gain then rails the command at Â±12 A within roughly 20â€“40 ms for any error above ~16 mm/s, so Run entry would begin with a full-rail current excursion nobody commanded. Zeroing costs at most one tick of tracking (the Pi rewrites `v_setpoint` within one packet period, and a Run genuinely meant to move starts from 0 m/s anyway). Deliberately **not** added beside the two `resetShareControlState()` calls that lack a `haltMotorOutput()` â€” the `R` profile start and the `P` operator setpoint â€” since neither drives the velocity loop and a reset there would zap a live `V` run the operator did not stop. (h) **Wrapper gate tolerance.** `youlaController_Drive()`'s internal Ts gate fires at `DRIVE_CTRL_TS_US âˆ’ DRIVE_CTRL_GATE_TOL_US` (2000 âˆ’ 200 Âµs). The outer gate (`rl_motor_last` / `MOTOR_CTRL_PERIOD_US`) is nominally the same 2 ms period, and two equal-period gates on the same clock beat: an outer tick arriving 1 Âµs early leaves the inner gate one microsecond short, the update is skipped, and the output holds 4 ms instead of 2. Benign (the design delay margin is 54 ms) but nondeterministic â€” it would make the effective sample rate depend on jitter. The margin absorbs the beat while still bounding a hypothetical *ungated* future caller to 1.8 ms, which is the inner gate's only real purpose. **Bench precondition unchanged and still open:** the VESC Battery Current Max / Regen Max are still unset (`docs/VESC_MOTOR_INTEGRATION.md` Â§4, open since fw v7), and this round changes the control law feeding that unbounded path â€” treat the first `V` run as gain identification, scope-armed, and prefer `A`/`T` (velocity-loop-free) first. | *(pending first flash)* |
| 11 | 2026-08-16 | **BLG RECORD FORMAT v5 â€” drive-controller internals in the SD bench log. Observability only: no control-law, pin, sequencing, fault or UDP-telemetry change (the Pi packet stays v4 / 58 B), and the drive controller's update law is byte-for-byte untouched.** The first closed-loop velocity runs are imminent and the drive loop is the least observable thing on the board: the record's `I_cmd` is *post*-clamp, so a transient spent on the Â±`MOTOR_I_CMD_MAX` (12 A) rail decodes identically to one that merely asks for exactly the limit, and the controller state that produced it appears nowhere at all. **Layout:** two little-endian float32 are **appended at the END** of the record â€” offset 68â€“71 `u_unsat` [A], 72â€“75 `drive_x0` â€” so **every v1â€“v4 field keeps its offset** and the record grows 68 â†’ 76 B (`LOG_REC_SIZE`). Header `hdr[4] = 5`; header layout is otherwise **identical to v4** (param-valid flags at 7, `fwVersion` at 18â€“19, Imax/Vmax at 20â€“23, b at 24â€“27, reserved 28â€“31) â€” only `hdr[4]` and `hdr[5]` (the record size, which decoders already read from the header) differ. Trailer unchanged. **Semantics, both builds:** `u_unsat` is the PRE-clamp motor-current command and `drive_x0` the integrator state. Youla build (`USE_YOULA_DRIVE_CONTROLLER=1`) â†’ `driveCtrl_uUnsat`, a new file-scope capture in `drive_controller.h` written immediately before the clamp inside `driveControllerStep()` and zeroed by `driveControllerReset()` (hence by `resetDriveControlState()`), and `driveCtrl_x[0]`, the EXACT integrator (`AD[0][0] == 1.0`) â€” together they show the Hanus self-conditioning working, or a rail episode, from the log alone. PI fallback build â†’ `targetMotorTorque/motorConstant` and `pi_motor_accum`, the same two roles, so an A/B flash logs **comparable** signals. **`flags` gains bit4** (this record's motor command came from the Youla DRIVE controller) **and bit5** (the share loop is the Youla SHARE controller); bits 0â€“3 unchanged. Both are compile-time constants, stamped per record anyway so a decoded run is self-identifying without cross-referencing this ledger. **Sampling relationship:** the drive controller steps at 500 Hz (`DRIVE_CTRL_TS_US`) while `logSampleTick()` samples at 1 kHz, so the log carries **pairs of identical `u_unsat`/`drive_x0` samples per controller tick by design** â€” the same zero-order hold `youlaController_Drive()` applies to its output. The logger **exposes the stored value and never re-evaluates** the output equation, which would run against an already-advanced state and would not be the value the clamp acted on. **Ring/bandwidth re-checked at 76 B/ms:** the ring stays 1024 records = 77824 B (76 KB, still DMAMEM, 1.024 s of coverage); `logDrainTick()` floors a 512 B chunk to 6 whole records = 456 B, i.e. 456 B/ms drained against 76 B/ms filled (6.0Ã— catch-up, was 7.0Ã— at 68 B), and the 32 MB preallocation covers ~7.4 min (was ~8). **No buffer constant needed to grow.** Logging stays observability-only â€” never faults, no card I/O in the sampling path. Decoder/analysis tooling updated in lockstep (v1â€“v4 files decode byte-identically). **Bench precondition CLOSED:** the VESC Battery Current Max (6.0 A forward) and Regen Max (1.5 A) were **set on 2026-08-16** and recorded in `docs/VESC_MOTOR_INTEGRATION.md` Â§4, closing the fw v7 S1 open precondition that fw v8â€“v10 carried. Regen matches the Â§12.4 derivation exactly; forward bounds the bus at **1.43Ã— the derived 4.2 A allowance and above the 5.4 A conditional ceiling whose scope validation has not happened**, and a 6.0 A bus draw split evenly is exactly `LIMIT_I_BT_MAX` = 3.0 A/channel with zero margin (an FC-heavy share setpoint exceeds `LIMIT_I_FC_MAX` = 1.4 A) â€” with the OC faults compiled out under a `BENCH_TEST` flash, so nothing in firmware catches either. **Outstanding: re-derive Â§12.4 against 6.0 A, or lower the setting, before a vehicle run.** This round also adds `resetDriveControlState()` to `setManualMotorCurrent()` (the State-98 `A` path): entering current mode abandons the velocity loop, and without the reset a `K`-logged `A` run following a `V`/`D` run would log the previous run`s frozen `u_unsat`/`drive_x0` against an unrelated `I_cmd` with flags bit4 set. UNCONDITIONAL, unlike `setManualVelocity()`s entry-edge reset â€” in current mode the controller is never stepped, so there is no operating point to preserve. The first `V` run is still gain identification against a never-tuned velocity loop â€” scope-armed, `A`/`T` (velocity-loop-free) first. | *(pending first flash â€” fw v10 was never flashed either, so the first flash carries v10 + v11 together)* |
| 12 | 2026-08-16 | **Edge-period velocity estimator replaces the position-buffer boxcar. Root-cause fix for the ML0136â€“ML0139 limit cycle. No pin, sequencing, fault, BLG-format or UDP-telemetry change; the drive controller's update law is untouched.** **Root cause:** the four first closed-loop `V` runs (ML0136â€“ML0139) limit-cycled at the drive controller's 16 rad/s design crossover. `updateWheelSpeed()` differenced `encoderPos` across a ring of position/timestamp samples pushed **once per main-loop iteration** (~1.13 ms) over ~98 slots â€” a **~113 ms boxcar with ~56 ms of group delay**, which at 16 rad/s is ~51Â° of lag against the synthesis design's **49.6Â° phase margin**. The entire margin was consumed by a block the synthesis plant never contained; its quantisation was one count per window, **0.0177 m/s**. **The fix:** `updateWheelSpeed()` now measures the **period between consecutive A-rising edges** â€” one edge type on one channel, so each interval spans exactly one full slot pitch (`ENC_SLOT_PITCH_M` = 2Ï€Â·`FLYWHEEL_RADIUS_M`/`ENCODER_SLOTS_PER_REV` = **3.990 mm** of rim travel) and the two OPB829DZ sensors' threshold/duty asymmetry cancels instead of appearing as per-edge ripple. `v = dir Â· NÂ·pitch / Î£(last N periods)`, `N = ENC_PERIOD_AVG_N` (configurable, **default 2**). **ISR:** the tap sits at the end of `doEncoderA()` and reuses the pin levels the quadrature decode already read (`pinA_read == 1` *is* the rising edge on a CHANGE interrupt), adding no `digitalRead`; at 5 m/s the edge rate is 1.25 kHz against ~800 Âµs spacing, so the added subtraction/compare/stores are negligible. **The Ã—2 decode, `encoderPos` and `encEdgeCountA/B` are untouched** â€” `encoderPos` stays the position/direction truth and the estimator takes its **sign** from it (Î”`encoderPos` per cycle is +2 forward, âˆ’2 reverse); a direction flip or an ambiguous zero delta **invalidates the ring** rather than averaging across it. **Reader:** consistent ring snapshot under a brief `noInterrupts()` section (chosen over a sequence-counter retry â€” the copy is far shorter than the 200 Âµs minimum edge interval, so no edge can be lost); the old per-loop `sampleTime` gate is gone. **Zero speed:** standstill when the time since the last accepted edge exceeds `max(1.5 Ã— last period, ENC_VEL_TIMEOUT_US = 150 ms)`, flooring the reportable speed at 3.990 mm / 150 ms = **0.027 m/s** â€” below ~0.03 m/s `v_actual` reads exactly 0. Boot/post-reset read 0 until N periods accumulate; all timing uses the file's unsigned-subtraction idiom, so `micros()` wrap is a non-event. **Glitch rejection:** intervals below `ENC_PERIOD_MIN_US` = 200 Âµs (â‰ˆ20 m/s, 4Ã— the fastest command) are dropped **without advancing the period base**, so the next genuine edge still spans a full pitch. **Plant interface (documented at the estimator):** the reading is the average speed over the last NÂ·pitch of travel, latched once per pitch â†’ effective delay â‰ˆ (N+1)Â·pitch/(2v), **speed-dependent**: ~3 ms at 2 m/s, ~12 ms at 0.5 m/s, ~48 ms at 0.125 m/s (N = 2). Quantisation is timer-limited (<0.1% above 0.2 m/s), **not** the old 17.7 mm/s ladder. Worst case is at LOW speed, so margin checks belong at the slowest tracked speed. **Deleted:** `posArr`/`timeArr`, `averagingTime`/`arraySize`/`BUF_DEPTH`, the buffer-depth `static_assert` and the boxcar's `lastMicros`/`index` statics â€” all function-local, so nothing outside `updateWheelSpeed()` referenced them. `wheelSpeedResetPending` survives with its name and its single producer (`doState3()`) and now calls the new **`encoderVelReset()`** (clears ring, period base, position reference, direction latch), which also runs on a stale timeout. The State-98 `S` dump gains a ring line (`periods=n/N last=â€¦us dir=â€¦ pitch=â€¦mm`) because the estimator adds a second silent-zero source beyond "no counts". **Trace comparability:** BLG `v_act` is the same field carrying finer values â€” fw â‰¤ 11 traces are comparable in **scale** but not in **dynamics** (they carry the 113 ms smoothing; fw 12 does not). **Controller re-synthesised in the SAME round â€” a v12 trace vs a v11 trace is TWO DIFFERENT CONTROL LAWS, not just two estimators.** `controller_design_MIMO/` was re-run against a plant that now contains this estimator: `K_v` recentred to 1.25, weights `WC = 60` / `Wu(0.25, 300, 12.5)`, Î³ = 33.07, `DRIVE_CTRL_KI` 73.63 â†’ 53.42, both SOS sections changed, and `teensy_controller/drive_controller_coeffs.h` regenerated in this same diff. Do not attribute a v11â†’v12 behaviour difference to the estimator alone. **Validity floor â€” the "root-cause fix for ML0136â€“139" claim is QUALIFIED:** the design is gate-checked for **v â‰¥ 0.5 m/s only**. This estimator floors at 0.027 m/s and needs 3 A-rising edges (~N+1 pitches, ~12 mm of travel) before it produces any reading at all, so a commanded step BELOW 0.5 m/s closes the loop around a deadband relay and is **expected to limit-cycle** â€” that is a validity boundary of the design, not an estimator defect. The ML0136â€“139 runs at 0.1 m/s therefore are NOT covered by this fix; only the â‰¥ 0.5 m/s portion of that failure mode is. **Ring-invalidation hold (safety review, MED-HIGH):** a ring invalidated mid-motion (direction flip, or the ISR's ambiguous zero-delta case â€” one noisy B sample suffices on the un-Schmitted OPB829DZ) HOLDS the last valid reading while it refills rather than reporting 0; a 0 at speed is a full-scale error step into a 544.8 A/(m/s) controller for N edge intervals (4 ms at 2 m/s, 16 ms at 0.5), a failure class the old boxcar could not produce. `v_actual` is forced to 0 on exactly three events â€” boot, `encoderVelReset()`, and the stale timeout â€” and the hold is bounded by that timeout (evaluated *before* the ring-depth test for exactly this reason). **Next bench:** re-run the ML0136-class `V` step scope-armed, at **â‰¥ 0.5 m/s**, and treat the first fw v12 run as a margin check of the new law + new estimator together, not a tuning run. | *(pending first flash â€” v10/v11 were never flashed either, so the first flash carries v10 + v11 + v12 together)* |
| 14 | 2026-08-16 | **Drive-controller coefficient regeneration following an upstream `K_F` force-axis correction. No firmware logic change whatsoever** â€” no pin, sequencing, fault, telemetry, BLG-format or command change, and no edit to any control code. The only firmware deltas are the regenerated `teensy_controller/drive_controller_coeffs.h`, the `FW_VERSION` bump, and two stale comments that quoted the old gear ratio. **Upstream cause:** the design plant's force chain carried the wrong gear ratio *and* the wrong radius. `Ï†` **9.49 â†’ 6.86** (the 9.49 was a stock-gearing figure; the fitted pinion is 29T against a counted 70T spur â€” Traxxas 4-Tec manual p.24 formula gives 6.88, the manual's chart cell 6.87, operator rolling counts 2.84â€“2.86 for the shaft/tire stage), and the FORCE radius **0.0762 â†’ 0.033 m**: torque acts through the gearbox on the **tire**, while the encoder and the inertia belong to the **flywheel**, and the two had been conflated. Net `K_F` 0.4516 â†’ **0.7538 N/A (Ã—1.669)**. The drag law rescales with it (`b_eff` 0.32 â†’ 0.534 NÂ·s/m, `F_c` 1.2 â†’ 2.00 N) because it was derived FROM hold currents THROUGH `K_F`, so `i_m0` = 4.07 A is invariant. The old Ã—2 ramp-vs-cruise gain contradiction **dissolves** and **`m_eff` = 3.5 kg is confirmed** â€” the 1.6â€“2.4 kg inferences were `F/a` fits through the understated axis, closing the fw v13 freeze. `K_v` re-centred 1.25 â†’ 1.00, corners â†’ {0.75, 1.00, 1.35}. Re-synthesis needed **no weight change**: the shipped rung (WC = 60, Wu(0.25, 300, 12.5)) passes every gate â€” crossover 17.52 rad/s, PM 50.8Â°, DM 50.6 ms, worst â€–Sâ€–âˆž 2.427 cont / 2.535 disc over 72 corners, PM 41.8Â° at the 0.5 m/s floor. âš ï¸ **A v14 `'V'` trace is NOT comparable with a v13 one: the control law changed** (new coefficients, new `K_I`, new plant gain â€” the loop is Ã—1.34 stiffer in DC plant gain). The VELOCITY chain is untouched â€” `FLYWHEEL_RADIUS_M` 0.0762 m, 240 counts/rev and the estimator are all unchanged, so `v_act` traces ARE comparable across v13/v14. `MOTOR_I_CMD_MAX` unchanged at 12 A. Sources: `controller_design_MIMO/calibration/motor_id_20260815.md` Â§"K_F force-axis correction (2026-08-16c)", `mimo_system_model.md` Â§4.2/Â§4.4, `drive_siso_metrics.txt`. **Replay-tolerance pipeline fix (same round, no firmware effect):** the regenerated coefficients failed `test_drive_controller_replay_regen` at **86.27 mA** against a **hardcoded 50 mA** gate inherited from the fw v12 coefficients. Characterised as benign clamp-boundary dither, not divergence â€” deviation is confined to the saturated transient (k â‰ˆ 213â€“442 of 2500), decays to **1.64 mA** over the last 200 samples, and the unsaturated `'small'` episode replays **bit-exactly**. Root cause of the 25 mA generation-time gate passing while C++ measured 86 mA: the gate perturbed only the *stimulus* while keeping numpy/BLAS summation order, and the **dominant** perturbation is the **arithmetic order** of the dot products â€” `driveControllerStep()` accumulates `CdÂ·x`/`AcÂ·x` with sequential scalar loops, worth **67.68 mA on its own** versus 19.07 mA for float32 stimulus storage, because the trajectory approaches a clamp decision boundary to within **255.9 ÂµA**. Fix: `synthesize_drive_siso.py` now replays the episode through the firmware's exact arithmetic, measures all four sensitivities, derives the tolerance as 2Ã— the worst rounded up (**180 mA** this round), interpolates every figure into the CSV/coefficient-header/metrics comments (all stale literals removed), and gates on non-divergence + tail decay; the tolerance ships machine-readably as `DRIVE_REPLAY_{SMALL,REGEN}_TOL_A` via `tools/gen_drive_replay_header.py`, which the replay tests now read instead of hardcoding. **Tests: 2860 production + 175 bench pass** (+2: generated-tolerance sanity and tail-decay checks). `validate_drive_siso.py` 15/16, the documented float32-state exception. | â€” (not yet flashed) |
| 13 | 2026-08-16 | **Edge-corruption hardening of the fw v12 edge-period estimator, plus the `v_setpoint` zero cutoff. No controller-coefficient, plant-model, telemetry or BLG-format change; no pin, sequencing or fault change.** The `controller_design_MIMO/` artefacts (coefficients, plant model, `m_eff`, `K_F`) are **frozen** this round pending a separate operator investigation â€” the operator's ruling is that `m_eff = 3.5 kg` is a *floor* from the flywheel inertia measurements, and the observed gain discrepancy most likely lives in `K_F`. **Root physical cause of all three estimator defects** (scope capture 15, `references/scope_captures/`): the OPB829DZ is a bare phototransistor with a 4.7 kÎ© pull-up and **no hysteresis**, so the channels present full-swing signals with ~0.5â€“1 ms *analog* edge ramps and the Teensy input re-crosses its threshold on ramp noise â€” producing both extra and swallowed edges. **A Schmitt-buffer hardware bodge is the real fix and is planned separately; this round is the firmware backstop**, and every mechanism below stays correct and beneficial after the Schmitt lands (the filters simply stop finding anything to reject). **(a) Adaptive period plausibility** (`ML0145`, `ML0143`). fw v12's glitch floor was a fixed `ENC_PERIOD_MIN_US` = 200 Âµs (â‰ˆ20 m/s) against real bench periods of **0.8â€“11 ms**, so it caught nothing: ML0145's spurious A-edges split one pitch into two multi-millisecond halves, putting `v_act` on rungs at **1.33Ã— and 2Ã— true speed** and reaching **100 % contamination at low speed** â€” where the trace looks perfectly clean while reading uniformly double. The test is now **relative** to an EWMA (Î± = 1/4) of accepted per-pitch periods (`encPeriodRefUs`), armed after `ENC_PERIOD_REF_SEED_N` = 2 accepted periods. *Low side:* a period below **0.625 Ã— ref** (shift-only form of the ~0.6 design threshold) is a spurious-edge artefact and is dropped **without advancing the period base**, so the next genuine edge spans the full true pitch and the two halves **merge**. Physics bound: the per-pitch fractional period change under acceleration is `aÂ·pitch/vÂ²`, a few percent at any speed where the ring is populated, so genuine consecutive periods cannot approach halving. *High side* (ML0143's missed edges, ~68 events/s at 3 m/s, speed/current-independent, trimodal `v_act` at 1, 2/3 and 1/2 of true speed): a period near `k Ã— ref` means **k pitches were traversed**, so it is **interpreted, not rejected** â€” the ring entry stored is `period/k`, the mean per-pitch period, which is the correct distance/time for an unchanged reader. `k` is nearest-integer against shift-built 1.5Ã—/2.5Ã—/3.5Ã— midpoints and **bounded at `ENC_PERIOD_MAX_MULT` = 3**; beyond 3.5Ã— ref the period is accepted as-is, because genuine strong deceleration is the likelier explanation and reading slow is the safe direction. Rejecting long periods instead would starve the ring during exactly the decel where the loop needs a reading. **Speed arming (safety review S1 + S2) â€” the mechanism is deliberately incomplete at low speed.** The entire adaptive mechanism (low-side gate *and* `k > 1` reinterpretation) is armed **only while `ref < ENC_ADAPT_MAX_REF_US` = 13000 Âµs**, i.e. above ~**0.307 m/s**. Below that, the *true* per-pitch ratio `T(n+1)/T(n) = 1/sqrt(1 + 2Â·aÂ·pitch/vÂ²)` crosses both thresholds on **genuine** rail transients: it falls under the 0.625 gate when `vÂ² < 1.667Â·aÂ·pitch` (v < 0.22 m/s at a = 6.96 m/sÂ², so a hard launch has its **true** edges rejected, the merged interval is then accepted as k = 1, and v reads mÃ— **low** while dragging `ref` up â€” recovering only via the 100 ms reading-age reset), and it exceeds the 1.5Ã— k = 2 midpoint when `vÂ² < 3Â·aÂ·pitch`, which can drop `ref` into the **k = 2 basin** (0.40Tâ€“0.667T) where every genuine period is halved and **re-confirms the poison** â€” v reading exactly **2Ã— forever on a clean trace**, in the destabilising direction (reads fast â†’ brakes harder). The arm speed is set by the stricter (decel) condition, `v_arm = sqrt(3Â·a_maxÂ·pitch)` = 0.289 m/s, with ~6 % margin. **Stated plainly: the 0.04â€“0.30 m/s regime is unmitigated by firmware** â€” which is exactly where ML0145's contamination was worst. That is not an oversight: in that band a spurious edge and a hard-launch truth produce the **same period ratio**, so no period-ratio filter can separate them. **That band belongs to the planned Schmitt-buffer hardware fix**, which removes the spurious edges at the source. **Poisoned-reference tripwire (S1 backstop):** `ENC_KBRANCH_RUN_MAX` = 4 consecutive accepted periods all taking the `k > 1` branch raise `encRefPoisonPending`, and `updateWheelSpeed()` performs a full `encoderVelReset()` â€” a genuinely persistent miss stream is indistinguishable from a poisoned reference and a reset is correct for both. The reset runs in the **reader**, not the ISR, because it writes reader-side hold state (documented deviation from the review's wording; effect is identical within one ~1.13 ms main-loop tick). `ENC_PERIOD_MIN_US` is **kept** as the absolute floor beneath the adaptive one, and is the bootstrap's only protection for the first two periods off standstill (documented breakaway caveat; self-correcting within a few pitches and capped by (b)). **Delay model unchanged:** a rejected edge never advances the base, so accepted periods still span *true* pitches and a clean edge stream still measures the nominal `(N+1)Â·pitch/(2v)` â€” the filter costs the plant model nothing. **(b) Reading-age stale bound** (`ML0140`, `ML0145`). fw v12's stale timeout was keyed to the age of the last raw **edge**, so it could never fire in the case that mattered: under direction dither the ISR's `dpos == 0` and flip branches re-invalidated the ring **faster than it refilled** while edges kept arriving fresh, and the reader held a stale â€” at a flip, **wrong-signed** â€” value for **120â€“560 ms** (ML0145 shows a **468 ms frozen hold** at breakaway, ~0.4 m/s). `updateWheelSpeed()` now also bounds the age of the last accepted **reading** (a completed `v` computation) by `max(ENC_VEL_STALE_K Ã— that reading's period sum, ENC_VEL_TIMEOUT_US)`, zeroing `v_actual` and resetting the estimator when exceeded, **regardless of the raw edge stream**. Holds *shorter* than the bound behave exactly as in fw v12. `ENC_VEL_TIMEOUT_US` drops **150 ms â†’ 100 ms** now that it is actually reachable, raising the reportable-speed floor to 3.990 mm / 100 ms = **0.0399 m/s** â€” deliberately kept **below** `V_SP_ZERO_THRESH`; that ordering is load-bearing and must survive any future edit to either constant. **(c) Reversal as data** (`ML0140`). A confirmed flip already stores the flipping edge's own period, so one valid same-direction period exists immediately; fw v12 nonetheless waited for a full ring. The reader now averages over **whatever the ring holds** (`cnt`, not a fixed `N`), so a signed reading is available after the **first** full same-type period in the new direction, returning to `N`-averaging as the ring fills. Cost: a `cnt = 1` reading is a single-pitch measurement, `N`Ã— less averaged â€” **accepted**, because a noisier reading of the right sign beats a clean one of the wrong sign. **Sign embargo (safety review S3):** a `cnt = 1` reading whose **sign differs** from the last published one is **held**, not published, until `cnt â‰¥ 2` corroborates the reversal; sign-*matching* `cnt = 1` readings stay allowed, preserving the fast warm-up and the same-direction partial-ring value. Without it the ML0140 dither publishes a **sign-alternating single-pitch reading at the dither rate** â€” a *stronger* excitation into the 544.8 A/(m/s) controller than the frozen hold it replaced â€” and, decisively, each publication refreshed `encVelLastReadingUs`, so (b) could **never** age the dither out. An embargoed tick therefore must **not** touch `encVelLastReadingUs`. **Correction to an earlier draft of this row:** (c) alone does *not* terminate the ambiguous `dpos == 0` / dither starvation case â€” it is (b) **plus the embargo's refusal to refresh the reading timestamp** that does, and only because embargoed ticks let the hold genuinely age. **(d) `v_setpoint` zero cutoff** (`ML0144`). Commanding `v_setpoint = 0` *through* the velocity loop is closing the loop **below its own validity floor** â€” the estimator reads exactly 0 under ~0.04 m/s, so the loop wraps a 544.8 A/(m/s) controller around a **deadband relay**. ML0144 recorded sustained **90 %-rail Â±12 A bang-bang**; the same run at 1.0 m/s settled cleanly. `motorControl()` â€” **both** build paths, Youla and the PI fallback â€” now commands 0 A and holds the controller reset while `|v_setpoint| < V_SP_ZERO_THRESH` = **0.05 m/s** (above the 0.0399 m/s estimator floor, far below the 0.5 m/s slowest gate-checked synthesis speed), skipping the controller step entirely. The reset fires on the **entry edge** only, so the exit is already clean under the existing reset semantics. This makes State 2 and the State-98 profiles behave the way `doState1()` (Idle) already did â€” Idle commands 0 A directly rather than regulating a zero setpoint. **Behavioural consequence for the profiles:** segments commanding genuine zero speed now **coast at 0 A** instead of being actively regulated to a standstill the estimator could not observe anyway â€” `DRIVE_CYCLE` phases 0 and 5 (standstill) plus the tails of phases 1 and 3, and `COMBINED_PROFILE` regions 0 and 15 plus the ends of regions 1 and 14. The regen hold (âˆ’0.5 m/s) and every other waypoint are above the threshold and unaffected; at the default `Y_VMAX_DEFAULT` = 1.0 m/s the smallest non-zero normalised waypoint is 0.2 m/s, 4Ã— the cutoff, so no `Y` plateau is clipped. **Safety review S4:** on the PI-fallback build `pi_motor_lastMicros` is refreshed on **every** cutoff tick (the accumulator clear stays entry-edge only) â€” a frozen `dt` reference would make the first tick after the setpoint rises integrate the *entire* cutoff duration in one step and slam the integrator to `integMax`, turning the cutoff into a windup source instead of a reset. **Safety review S5:** `'Y'` warns at parse time when `Vmax Ã— 0.2` (region 13, the table's smallest non-zero waypoint) falls below `V_SP_ZERO_THRESH`, so a deliberately slow bench run's flat 0 A trace is not misread as a controller failure; accepted with a warning, not refused. **Safety review S6:** `static_assert`s now pin the estimator-floor `<` `V_SP_ZERO_THRESH` ordering (`V_SP_ZERO_THRESH` promoted to `constexpr` for it) and tie `ENC_PERIOD_LO_FRAC` to the `(ref>>1)+(ref>>3)` shift form it documents. **Next bench:** re-enter the ML0143/ML0145 conditions scope-armed and confirm the `v_act` rungs (1.33Ã—, 2Ã—, 2/3, 1/2) are gone and that no *genuine* accel/decel transient is being filtered into a frozen reading; re-enter the ML0140 dither case and confirm every hold now terminates within ~100 ms; re-run ML0144's `v_sp = 0` segment and confirm 0 A coast in place of the bang-bang. The Schmitt bodge remains the root fix â€” this backstop is not a reason to defer it. | *(pending first flash â€” v10â€“v12 were never flashed either, so the first flash carries v10 + v11 + v12 + v13 together)* |
| 15 | 2026-08-17 | **`dpos`-based pitch count in the edge-period estimator ISR â€” the estimator's last ratio *inference* becomes a decoder *measurement*, and both reference-poison basins are eliminated. No pin, sequencing, fault, UDP-telemetry (v4/58 B), BLG-format (record v5/76 B) or controller-coefficient change; the entire reader side (`updateWheelSpeed()`, the holds, the sign embargo, the reading-age bound) is untouched and the estimator's `(N+1)Â·pitch/(2v)` delay model is unchanged, so `v_act` traces stay comparable with fw v12â€“v14.** **Defect.** The fw v13 adaptive filter had an **absorbing slow-reading basin at `ref â‰ˆ 2T`** (`T` = true per-pitch period). Every genuine edge then arrives at 0.5 Ã— ref, fails the 0.625 low-side gate and is rejected **without advancing the base** â€” which is the *correct* handling of a real spurious edge and is what makes the ML0145 split pair merge. The next genuine edge therefore measures `2T` from the un-advanced base, i.e. ratio 1.0 against ref, and is accepted as a **single** pitch; the EWMA is fed `2T` and re-anchors the basin. `v_actual` reads **exactly half** true speed indefinitely **on a clean trace**, so the drive controller sees double the speed the car is making. The `ENC_KBRANCH_RUN_MAX` tripwire could not catch it: it counted *consecutive `k > 1` acceptances*, and in this basin every acceptance is `k == 1`, which **reset** the counter â€” it only ever guarded the mirror-image `ref â‰ˆ T/2` fast-reading basin. Entry is cheap: a missed-edge burst that trips any reset re-seeds `ref` with **no ratio gating at all** (`ENC_PERIOD_REF_SEED_N` = 2 accepted periods), so a reset taken mid-burst can seed straight into the basin and lock when the burst ends. **Bench motivation:** an abrupt apparent **2Ã— speed-up** observed 2026-08-17, and the ML0151 t â‰ˆ 27.5 s "speed collapse" of 2.55 â†’ 1.30 m/s (**ratio 1.96**) is a candidate instance of the same mechanism â€” noting that the ML0151 drag step-change reading is *not* thereby retired, since the two explanations are separable by bus input power, which did change. **Fix.** `pitches = nearest-integer(|Î” encoderPos| / 2)`, minimum 1, computed as `(|Î”| + 1) >> 1` â€” shift/add only, no float, and the single pre-existing `period /= pitches` UDIV is unchanged, so ISR cost is unchanged. It is sound because a **rejected edge advances neither `encLastEdgeUs` nor `encPosAtLastEdge`**, so the position delta accumulates across rejections in lockstep with the period measurement: an accepted interval that really spans `k` pitches carries `|Î”| â‰ˆ 2k` regardless of what the reference believes. Being a **count** rather than a ratio, it requires no reference and **no speed arming** â€” it runs during ref seeding and below `ENC_ADAPT_MAX_REF_US`, where the S1/S2 arming rationale (ratio ambiguity under hard accel/decel) simply does not apply. Both basins become non-stable: at `ref â‰ˆ 2T` the `2T` interval counts as **two** pitches, stores `T` and feeds `T` to the EWMA, walking `ref` back to truth; at `ref â‰ˆ T/2` the true period passes the gate and counts as one pitch, again feeding `T`. The seed-poisoning entry path therefore needs no separate hardening. **Removed:** the ratio-derived `k = 2/3` branch; `ENC_PERIOD_MAX_MULT` (the count is **uncapped** â€” a long rejection streak legitimately spans more than 3 pitches, and capping would store a per-pitch period knowingly *too long* for the distance actually travelled, i.e. read fast); and the `ENC_KBRANCH_RUN_MAX` tripwire with `encKBranchRun` / `encRefPoisonPending` and its `updateWheelSpeed()` consumer. The tripwire is not merely unused but **actively wrong** under the new mechanism: escaping the `2T` basin produces several consecutive `pitches == 2` acceptances, so a run-length reset would fire during exactly the **recovery** it was meant to protect, discard the ring and re-seed from the same corrupted stream. Its removal also deletes the one path that injected a full-scale `v = 0` step into the 544.8 A/(m/s) controller on the basis of an *inference* rather than a measurement. **Kept as-is:** the 0.625 low-side gate and its no-base-advance merge (with its S1/S2 speed arming, now the only thing that arming governs â€” the threshold *value* is left at the S1-derived figure, which is the stricter of the two conditions), `ENC_PERIOD_MIN_US`, the EWMA, the direction handling and the `dpos == 0` ring invalidation, the ring, and every reader-side semantic. **Known residual:** if **both** edges of one slot are lost on channel A, the `Afirst*`/`Bfirst*` handshake loses that pitch's counts too, so `|Î”|/2` under-reads by one pitch and the interval is stored **slow**. That is the safe direction (reads low â†’ the loop drives, it does not brake) and the EWMA absorbs it; the ratio cross-check that would have caught it is the very mechanism whose ambiguity created the basins, so it is deliberately **not** reinstated. The 0.04â€“0.30 m/s band and the underlying un-Schmitted OPB829DZ edge corruption still belong to the planned **74HC14 hardware fix**, which this round is not a reason to defer. **Observability (fw v8 lesson):** diagnostic-only `encLastPitches` and `encMultiPitchCount` are added and printed in the State-98 `'S'` dump alongside `ref`; no control path reads either. **Review round (two-lens, no HIGH/MED):** S1 (LOW, accepted-strengthened) added a **per-pitch absolute floor** -- after the pitch division, a per-pitch period below `ENC_PERIOD_MIN_US` (200 us) is dropped exactly like a glitch (no store, no EWMA, no base advance), closing the uncapped-count underflow/fast-reading path and serving as the principled fast-direction backstop (per-pitch >= 200 us bounds indicated speed at the ~20 m/s the floor always implied -- S2's arbitrary count cap rejected in its favor); S3 (LOW, doc-only) documents that the `dpos == 0` branch still feeds the EWMA the raw elapsed interval, biasing `ref` high under dither -- conservative direction only. | *(pending first flash â€” v10â€“v14 were never flashed, so the first flash carries v10 + v11 + v12 + v13 + v14 + v15 together)* |
| 16 | 2026-08-17 | **BLG record format v6 â€” encoder / velocity-estimator diagnostics in the bench log. Observability only: no estimator, control-law, coefficient, pin, sequencing, fault, UDP-telemetry (v4/58 B) or command change.** MOTIVATION: the fw v15 reference-poison basin took a bench session to diagnose because the log carried `v_act` and nothing behind it â€” neither the raw position that would have shown the exact Ã—2 scale error, nor the EWMA reference that was holding the basin. Four fields are **APPENDED at the END** of `BenchLogRecord`, so every v1â€“v5 field keeps its offset and the record grows **76 â†’ 92 B**: offset 76 `encoder_pos` (`int32`, `encoderPos` at sample time), 80 `enc_period_ref_us` (`uint32`, `encPeriodRefUs`), 84 `enc_multi_pitch_count` (`uint32`, the fw v15 missed-edge counter), 88 `enc_spurious_drop_count` (`uint32`, NEW). Header layout, trailer format and the `flags` bits are unchanged; only `hdr[4]` (5 â†’ **6**) and `hdr[5]` (the record size, which decoders already read from the header) differ, so a v5 decoder needs only the four tail fields. `encoder_pos` is **estimator-free ground truth** â€” differencing it across a decoded run yields a velocity that owes nothing to the ISR filter, separating a scale error, a poisoned reference and a genuine speed change into three distinguishable offline signatures. NEW COUNTER `encSpuriousDropCount`: one increment per interval **dropped** by any of the three drop paths in `doEncoderA()`'s velocity tap (the absolute `ENC_PERIOD_MIN_US` floor, the adaptive 0.625 Ã— ref low-side gate, and the fw v15 per-pitch floor after the pitch division) â€” the **spurious**-edge rate, complementing `enc_multi_pitch_count`'s **missed**-edge rate; together they bound the un-Schmitted OPB829DZ front end's edge integrity directly and make the pending 74HC14 bodge measurable rather than merely plausible. Also printed by the State-98 `'S'` dump beside `ref`/`lastPitches`/`multiPitch`. The last two fields are **CUMULATIVE counters - decoders diff consecutive samples for a rate**, while `enc_period_ref_us` is the estimator's EWMA state (a level in microseconds - read directly, never differenced); all three are cleared by `encoderVelReset()` so they share one epoch (a reset reads as a step to zero - a NEGATIVE diff means reset, not wrap), and `uint32` wrap of the two counters is unhandled (acceptable at bench timescales). Sampling stays a plain volatile 32-bit load per field in the 1 kHz path â€” atomic on the Cortex-M7, no added IRQ masking, no card I/O â€” and is filled on **both** build paths. Ring math re-checked at 92 B/ms: 1024 records = 94208 B (92 KB, still DMAMEM/RAM2) covering 1.024 s; the drain floors a 512 B chunk to 5 records = 460 B (5.0Ã— catch-up, was 6.0Ã—) and the 32 MB preallocation covers ~6.1 min (was ~7.4) â€” no buffer constant grew. New `offsetof` static_asserts pin the v5 and v6 offsets so a reordering edit fails the build. **`v_act` traces stay comparable with fw v12â€“v15** (nothing in the estimator or the control law moved). |
| 17 | 2026-08-17 | **Velocity-estimator integrity: the Ã—2 rounding basin and the mid-run `v = 0` injection.** Two independent defects found in the ML0164â€“ML0180 analysis round, both of which put a wrong velocity into the ~545 A/(m/s) drive loop. **No BLG format change (stays v6 / 92 B), no UDP, command, pin, sequencing, fault, controller or coefficient change.** (1) **FRACTIONAL-PITCH LEDGER.** fw v15 rounded the decoder pitch count `pitches = (|Î” encoderPos| + 1) >> 1` to the nearest **whole** pitch, so a spurious mid-pitch A-edge â€” which carries `|Î”| = 1`, a genuine **half** pitch â€” rounded **up**, and a `T/2` interval was stored as a full pitch: `v_actual` read exactly **2Ã— true speed**, self-sustaining once the EWMA seeded at `T/2` (the 0.625 low-side gate is keyed to `ref`). Every run in the batch entered it at breakaway (0.08â€“0.24 m/s); the locked runs (ML0164/ML0168) show an accepted-interval rate of exactly 2.00 per true slot and `ref/T_true = 0.500`, escaping only above ~1.0â€“1.6 m/s. `|Î”|` is already in **half-pitch units** (Ã—2 decode), so the stored per-pitch period is now `period Ã— 2 / |Î”|` â€” a ledger, not a count. `|Î”| = 2` takes an arithmetic-free fast path and is **byte-identical** to fw v12â€“v16 (clean-stream `v_act` traces stay comparable); `|Î”| = 1` stores `T`, `|Î”| = 3` (1.5 pitches) stores `2/3 Ã— period` where fw v15 stored `period/2`, and `|Î”| = 2k` stores `period/k` as before. Still **one UDIV** per accepted interval, none in the `|Î”| = 2` common case; the per-pitch `ENC_PERIOD_MIN_US` floor now applies to the fractional value. `encLastPitches`/`encMultiPitchCount` deliberately stay whole-pitch **display** quantities. (2) **MID-RUN RESET â€” ROOT CAUSE.** `updateWheelSpeed()` latched `now = micros()` several instructions **before** snapshotting `encLastEdgeUs`; an A-rising edge accepted inside that window writes a timestamp **greater** than `now`, the unsigned age `(now - lastEdge)` wraps to ~2^32, and the edge-age stale test declared a fast, healthy wheel stopped and forced a full `encoderVelReset()`. Nanosecond window Ã— ~380 accepted edges/s Ã— 25 s runs â‰ˆ 0.5 hits per long run â€” matching YP0166 (t = 26.2398 s: `v_act` â†’ 0 for ~6 ms, both ISR counters cleared, drive controller answered +12 A then âˆ’12 A within 12 ms) and TP0171 (t = 11.844 s, during coast). Fixed by a **signed** age comparison in both age tests: a future timestamp has age 0. (3) **DEFENCE IN DEPTH.** An `encoderVelReset()` taken while the last published `|v|` exceeded `ENC_VEL_CORROB_MIN_MPS = 0.30 m/s` now **captures that reading and holds it** instead of publishing a naked 0, until a FULL-ring reading of either sign corroborates it (depth only, no sign term â€” safety review MED-1: a full-ring opposite-sign reading is a vetted genuine reversal); the magnitude defence against TP0171's half-pitch re-seed (`ref = 925 Âµs`) is the fractional-pitch ledger, not this gate. The hold is bounded at `ENC_VEL_TIMEOUT_US` (100 ms) from the reset, the same class as every other hold in the reader; the two stale paths explicitly disarm it, so boot and standstill zero exactly as before (a State-3 reset above 0.30 m/s now holds the true coasting value for up to 100 ms into Idle â€” log-trace change only). The documented "forced to 0 on exactly FOUR events" contract becomes **three**. (4) **PER-PATH DROP ATTRIBUTION** (diagnostic): `encDropRawFloor` / `encDropLowGate` / `encDropPitchFloor` split the fw v16 `encSpuriousDropCount` sum by cause; the sum keeps its exact semantics and remains the only one logged (v6 decoders untouched), and the three new counters appear in the State-98 `'S'` dump only. Tooling in lockstep: the `encoder_diagnostics` panel (b) now compares the reference-implied speed against the `encoder_pos` truth velocity instead of against `v_act` â€” the old pairing was self-confirming, since both traces are estimator outputs and agree with each other inside a basin. | *(pending first flash; fw v16 is on the board â€” confirmed in every logs-164â€“180 header â€” so this flash carries v17 alone)* |
| 18 | 2026-08-25 | **Encoder wheel replaced: 120 slots â†’ 90 slots, and the constants that derive from the slot pitch.** A HARDWARE change, reconciled in firmware. The flywheel encoder disc was physically swapped on the bench; source is an **operator hand count (2026-08-25) cross-checked against the decoder â€” one hand-turned flywheel revolution reads `encoderPos == 180`**, which confirms the slot count and, again, the Ã—2 quadrature decode. `FLYWHEEL_RADIUS_M` is **unchanged** at 0.0762 m (same flywheel, new disc), so `v_actual` remains flywheel SURFACE speed and the surface-speed convention is untouched. **No pin, sequencing, fault, telemetry (UDP v4 / 58 B), BLG format (stays v6 / 92 B) or command change, and no control-code edit** â€” the ISR, decoder, drive-controller wrapper and both PIs are byte-for-byte as fw v17 left them. (1) **SCALE.** `ENCODER_SLOTS_PER_REV` 120.0f â†’ **90.0f**, so the derived `ENCODER_COUNTS_PER_REV` 240 â†’ **180** and `ENC_SLOT_PITCH_M` = 2Ï€Â·0.0762/90 = **5.3198 mm** (was 3.990 mm). âš ï¸ `v_actual` and the BLG `v_act` column read **4/3 Ã—** their fw v17 value for identical motion: **a v18 trace is NOT comparable with a v12â€“v17 trace**, and pre-v18 logs were taken on a physically different wheel. The header `fwVersion` disambiguates, and `tools/benchlog_analysis/figures.py` now selects the pitch from each log's own `fw_version`. `VELOCITY_CHAIN_CALIBRATED` stays **1** â€” a direct slot count cross-checked against the decoder is the strongest source the chain has had. (2) **`ENC_ADAPT_MAX_REF_US` 13000u â†’ 15000u, RE-DERIVED, not retuned.** The low-side gate's arming speed is the fw v13 S1 decel condition `v_arm = sqrt(3Â·a_maxÂ·pitch)` at `a_max = 6.96 m/sÂ²`: `sqrt(3 Ã— 6.96 Ã— 5.3198e-3) = 0.3333 m/s`, armed ~6 % inside at 0.3533 m/s, `ref = pitch/0.3533 = 15058 Âµs`, rounded to the clean **15000 Âµs** (arms at 0.3546 m/s = 1.064 Ã— v_arm). The firmware-unmitigated low-speed band therefore widens from 0.04â€“0.31 to **0.05â€“0.36 m/s**; it still belongs to the 74HC14 Schmitt hardware fix, which remains outstanding. (3) **`ENC_VEL_CORROB_MIN_MPS` 0.30f â†’ 0.35f**, tracking the arming point it is deliberately pinned to â€” the fw v17 rationale is unchanged, only the speed it names moved. (4) **`V_SP_ZERO_THRESH` 0.05f â†’ 0.07f.** The wider pitch raises the estimator's reportable zero-speed floor (`pitch / ENC_VEL_TIMEOUT_US`) from 0.0399 to **0.0532 m/s**, which the existing `static_assert` no longer admitted against 0.05. 0.07 restores a ~24 % margin â€” the same class as the pairing it supersedes â€” and stays far below the 0.5 m/s slowest gate-checked speed of the drive synthesis. **Zero-cutoff logic is unchanged.** `ENC_PERIOD_MIN_US` stays 200 Âµs; the speed it bounds rises 19.95 â†’ **26.6 m/s** with the pitch, widening the margin over `MANUAL_MOTOR_V_MAX`. (5) **DRIVE COEFFICIENTS REGENERATED.** `controller_design_MIMO/plant_mimo.py` `ENC_SLOTS` 120 â†’ 90 (and `V_EST_MIN` 0.03 â†’ 0.053, the same floor), then `synthesize_drive_siso.py` re-run at the **SAME weight rung WC = 60, Wu(0.25, 300, 12.5) â€” no weight retune**. The estimator transport delay `(N+1)Â·pitch/(2v)` scales with the pitch, so it grew Ã—4/3 at every corner (0.5 m/s: 11.97 â†’ **15.96 ms**; 2 m/s: 2.99 â†’ 3.99 ms; 5 m/s: 1.20 â†’ 1.60 ms). **ALL GATES PASSED**: crossover **17.25 rad/s** (was 17.52), PM **50.2Â°** (was 50.8), DM **50.76 ms**, GM 15.7 dB, 72/72 corners stable, worst-corner â€–Sâ€–âˆž **2.867 cont / 3.017 disc** (was 2.427/2.535 â€” the binding corner is `K_v = 1.35, pole_factor = 0.5, v0 = 0.5 m/s`; the continuous figure passes the < 3 gate but now exceeds the < 2.5 *target*, and the discrete figure is above 3 â€” worth watching, it is the price of the longer estimator delay at the validity floor), estimator phase at crossover **3.94Â°** at 2 m/s, and PM at the 0.5 m/s validity-floor corner **38.4Â°** (was 41.8Â°, gate > 30Â°). `validate_drive_siso.py` **15/16** â€” the one failure is the documented float32-STATE regen divergence (1.555e-2 A), unchanged in kind. âš ï¸ **A v18 `'V'` trace is a DIFFERENT CONTROL LAW from a v17 one**; `figures/drive_siso_step.csv` was regenerated with it and is the correct overlay reference. (6) **ANTI-WINDUP REALIZATION CHANGED â€” a SAFETY fix found by this round's test agent and folded in so it ships in the same flash.** `drive_controller.h`'s state update becomes the GENERAL Hanus conditioned form `x_next = ADÂ·x + BDÂ·e + LÂ·(u âˆ’ u_unsat)`, with a new pole-placed coefficient `DRIVE_CTRL_L`, replacing the fw v10â€“v17 SELF-conditioned special case `L = BD/DD` (`x_next = ACÂ·x + BDÂ·u/DD`). **Root cause:** `eig(AC)` are the discrete controller's transmission ZEROS, and one sits at **exactly z = âˆ’1** â€” structurally, because the controller is a PARALLEL sum of two TUSTIN branches whose numerators both carry `(z+1)` (Tustin of the integrator is `(kIÂ·Ts/2)(z+1)/(zâˆ’1)`), so the factor survives at any weight rung on any plant. Zero damping at Nyquist in the saturated mode let a relay limit cycle live against the Â±12 A clamp. **Measured** on constant-error rail dwells (e âˆˆ [0.25, 12.0] step 0.25, 2000 ticks, shipped float32 coefficients + double state): **fw v17 fails 14 of 48 cases (e = 8.25â€¦11.75), fw v18-pre-fix 15 of 48**, every failure a full **24 A peak-to-peak period-4 (++âˆ’âˆ’) square wave at 125 Hz**. âš ï¸ **The defect shipped from fw v10 onward and is NOT a v18 regression** â€” the v18 coefficients merely moved a basin boundary onto the `e = 5.0` probe the tests used; fw v17 would have failed at `e = 9.0`. Hardware-reachable wherever a large error persists at the rail with no vehicle response, e.g. the ML0151 ~428 ms VESC post-reversal dead window. **Unsaturated behaviour is unchanged** â€” the conditioning term is identically zero off the clamp â€” so `AD/BD/CD/DD`, the corner sweep and every linear gate are untouched. `L` = `[+1.45551145e-1, âˆ’8.581254e-3, âˆ’1.9437848e-2, +2.904284e-3, +2.403627e-3]`, placed by **minimal perturbation** of `eig(AC)`: only the z = âˆ’1 mode moves (to **+0.5**); the slow positive-real modes (0.999695, 0.983988) are deliberately left alone, because pulling them inward destroys the integrator's conditioning memory (measured on that attempt: âˆ’0.227 m/s standing error and a 17.9 mm/s hunting cycle, six Â§8 gates failed). Achieved `eig(AD âˆ’ LÂ·CD)` = {âˆ’0.591196, +0.245326, +0.500000, +0.983988, +0.999695}. **Two NEW synthesis gates (Â§8e):** (a) every *oscillatory* (non-positive-real) eigenvalue < 1 âˆ’ 1e-3 â€” worst is 0.591196; the slow real modes are exempt as a documented deviation, since a decaying exponential cannot sustain oscillation and forcing margin on them breaks the controller; and (b) **load-bearing**, the 48-case dwell sweep, worst tail p-p **0.000e+00 A**. Gate (a) is acceptance-checked to FAIL both the fw v17 (âˆ’1.0000009) and fw v18-pre-fix (âˆ’0.9999990) spectra â€” a plain `max|eig| < 1` test would have passed the latter and still limit-cycled. A third replay episode **`dwell`** (600 ticks at e = 5.0 m/s, tolerance 0.10 mA) ships in `drive_replay_vectors.h` as the firmware-side regression; neither existing episode could detect this (`small` never clamps, `regen` releases after ~0.8 s). `validate_drive_siso.py` is now **17/17** â€” the long-standing float32-STATE "expected failure" is GONE: re-measured, a float32 state recursion diverges only ~1.1e-5 A on the regen episode (was ~1.4e-2) because the conditioned trajectory no longer rides the clamp boundary. **The state stays DOUBLE regardless** (only ~2 decades of margin, and nothing gates float32 off these two trajectories). Stale claims in `drive_controller.h` and the emitted coefficient header were corrected in lockstep. | *(pending first flash; fw v16 is on the board, so the next flash carries v17 + v18)* |
| 19 | 2026-08-25 | **Two changes: encoder fields on the `'L'` Serial-Plotter stream, and a conduction-aware handoff slew for the droop share ratio. No pin, sequencing, fault, controller-coefficient, BLG-format (stays v6 / 92 B) or UDP-telemetry (stays v4 / 58 B) change; no edit to the motor/drive controller, the encoder ISRs or estimator, the quadrature decode, `applyShareRatio()`, the share controller, or the PIs.** **(a) `'L'` stream: eight fields -> ELEVEN.** Appended after `v_act`, in order: `enc_pos` (`encoderPos`, the raw x2 quadrature count), `edgeA` (`encEdgeCountA`) and `edgeB` (`encEdgeCountB`). Operator request: watch position and raw per-channel edge counts live while hand-rotating the wheel, which separates "a channel is dead" from "the decode is wrong" without dropping into the `'S'` dump - the fw v8 observability lesson applied to the live stream rather than the dump. All three are ISR-written diagnostic volatiles already read by the `'S'` dump (and `encoderPos` by `logSampleTick()`); they are read as plain single 32-bit reads, atomic on Cortex-M7, and deliberately NOT snapshotted as a set (one-edge skew is irrelevant at the 20 ms plot cadence). No control path reads them and no new state is introduced. The fixed-field-count rule is UNCHANGED - the IDE plotter keys its series off the labels and re-legends the graph if the count varies mid-run, so it is eleven now, always eleven. Backpressure guard **110 -> 192 B**. The itemized worst-case line is labels+separators 76 + 8 floats x 7 B = 56 + int32 11 + 2 x uint32 20 + CRLF 2 = **exactly 165 B**; 165 was rejected in review as an exact fit rather than a guard (one 8-character float such as `-123.456` overruns it, and Teensy USB-CDC `write()` then BLOCKS - the loop stall this guard exists to prevent), so 192 ships, leaving ~one extra character per float of headroom. The itemization lives at the guard so any future field is re-derived, not estimated. **(b) Conduction-aware handoff slew (TP0201 mitigation, safety-relevant).** New `SHARE_HANDOFF_MIN_A` = **0.15 A** (dark threshold), `SHARE_HANDOFF_LIVE_A` = **0.20 A** (live re-entry), `DROOP_RATIO_SLEW_HANDOFF_PER_TICK` = **0.002/tick** (10x slower than `DROOP_RATIO_SLEW_PER_TICK` = 0.02) and `SHARE_HANDOFF_DWELL_MAX_TICKS` = **175** (~200 ms at the ~880 Hz loop rate, counted only on ticks where the ratio actually MOVED). `updateShareSlewMode()` runs **once per `powerBalance()` tick**, above the open-loop feedforward branch so feedforward ticks advance the state too, and stores the tick's ceiling in `shareSlewStepThisTick`; **all three** in-band slew sites (open-loop feedforward, fw v6 effective-setpoint reference, closed-loop actuation) READ that stored value, which makes the fw v6 requirement - reference and actuation cannot disagree about how fast the split may move - structural rather than accidental. **Evidence chain - TP0201 (fw v18, 2026-08-25):** the share loop armed OPEN->CLOSED while FC was the sole conductor (`share_act` ~ 1.0, `I_batt` 0.06-0.08 A of trickle). Seeded at `droopSlew_prev` ~ 0.85, the controller walked toward the governor-clipped ~0.556 at the full 0.02/tick, crossing the FC->BT conduction handoff in ~20 ms. FC ceased conducting - **`V_fc` ROSE 0.66 V, the unloaded signature** - before BT's reactive RT1987 ideal diode picked up; the bus ran **unsourced for ~5.7 ms** and decayed capacitively **15.86 -> 12.185 V**, 0.185 V above `LIMIT_V_BUS_MIN` and 10 ms inside the 20 ms fault dwell, so nothing faulted. TP0200 took the identical arming step harmlessly because BT was already conducting 0.556 A. The hazard precondition is therefore NOT arming but: **the ratio is slewing while a channel that must pick up load is not meaningfully conducting** - which is why the fix is keyed to conduction, not to the arming transition. The `V_fc` rise also **refutes the supply-transient hypothesis for TP0201**; see `docs/boost-bringup-debug.md`. **Review-round mechanism detail (all three findings changed the shipped design):** the conduction test runs on per-channel **EMAs** of `fabsf(I_fc)`/`fabsf(I_batt)` at `SHARE_GOV_FILT_ALPHA` (0.05/tick, ~20 ms - the same weight and doctrine as `share_govTotAFilt`), not on raw ADC reads, because a raw test would chatter the ceiling near 0.15 A and the governor doctrine forbids a raw measurement gating the reference; and the dark/live decision is **hysteretic** (dark below 0.15 A, live only at 0.20 A) so the boundary cannot dither. The filters advance only on ticks that reach the update, which is correct because both earlier returns in `powerBalance()` (the setpoint-latch cutoff and the `SHARE_I_TOT_MIN_A` minimum-load gate) freeze the entire loop - no slew site can consume a stale value. State is seeded **DARK** at boot and by `resetShareControlState()`: dark selects the slower, more conservative ceiling, so a cold start or reset cannot begin by slamming the ratio across an unmeasured conduction state. **The `SHARE_HANDOFF_DWELL_MAX_TICKS` cap is load-bearing, and an earlier draft of this feature's rationale was WRONG.** That draft claimed the fw v6 review-S4 regime and the handoff regime were disjoint (S4 accepts a ~35-tick governor floor-clip walk because it is far shorter than a ~50-60 ms TP0010/TP0013 dropout cycle). **The claim is struck:** the dropped-out half of a dropout cycle IS a sub-0.15 A channel, and S4's post-load-fall walk is the same dark-minority regime, so the reduced rate applies exactly where S4's bound was load-bearing. What actually bounds the exposure is the dwell allowance - each **distinct** dark event gets **one** allowance of at most 175 slow ticks ON WHICH THE RATIO ACTUALLY MOVED, after which the FULL rate resumes even while the channel stays dark, the allowance re-arming only on a LIVE transition. So S4's bound is **relaxed, not preserved**: at most `SHARE_HANDOFF_DWELL_MAX_TICKS` of slow WALK plus the fast remainder, once per dark event (the motion gate leaves that walk-length bound intact - S4's hazard is the walk itself, not elapsed time), instead of the unbounded ~350-tick slow walk an uncapped design would have permitted. **The MOTION GATE on the dwell counter is load-bearing, and its absence was a HIGH finding in the orchestrator's final review (O1).** An elapsed-tick counter would have defeated this mitigation in the very scenario it was built for: TP0201's pre-arm was a ~1.7 s (~1500-tick) **in-band feedforward HOLD** at sp = 0.85 with BT dark, and every one of those ticks passes the latch and minimum-load gates and selects the handoff rate - so the 175-tick allowance is spent ~1.3 s BEFORE the closed-loop arming, and the arming walk (the hazardous conduction crossing itself) then runs CAPPED at the FULL rate. Motion is detected as a delta in `droopSlew_prev` - the MDAC-truth ratio `applyShareRatio()` records - between consecutive `updateShareSlewMode()` calls, giving a deliberate one-tick lag that only makes the budget one tick more generous. The CEILING selection is NOT motion-gated (it must already be the handoff rate on the first moving tick); only the BURN is. `shareHandoffPrevRatio` is seeded FROM `droopSlew_prev` in `resetShareControlState()` rather than from a literal, because that function deliberately does not touch `droopSlew_prev` and a literal seed would read as spurious first-tick motion. Consequence for the `'S'` dump: CAPPED means the allowance is SPENT, not that the channel has been dark a long time - a long static hold reads HANDOFF with dwell unchanged. A short slow \*\*prefix\*\* followed by the full rate was considered and **rejected**: TP0201's conduction crossing sat at the END of its walk (gains roughly equal ~24.5 ms in), so a prefix-only allowance would not have covered the very event this mitigates. **Known residual, DEFERRED:** at the handoff rate the slew limiter becomes the dominant actuator dynamic and is invisible to `youlaController_Power()`, whose anti-windup back-calculates only against its own [0,1] authority span - its integrator can advance against a split the MDACs have not reached. Exposure is bounded by the dwell cap (~200 ms per event); extending the anti-windup would mean editing the share controller, which is on the do-not-change list, so it is documented at the actuation site rather than fixed. **Observability (`'S'` dump):** a new `share slew mode:` line reports FULL / HANDOFF / CAPPED, both filtered magnitudes with the two thresholds, the dwell counter against its cap, and the tick's step. This is the **only** observable for the mode - it is NOT reconstructible from the logged raw currents (filtered + hysteresis + dwell state, none of which are in the BLG record), and that is noted in the benchlog skill's `log-conventions.md`. **Operator note:** a setpoint-latch release with the re-closed channel still dark walks at the handoff rate for up to ~200 ms before reverting; that lag is the mitigation working, not a stuck loop. **Behaviour invariants:** converged holds with both channels conducting are **bit-identical** to fw v18 (full rate; `constrain()` lands exactly on target once within one step); no change to arming or seed logic, the setpoint latch, governor bounds, `applyShareRatio()`, or the Youla share controller. **NEXT BENCH - both gates are required before this mitigation is trusted, and the S4 relaxation is argued, not measured:** (1) repeat the **light-load dropout condition** (the TP0010/TP0013 class) and confirm the dwell-capped walk does not lengthen a dropout cycle; (2) **re-validate the fw v19 share sweep** end-to-end, watching for a settling overshoot after a dark-event walk (the deferred anti-windup residual); (3) repeat the TP0200/TP0201 arming condition with a deliberately dark standby channel and confirm the bus no longer decays below ~15 V through the handoff - scope-armed, since the ~5.7 ms gap is at the edge of what the ~882 Hz log resolves. Read the `'S'` dump's slew-mode line on every run. | *(pending first flash; fw v18 is not yet flashed either, so the next flash carries v18 + v19)* |
| 20 | 2026-08-25 | **BLG bench-log RECORD FORMAT v7 (92 B -> 106 B, `hdr[4]` = 7): the two raw per-channel ISR edge counters plus three new quadrature phase / duty-asymmetry statistics.** Observability only. No pin, sequencing, fault, command, controller-coefficient or UDP-telemetry (stays v4 / 58 B) change; the header LAYOUT is unchanged (only `hdr[4]` and `hdr[5]`, the record size decoders already read from the header, differ), and every v1-v6 record field keeps its byte offset. **(a) Edge counters:** `enc_edge_count_a` (uint32, offset 92) and `enc_edge_count_b` (uint32, offset 96) - every `ENC_A`/`ENC_B` CHANGE interrupt taken, pre-decode and pre-filter. The v6 counters describe what the ESTIMATOR did with the edge stream; these describe the stream itself, which makes them the direct before/after acceptance metric for the pending 74HC14 Schmitt bodge and the only signal that separates a DEAD CHANNEL from a DEAD ESTIMATOR offline (A rising with B flat = sensor fault; both rising with `encoder_pos` flat = decode/quadrature fault; both flat = no motion). Until fw v20 they existed only LIVE, on the fw v19 `'L'` stream and the `'S'` dump. **(b) Geometry levels (new firmware feature, not just a log field):** `enc_phase_ewma` (uint16, offset 100), `enc_duty_a_ewma` (102), `enc_duty_b_ewma` (104) - shift-based integer EWMAs (alpha = 1/4, the fw v13 reference-filter idiom), fixed point in **1/256ths of one slot pitch**, computed entirely in the ISRs. Phase is the last ACCEPTED A-rising edge to the next B-rising edge over `encPeriodRefUs` (the sensor pair's MOUNT geometry); the duties are each channel's rise-to-fall high time over the same reference (that channel's OPTICAL/threshold health, independent of the mount). Phase alone cannot separate a mis-mounted sensor from an off-centre logic threshold on a slow phototransistor edge - duty moves for the second and not the first - so together they **replace a scope for verifying the quarter-pitch offset UNDER ROTATION**, the only condition that matters and the one a static probe cannot reproduce. **Expected healthy on this board: phase 0.25 pitch (64), both duties 0.50 (128).** The 90-slot wheel's sensor offset is 43 deg = 10.75 pitches (fractional part 0.75), but the two sensors were **physically swapped** when that wheel was installed, so A leads B forward and the measured A-rise-to-B-rise fraction is the complement, 0.25. **Bench acceptance, first spin:** phase 0.25 +- 0.05, duties 0.50 +- 0.05 each; drift of phase toward 0.00 or 0.50 is the ALIGNED-EDGES failure this catches (at either limit the channels leave quadrature and the `AfirstUp`/`BfirstUp` handshake loses cycles silently - the fw v8 observability lesson). **Gates:** phase is DIRECTION-GATED to `encPeriodDir == +1`, because a mixed-direction average of phi is meaningless (reverse reads `1 - phi`, so averaging 0.25 with 0.75 lands on exactly the fault signature); every sample is PLAUSIBILITY-GATED at `dt < period_ref`, bounding pollution from a spurious B-edge or an unseen fall injecting phi ~ 0.5. Nothing else is filtered - raw-edge visibility is the point. Samples are valid only once the period estimator is seeded (acceptable: the target is geometric and read at cruise). **ISR budget:** two timestamp latches, one subtract, one shift-EWMA per edge; the ONE divide per sample is confined to the rising/falling branches that produce one (~3.3 kHz worst case at the 5 m/s ceiling), and the common A-rising period path gains NO division beyond the fw v17 fractional-pitch ledger's. **"What NOT to change" exception, same class as fw v15/v17:** this is a TAP - the quadrature decode blocks are untouched, no control path reads any of the three, and the estimator's inputs and outputs are unchanged. All three are also printed in the State-98 `'S'` dump as pitch fractions (value/256.0). **(c) DECODER CONTRACT - the record now carries THREE field classes** and mixing them up produces plausible nonsense: **LEVELS** (`enc_period_ref_us` + the three new ones - read directly, NEVER differenced; a 0 means "no measurement yet", not "0.00 pitch"); **RESET-CLEARED CUMULATIVE COUNTERS** (`enc_multi_pitch_count`, `enc_spurious_drop_count` - negative diff = a mid-run `encoderVelReset()`); **BOOT-MONOTONIC CUMULATIVE COUNTERS** (`enc_edge_count_a/b` - never cleared by anything, the only writes in the firmware are the two ISR increments, so a negative diff is a uint32 wrap or an MCU reset). No clear site may be added for the edge counters: the `'L'`/`'S'` consumers rely on the monotonicity. The three levels ARE cleared by `encoderVelReset()`, together with their latch timestamps, because they are normalised by `encPeriodRefUs`, which that reset also clears. **(d) Ring/drain math re-derived at 106 B:** ring = 106 x 1024 = 108544 B (106 KB, still DMAMEM/RAM2); the ~250 ms stall rationale is stated in RECORDS (1024 samples = 1.024 s at 1 kHz) and is therefore unaffected by the record growing; whole-record chunk floor 512/106 = **4 rec = 424 B/tick** against a 106 B/ms fill = **4.0x catch-up** (down from 5.0x at v6's 92 B (5 x 92 = 460 B) and 6.0x at v5's 76 B - still four times the fill; a full ring drains in ~0.34 s); 32 MB preallocation covers 33554432 / 106000 = 316 s (**~5.3 min**, down from ~6.1). No buffer grew; `LOG_RING_RECORDS`, `LOG_CHUNK_MAX`, `LOG_PREALLOC_BYTES` unchanged; `LOG_REC_SIZE <= 255` still holds with 149 to spare. `logSampleTick()` stays a **pure copy** - all five new fields are plain aligned volatile reads (atomic on the Cortex-M7, no IRQ masking) in the build-independent diagnostics section. `offsetof` static_asserts pin 92/96/100/102/104 and `sizeof == 106`. **Decoder/tooling lockstep required** (`tools/decode_benchlog.py` + the analysis pipeline) before the next log round; the phase/duty columns want a LEVEL plot, not a rate. **Next bench:** flash (the flash now carries v18 + v19 + v20); read the `'S'` dump's `phase=`/`dutyA=`/`dutyB=` line on a forward hand-spin FIRST and confirm 0.25/0.50/0.50 before trusting any velocity number on the new wheel; then keep one pre-Schmitt run as the "before" edge-rate baseline, so the same fields quantify exactly what the 74HC14 fixed. | *(pending first flash; fw v18/v19 are not yet flashed either, so the next flash carries v18 + v19 + v20)* |
| 21 | 2026-08-26 | **HIL (hardware-in-the-loop) mode: new `HIL_SIM` compile flag (default 0) that turns the real Teensy into a device under test against a host-simulated plant.** Under `-DHIL_SIM=1` (which `#error`s unless `USE_ETHERNET=1`, since HIL rides the existing UDP socket on `local_port` 5001) `updateSensors()` sources `V_fc`/`V_batt`/`V_bus`/`V_chg`/`V_rgn`/`I_fc`/`I_batt`/`v_actual` from an injected UDP frame in POST-scaling engineering units instead of the ADCs, and `loop()` streams a 16-byte observation frame back at 1 kHz. **Nothing else changes:** `detectFaults()`, the §2 sequencing rules and guards (`assertFcChargeEnable()`, the hot-plug gate), both controllers and the charger logic all run UNMODIFIED on the injected values — that is the point, this is a fault-INJECTION rig and there is deliberately no HIL-specific fault suppression. **(a) Injection frame (host -> board), 40 B** (35 B as first written; extended in the same pre-flash round — see (k)): sync `0xB5`, seq u8, nine little-endian float32 (`V_fc`, `V_batt`, `V_bus`, `V_chg`, `V_rgn`, `I_fc`, `I_batt`, `v_actual`, `I_charge`), then the raw Ag105 Table 6 status byte, XOR checksum over bytes 1–38. Sync bytes are distinct from the Pi link's (`0xAA` TX / `0xBB` RX) so a HIL frame can never be mistaken for a command packet. NaN/Inf fields are rejected for the same reason `receiveCommands()` sanitizes the Pi floats (a NaN reaching `v_actual` poisons the drive controller's recursion permanently). **(b) Observation frame (board -> host), 16 B:** sync `0xB6`, seq echo, `mainState`, the `switch_state` bitmask, an aux-pin byte (`FC_REG_ENABLE`/`BT_REG_ENABLE`/`MPPT_DISABLE`/`CBAL_DISABLE`), post-clamp `current` float32, the last two 16-bit droop-MDAC command words, `fault_flags` u16, XOR checksum over bytes 1–14. Sent only after the first accepted injection frame, to the address it came from — before that there is no host to send to. **(c) `receiveCommands()` now dispatches on LENGTH:** exactly 22 -> the existing command path, BYTE-IDENTICAL (the Pi bridge parses fixed offsets); exactly 40 -> the HIL parser (acted on only under `HIL_SIM=1`, silently dropped otherwise so a stray HIL packet can never reach the command parser); anything else dropped, exactly as before. **UDP telemetry stays v4 / 58 B and the 22-byte command packet is untouched — no protocol bump.** **(d) Two-stage hold-then-zero on link loss** (`HIL_STALE_MS` 50, `HIL_ZERO_MS` 250): a stale frame HOLDS the last injected values (a missed 1 ms host tick is a scheduling artefact, not a plant event — zeroing on it would inject a full-scale rail collapse into `detectFaults()` and latch a bogus UV fault), while a link dead beyond 250 ms forces safe zeros so a genuinely disconnected host cannot read as a healthy plant frozen at its last good values. Before the FIRST frame ever arrives the real ADCs are still read, so a HIL flash stays readable with the simulator not yet started. **(e) `updateWheelSpeed()` is SKIPPED (not modified) under HIL** — the encoder estimator is not the DUT boundary here and would otherwise fight the injection for `v_actual`; the ISRs may still fire, moving only `encoderPos` and the diagnostic counters, which no control path reads. **(f) Refactors, both behaviour-preserving:** the six-line `switch_state` packing in `sendTelemetry()` is factored into `readSwitchState()` and called from both senders (duplicated logic would rot; the telemetry byte and offset are unchanged); and `setDroopMdac()` now mirrors the two command words into `mdacLastCodeFC`/`mdacLastCodeBT` at the single SPI chokepoint, because the AD5443 has no readback and the commanded droop was otherwise invisible to any observer. **(g) Codec compiled unconditionally:** `hilParseInjectFrame()`, `hilPackOutputFrame()`, `hilChecksum()`, `readSwitchState()`, `readHilAuxState()` are outside `#if HIL_SIM` so the host test suite exercises them in the ordinary production and bench builds; only the wiring is gated. **(h) Observability:** a prominent boot banner (the build's sensors are FICTION — never attach a live power stage) and an `--- HIL ---` block in the State-98 `'S'` dump (link up/stale/dead/no-frame, frames accepted/rejected, last seq, age ms) — the fw v8 lesson that every new mechanism gets a dump line. **(i) Host side:** new `tools/hil_plant_sim.py` (stdlib-only, 1 kHz soft-real-time with drift correction, reports achieved rate and max overrun) carrying the fw v14 calibrated constants (`m_eff` 3.5 kg, `K_F` 0.7538 N/A, `F_c` 2.00 N, `b_eff` 0.534 N·s/m, `V_BUS_NOMINAL` 16.0 V) with scenarios `steady` / `step-load` / `sag` / `comm-loss` / `drive` and a per-tick CSV. Its electrical model is deliberately first-order and two simplifications are named at the site: a single droop bus node (no boost or RT1987 turn-on dynamics) and an FC/BT split proportional to the droop MDAC code ratio (sign- and monotonicity-preserving, not the true analog gain). Full architecture, frame tables, build/run instructions and the HIL test plan are in `docs/HIL_MODE.md`. **Limitations, stated up front:** signal-level injection, not power-HIL; 1 kHz soft real time on a non-realtime host; the encoder estimator is bypassed. **(j) Review-round fixes (2026-08-26, applied before first flash).** **MED-1 — bounded receive drain:** `receiveCommands()` consumed exactly ONE datagram per loop tick, so at the 1 kHz injection rate any long tick built an invisible, unbounded socket backlog — the firmware then ran on ever-staler values (never flagged, since `hilLastFrameMs` was restamped by the *arrival* of an old frame) and, worse, a queued 22-byte Pi command waited one tick per backlogged frame, starving the command path. It now drains up to `UDP_DRAIN_MAX_PER_TICK` (8) datagrams per tick: every 22-byte command dispatched in arrival order (the body split out unchanged into `processPiCommandPacket()` so its early returns no longer abort the drain), all injection frames parsed for truthful counters but only the LAST accepted one committed (a frame is a plant SNAPSHOT, not an event). New `udpDrainedLastTick`/`udpDrainedMaxTick`/`udpDrainCapHits` counters in the `'S'` dump make a backlog visible. Non-HIL behaviour unchanged (the injection frame is still dropped unread). **MED-2 — the stale hold is no longer silently commanded into a real VESC:** entering the hold now calls `haltMotorOutput()` on the ENTRY EDGE (a frozen `v_actual` is still live feedback to a ~454 A/(m/s) loop and a constant error rails it), while the sensor hold itself is kept; and the ZERO stage latches a real fault, `ERR_HIL_STALE` (0x10, APPENDED to `ErrorCode_t` — no existing value renumbered) via `triggerFault()`. Zero-stage rather than stale-stage because a 50–250 ms gap is a recoverable host artefact and a dead link is not; previously the dead-link outcome was non-deterministic, depending on whether `uvBusArmed` happened to be set. The fault BIT is a documented ALIAS of `FAULT_PI_TIMEOUT` (`#define FAULT_HIL_LINK`): all 16 bits of the fixed-width `fault_flags` word are already allocated and it is a telemetry/BLG field, so widening it for a HIL-only condition would be a protocol break; `error_code` names the true cause. **MED-3 (doc) — the fw v17 wrap-guard invariant:** `updateWheelSpeed()`'s TOCTOU staleness clamp is documented as depending on that function being called UNCONDITIONALLY from `loop()`, which HIL violates. The comment at the clamp now records the HIL exception and why it is safe in that build only (the estimator's output is entirely unread once `hilHaveFrame` — `v_actual` is overwritten, `encHaveLastEdge` is consumed by nothing), with a mirror warning at the HIL return site noting that `wheelSpeedResetPending` becomes a set-with-no-clear latch there and that any future "revert to real sensors" fallback must restore the unconditional call (or add a wrap guard) and clear it. **LOW-1 — BLG provenance:** records written under `HIL_SIM=1` set `flags` bit6 (0x40), so a HIL log is no longer byte-indistinguishable from a real bench run; record size and header unchanged (BLG stays v7). *Follow-up: `tools/decode_benchlog.py` does not yet surface the bit.* **LOW-2 — provenance markers:** `'S'` dump and `printSensors()` mark `v_actual` (and the rails block) `(INJECTED)` when `hilHaveFrame`, and the `'S'` dump prints `age: n/a (no frame yet)` instead of the old `0 ms`, which read as the healthiest possible link in the one state where there is no link. **LOW-3 — host lock:** the host address/port is learned on the FIRST accepted frame and re-learned only after the link goes dead; while the link is up, a well-formed frame from a different source is ignored ENTIRELY (not applied, and not allowed to restamp the freshness timestamp) and counted in `hilFramesForeign` (`'S'` dump). Under the old learn-every-frame rule one spoofed or stray frame both injected plant values and redirected the observation stream. Accepted consequence: a simulator restarted on a new port must wait out the 250 ms dead-link window. **LOW-4 — the charger path was NOT simulated; now CLOSED by (k) below.** **(k) Charger extension (2026-08-27, same pre-flash round, `FW_VERSION` stays 21).** The injection frame grows 35 B -> 40 B: `I_charge` float32 at offset 34 (amps, already scaled — the 0.011 A/count conversion is the simulator's job, mirroring the single scaling site in the real read) and the raw Ag105 Table 6 status byte at 38; checksum span 1–38. The 35-byte layout was never flashed, so this is a clean size bump with NO back-compat path — a 35 B datagram no longer matches the length dispatch and is dropped unread, which surfaces as the `'S'` dump's accept count stuck at zero (a loud failure, not a half-decoded frame). NaN/Inf rejection extends to `I_charge`. **`pollAg105()` gains a HIL branch:** once `hilHaveFrame` it never touches the Wire bus — a HIL rig has no Ag105, so every real poll is a NACK/timeout burning loop time in the 50 Hz slot and feeding exactly the drain backlog MED-1 was added to bound. The branch mirrors the real path's semantics line for line: the power edge still starts the settle timer and `chargerHasPower()` is still read from the REAL switch pins (the sequencing under test is the firmware's, not the host's); unpowered re-arms `ag105Configured` and clears `ag105DataValid`/`ag105_status_raw`/`I_charge` (identical to the real path's `!powered` block AND to its subsequent NAK, since an unpowered charger cannot ACK); powered takes the injected status byte and current as a successful 2-byte read (`ag105DataValid` true, `0x00` still treated as the real "Battery Disconnect" status with validity tracked out-of-band); settled sets `ag105Configured`. The dead-link zero stage clears the two new fields alongside the rails (`I_charge` 0, status `0x00` — the same pair the real failed-read path leaves behind). **Now exercised in HIL:** the GENSTAT decode / `ag105IsReady()`, the `detectFaults()` GENSTAT error check (inject GENSTAT `0b101`/`0b110`/`0b111`), `I_charge` in telemetry and the BLG, and `chargingControl()`'s MPPT release gating. **Still NOT simulated:** the I2C transport and the config handshake — `initAg105Charger()`'s read-verify-write never runs under HIL, `ag105Configured` is set by fiat, and neither `ERR_I2C_CHARGER` nor the charger `ERR_INIT_FAIL` is reachable in a HIL build. Before the first frame the real I2C path still runs (matching the ADC fallthrough). `'S'` dump charger block gains an `(INJECTED)` provenance marker. **Simulator:** `tools/hil_plant_sim.py` packs the 40 B frame and gains a status-level Ag105 model — power gating mirroring `chargerHasPower()` plus an input-rail floor, an `AG105_SETTLE_S` bring-up window reporting GENSTAT Bring-Up Charge, then Charging + CC with a first-order ramp toward the configured 2.5 A ceiling, and the MPPT/tracking flags following the active-LOW `MPPT_DISABLE` pin. Status byte values are taken verbatim from `references/Datasheets/Ag105_Table6_I2C_Status_Byte.json`. No SoC model, no CV taper, no perturb-and-observe. Two new CSV columns (`I_charge`, `ag105_status`) and the 1 Hz status line reports both. **LOW-5 (doc) — production HIL boot is order-sensitive:** on a `BENCH_TEST=0` flash the simulator must be streaming BEFORE the board is powered, or the staged bring-up latches `INIT_FAIL` at ~800 ms on real (disconnected) ADCs; added to the boot banner and the doc. **Simulator:** the per-tick CSV logged `seq` AFTER the increment, so every row was off by one against the frame it described and against the board's seq echo — it now logs the value actually sent (blank on a non-transmitting `comm-loss` tick); `apply_scenario()`'s always-True `tx_enabled` in/out parameter was removed in favour of a plain return value (the gate is recomputed statelessly from `t`, never latched). **Next bench:** HIL is a desk activity — flash `-DHIL_SIM=1 -DUSE_ETHERNET=1` on a board with NOTHING attached to the power stage, run the `docs/HIL_MODE.md` test plan, then reflash a normal build before any bench run. | *(pending first flash; the next flash of a NORMAL build carries v18 + v19 + v20 + v21)* |
| 22 | 2026-08-30 | **HIL sequential runs: State-0 injection wait gate + auto-recovery from the HIL dead-link latch.** Every change is inside `#if HIL_SIM`; a non-HIL build is byte-identical to fw v21 (same pins, sequencing, faults, telemetry v4/58 B, 22-byte command packet, BLG record v7/106 B, controller coefficients). **Why:** fw v21 latched State 99 (`FAULT_HIL_LINK`/`ERR_HIL_STALE`) whenever the simulator stopped — which is the normal end of an HIL run — and the latch was permanent, so the 38-run `tools/run_hil_suite.py` plan cost 38 physical power cycles. **(a) State 0 under HIL always runs the STAGED bring-up.** The `BENCH_TEST` dark-boot bypass exists because enabling a boost at boot on a soft bench supply browns out the board-powered Teensy — a statement about a REAL power stage. An HIL build has none, so the bypass only made a `BENCH_TEST=1` HIL flash unable to reach Run without an operator driving `'T'`/`'G'`/`'Q'` at the USB console. HIL builds now take `busBringupStart()`/`busBringupTick(true)` at BOTH `BENCH_TEST` values; non-HIL builds keep fw v21's behaviour verbatim. **(b) Injection wait gate.** Before ARMING the bring-up machine, a HIL build waits — non-blocking, 1 Hz `"State 0: waiting for HIL injection stream..."`, `detectFaults()` live — until `hilHaveFrame && age <= HIL_STALE_MS`. No bring-up phase clock starts before the plant exists, so the documented fw v21 boot-order race (a production HIL flash latching `INIT_FAIL` ~800 ms after power-on if the simulator had not started) is REMOVED, not documented; the setup() banner was re-worded accordingly. The gate guards the START only (skipped once `bringupActive`): a mid-bring-up hiccup belongs to the existing two-stage hold-then-zero. A dark wait cannot latch a spurious UV — `uvBusArmed` needs a matched source pair and the stage is dark. **(c) Auto-recovery.** In `state99Phase == 3` (teardown COMPLETE) a HIL build calls the new `hilWarmReset()` and re-enters State 0 when ALL of: `error_code == ERR_HIL_STALE`; `fault_flags == (FAULT_ERROR\|FAULT_PI_TIMEOUT)` EXACTLY (0x8010 — `FAULT_HIL_LINK` aliases `FAULT_PI_TIMEOUT`, so testing for EQUALITY rather than bit-presence is load-bearing: any additional bit means a real fault also latched and the board must stay latched); the link has been continuously fresh for the new `HIL_RECOVER_DEBOUNCE_MS` (500 ms, deliberately > `HIL_ZERO_MS` 250 ms so a flapping link settles into the latch instead of cycling the board through bring-up); and the SD bench log is fully closed (`!logActive && !logCloseRequested && !logFile.isOpen()` — `triggerFault()` only REQUESTS the close and `logDrainTick()` does its card I/O in phase 3, the same window). **`hilWarmReset()` touches NO pin:** phase 2 already left the stage in the `setup()` configuration except `BT_SEQUENCE_ENABLE`, which the IO CSV says need not be turned off and which bring-up P1 drives HIGH anyway — so recovery opens and closes no switch and only the staged machine sequences the power path (CLAUDE.md §2). It resets: `fault_flags`/`error_code`/`error_source_state`/`state99Phase`; the UV/OV arming + dwell state (`uvBusArmed`, `uvBusUnderActive`, `uvBusDwellMs`, `uvBusLastTickMs`, `ovBusOverActive`, `ovBusOverSamples`, `fcUvArmed`, `fcUvUnderActive`, `fcUvDwellMs`, `fcUvLastTickMs`) while LEAVING the transient counters cumulative; `haltMotorOutput()` (motor + Youla drive state); `resetShareControlState()` plus all four of `shareIsoFC`/`shareIsoBT`/`shareSpCutFC`/`shareSpCutBT` (phase 0 clears only three — `shareIsoBT` survived), `powerBalanceLive`, `pi_power_accum`; `resetControlRateLimiters()`; `changeToRun`/`changeToFin`; `bringupActive`/`bringupPhase` (asserted, not assumed — `busBringupStart()` REFUSES to arm on a leaked flag and would strand State 0); the Pi command state to boot defaults (`v_setpoint` 0, `power_share_setpoint` 0.5, `charge_goal` 0, `mode_cmd` 4 = SAFE, `pi_ever_connected` false, `last_rx_ms` re-stamped) so a stale MODE_HYBRID cannot re-enter Run and the watchdog re-arms exactly as after a boot; the Ag105 session flags (`ag105Configured`/`ag105DataValid`/`ag105HadPower`/`ag105PowerOnMs`/`ag105_status_raw`/`I_charge`); the State-98 bench-tool residue (`driveCycleActive`, `powerShareProfileActive`, `trapProfileActive`, `combinedProfileActive`, `wProfileActive`, `tsweepActive`, `vescWatchActive`, `plotModeActive`, `plotArmTarget`, `pendingInput`, `inputBufIdx`) which `triggerFault()` never cleared; `encoderVelReset()` + `wheelSpeedResetPending`; and the recovery arming itself. The `'S'` dump gains a `recover arm:` / `warm resets:` pair in the `--- HIL ---` section (fw v8 observability lesson). Docs updated in lockstep: `docs/HIL_MODE.md` link-loss section, `docs/HIL_USER_MANUAL.md` §2.4/§3.0/§4.1 and the boot-order text. **Next:** flash `-DHIL_SIM=1 -DUSE_ETHERNET=1`, then run `tools/run_hil_suite.py` end-to-end without touching the board between cases. | *(pending first flash)* |
| 23 | 2026-08-30 | **HIL auto-recovery extended from the dead-link latch to ANY latched fault, gated on a RUN BOUNDARY.** Every change is inside `#if HIL_SIM`; a non-HIL build has no behavioural change vs fw v22 other than the `FW_VERSION` stamp (same pins, sequencing, faults, telemetry v4/58 B, 22-byte command packet, BLG record v7/106 B, controller coefficients). **Why:** a `tools/run_hil_suite.py` pass latched `FAULT_UV_BUS` in one scenario; because fw v22's recovery admitted only `fault_flags == 0x8010` with `error_code == ERR_HIL_STALE`, every subsequent run found the board latched and the suite needed a power cycle. Under `HIL_SIM` the plant is a simulation, so every fault is a simulated-plant event belonging to ONE run. **The change:** the `deadLinkOnly` signature test is REMOVED from the `doState99()` recovery admission, and a **run-boundary** condition takes its place. A run boundary is `HIL_RUN_BOUNDARY_MS` (1000 ms) with NO injection frame, observed while in State 99. **The window is anchored to the last accepted frame (`hilLastFrameMs`), not to the first dead tick**, so the figure means literally 1000 ms of link silence; anchoring to the observation would have added the 250 ms `HIL_ZERO_MS` detection latency plus the teardown, making the effective boundary ~1250 ms+ so that a literal 1 s host gap (the shipped `comm-loss` scenario, test H3) would never recover. A gap of exactly 1.0 s is knife-edge (one loop tick of margin), so host-side gaps are specified well above 1 s. Tracked by `hilDeadSinceMs`/`hilDeadSinceValid` and recorded in the sticky `hilRunBoundarySeen`. Admission is now `state99Phase == 3 && hilRunBoundarySeen && logClosed && hilRecoverArmed && (fresh window >= HIL_RECOVER_DEBOUNCE_MS)`. **Why a boundary and not a wider signature:** `triggerFault()` ORs bits into `fault_flags` even when already latched while `error_code` is first-cause-only, so "a real fault, then the simulator stops" yields `0x8010`-plus-bits with a non-HIL `error_code` — no equality signature can distinguish the cases. **Why it is safe:** the boundary preserves in-run latch semantics (a running scenario keeps streaming, so no boundary can accrue and a mid-scenario fault cannot self-clear — the replay suite's `fault_latched` deviation checks depend on that), and 1000 ms is deliberately >> `HIL_ZERO_MS` (250 ms) so a host-side hiccup (suite kill lag, GC pause) cannot forge a boundary, while a between-scenario gap is multiple seconds. **Bounded:** `hilWarmReset()` clears `hilRunBoundarySeen`/`hilDeadSinceValid`/`hilDeadSinceMs`, so one boundary admits at most ONE recovery attempt; a persistent fault condition simply re-latches on the next run instead of looping the board through bring-up. **Dead-link timing is essentially unchanged from fw v22:** in the pure dead-link case the link is dead from the moment of the latch, so the boundary accrues during the teardown before any frames return and only the existing debounce gates the reset. **Boundary tracking runs in EVERY State-99 phase** (a deliberate deviation from the arming logic's phase-3 scoping), for the same reason as the frame anchoring: the teardown phases are not short against a 1000 ms window. The boundary is only an admission precondition — the reset is still gated on phase 3, the closed log and the debounce, so nothing sequences early. **Observability (review S3/S4):** `hilWarmReset()` prints the outgoing `error_code`/`fault_flags`/`error_source_state` *before* clearing them (with any fault now recoverable, the reset is the last moment the cause exists on the board and it is not on the observation frame), and the State-99 1 Hz report appends the live boundary/arm/phase status, since the `'S'` dump is not reachable from State 99. `hilWarmReset()` still touches NO pin. The `'S'` dump gains a `run boundary:` line (SEEN / accruing-with-elapsed / not-seen) above the existing `recover arm:` pair, so a board that refuses to recover shows whether it is waiting on the debounce or on the boundary. Docs updated in lockstep: `docs/HIL_MODE.md`, `docs/HIL_USER_MANUAL.md`. **Next:** flash `-DHIL_SIM=1 -DUSE_ETHERNET=1`, then run a full `tools/run_hil_suite.py` including a scenario that latches a real fault, and confirm the following runs start clean. | *(pending first flash)* |
| 24 | 2026-08-31 | **Dynamic Ag105 MPPT input-voltage threshold (reg 0x02), plus a firmware UV backoff on the charger path.** WHY: the Ag105's MPPT is a THRESHOLD regulator, not perturb-and-observe (`AG105_Silvertel.pdf` §2.9) — charging commences only once V_CHG-IN exceeds a threshold set by an MPPTS resistor or I2C reg 0x02, factory default **18 V**. This board feeds V_CHG-IN from the ~15.95 V bus, so a released Ag105 at the default threshold never charges: `ag105IsReady()` stays false, `chargingControl()` re-asserts `MPPT_DISABLE`, and the firmware hunts (~40 ms period, measured on the HIL bench 2026-08-31, `mppt-tracking` scenario). NEW `ag105ManageMpptThreshold()`, driven from `pollAg105()`'s successful-ACK path and only while `ag105Configured`: it accumulates a windowed **minimum** of `V_chg` (400 ms, sampled only while `fcChargePathIsPowering()` — FC_CHARGE high AND REGEN low, so the regen node cannot contaminate the sample — after a 150 ms path-open skip), targets `(min − 3.0 V)` quantized DOWN and clamped **in counts** to `[AG105_MPPT_N_FLOOR 15, AG105_MPPT_N_CEIL 28]` = 12.320–13.464 V, then reads reg 0x02 and reg 0x07 one transaction per 50 Hz tick and applies **monotone non-increasing** write rules: (a) take over from external-resistor mode (≥251); (b) ratchet DOWN when the register exceeds target by more than `AG105_MPPT_DEADBAND_CNT` 3 — gated on sustained `AG105_MPPT_GUARD_V` 0.5 V proximity for `AG105_MPPT_GUARD_DWELL_MS` 1000 ms, ≤`AG105_MPPT_RATCHETS_MAX` 2 per powered session, ≥`AG105_MPPT_MIN_WRITE_MS` 30 s apart; (c) raise to the floor if the register is below it — the ONLY automatic rule that raises; (d) otherwise nothing. No ping-pong is possible: (b) writes a value already inside the clamp band, so (c) can never fire on it, and after the write (b)'s own strict `> target + deadband` is false. Two `static_assert`s pin the band — the ceiling must stay at/below `V_BUS_CHARGED_THRESH` (the no-hunt invariant) and the floor clear of `LIMIT_V_BUS_MIN + 0.25`; two more pin the backoff hysteresis. Writes go through the existing `ag105WriteConfigRegVerified()` (read-first / skip-if-equal / read-back), are capped at `AG105_MPPT_WRITES_PER_BOOT` 8 since boot (a boot-scoped budget that survives charger power cycling and `hilWarmReset()`), and are confirmed by an **independent readback on the NEXT poll tick** — no blocking wait is added anywhere. **0xFF DISCRIMINATOR:** §2.11.5 makes the module report 0xFF on every measurement while it is under its own threshold, so a reg-0x02 readback of 0xFF is ambiguous between genuine resistor mode and that sentinel; reg 0x07 (Measured Input Voltage, 0.141 V/count) cross-checked against the board's own `CHG_VOLTAGE` ADC within `AG105_MPPT_VIN_XCHECK_V` 1.0 V `TODO(calibrate)` resolves it. An undecidable evaluation DEFERS (≤3, 1 s apart) and then **fails safe**: `MPPT_DISABLE` is held asserted for the session, no fault is latched, a counter and a Serial warning record it, and the state re-arms on the next charger power cycle. **MPPT RELEASE HOLDOFF** `MPPT_RELEASE_HOLDOFF_MS` 1000 ms in `chargingControl()` after a readiness-driven re-assert, bounding any residual hunt to ≤1 Hz under either reading of the MPPTD-disabled charge semantics (§2.9 says disable "does not terminate charge"; `TODO(verify)` on the bench). **FIRMWARE UV BACKOFF:** `V_bus` under `AG105_CHG_BACKOFF_V` 12.8 V for `AG105_CHG_BACKOFF_DWELL_MS` 15 ms closes `FC_CHARGE_ENABLE` through `assertFcChargeEnable(false)` and restores BT to the bus (identical ordering to the `charge_goal == 0` branch — no mutual-exclusion or back-feed rule is touched); it re-opens only above `AG105_CHG_RESUME_V` 13.6 V, so a bus sitting at the trip cannot chatter the RT1987. **COMPANION FIX:** a 0xFF on the reg-0x06 measured-current read is the same §2.11.5 sentinel (255 × 0.011 = 2.805 A, above the 2.5 A this firmware configures — not a reachable reading); `I_charge` now goes to 0.0 with a new `ag105MeasUnavailable` diagnostic flag instead of shipping a plausible 2.8 A into telemetry and the BLG. GENSTAT / `ag105IsReady()` are untouched. **State-98 `'N'`** (single line, `T`/`Y`/`W`/`K` convention): empty = status block, `N <volts>` = force a write (clamped 11–33 V, quantized down — the only path that may raise above the floor; refused when the charger is unpowered, during the staged bring-up, or with a profile running; counts against the boot budget), `N R` = restore 0xFF. The `'S'` dump gains the same MPPT block. Two stale "perturb-and-observe" comments in `chargingControl()` corrected to the threshold mechanism. **PROTOCOL:** the HIL observation frame grows **16 → 17 bytes** (`mppt_thresh_count` appended at offset 15; XOR span 1..15) — the ONLY wire change. Telemetry v4/58 B, the 22-byte command packet, the 40-byte injection frame and BLG record v7/106 B are byte-identical; `tools/hil_plant_sim.py` must be updated in lockstep (SEPARATE tooling round). Under `HIL_SIM` with a live link the manager is never called (it is an I2C state machine and that path must touch `Wire` zero times); the believed count is mirrored by fiat from the injected `V_chg`, exactly as `ag105Configured` already is. **Next:** update the simulator to the 17-byte frame, then bench: open FC_CHARGE with `'5'`, read `'N'`, confirm reg 0x02 leaves 0xFF for a value near 12.3–13.5 V, and confirm the release/re-assert hunt is gone. | *(pending first flash)* |
| 25 | 2026-09-01 | **Share-cut handoff guards: a load-aware r-based cut, and survivor-turn-on blanking on both cut paths.** WHY: campaign `hil_report_20260901_080905` latched `ERR_OC_BT` on a share cut that opened the only CONDUCTING source 5 ms after the survivor's bus switch closed. Every FC-charge window holds `BT_BUS_ENABLE` LOW (`assertFcChargeEnable()`'s mutual-exclusion guard), so the measured share pins at 1.0 and the loop winds `r` onto `DROOP_R_MIN` exactly — zero margin. At window close `chargingControl()` restores `BT_BUS`, and on that SAME tick `applyShareRatio()` saw both switches HIGH with `r` still below the band and cut FC at `I_fc` = 0.6371 A while BT was inside the RT1987's ~8 ms `t_D_ON` delivering 0 A. `V_bus` fell 14.56 → 12.40 V in 3 ms on bus capacitance; BT's ideal diode picked up reactively, the share slew overshot `I_batt` to 4.64 A, and `OC_BT` latched (correctly — a real overcurrent). TWO holes: **(a)** the r-based cutoff had NO load guard — the setpoint-latch path got `SHARE_CUT_MAX_HANDOFF_A` (0.5 A) in fw v6 and its twin never did, and the `shareCutDeferred*` suppression only covers OUT-OF-BAND SETPOINTS, whereas here the setpoint (0.85) was in band. Both r-based branches now gate on the doomed channel's measured current; a blocked cut falls through to the band-edge droop clip exactly like the pre-existing last-source guard (no cut, no latch, no new flag, droop authority live, retried every tick). **(b)** NEITHER path knew whether the survivor was conducting yet. All 26 writes to `FC_BUS_ENABLE`/`BT_BUS_ENABLE` now route through a `writeBusSwitch()` chokepoint that timestamps LOW→HIGH transitions; a cut is refused while the SURVIVOR's rising edge is younger than `SHARE_CUT_SURVIVOR_BLANK_MS` = 30 ms (RT1987 `t_D_ON` ~8 ms, DS §17.4/§17.6 Table 1, plus the 100 nF CSS soft-start ramp `tON = (VIN/35)(CSS_nF/0.0023 − 100)` µs = 19.8 ms at 16 V; conservative — too short leaves a residual dark-survivor handoff, too long only extends a slew-limited band-edge sit; `TODO(calibrate)` on the bench). A switch already HIGH with no recorded edge is NOT blanked (unknown edge reads as old); ages use wrap-safe unsigned arithmetic. The helper only timestamps — HIGH→LOW writes forward unchanged, so `safeAllSwitches()`/`doState99()` teardown ordering is byte-for-byte identical. **Diagnostics:** `shareCutRefusedLoad`/`shareCutRefusedBlank` counters and both rise ages in the State-98 `'S'` dump; no control path reads them. Cuts at light load past the blanking window are unchanged in effect; re-entry logic, hysteresis, deferral machinery, `DROOP_R_MIN`/`MAX` and both controllers untouched. **PROTOCOL:** the HIL observation frame grows **17 → 18 bytes** (`error_code` appended at offset 16, XOR span 1..16) — `FAULT_PI_TIMEOUT` and `FAULT_HIL_LINK` share fault bit `0x0010` and were wire-indistinguishable; `triggerFault()` latches first-cause in `error_code` while only ORing bits into `fault_flags`. v4/58-byte telemetry, the 22-byte command packet, the 40-byte injection frame and BLG v7/106 B are byte-identical. `tools/hil_plant_sim.py` must learn the 18-byte length in lockstep (separate tooling round). Also corrected: the fw v24 changelog said the charger UV backoff dwell was 60 ms; `AG105_CHG_BACKOFF_DWELL_MS` is 15 ms. | (pending flash) |
