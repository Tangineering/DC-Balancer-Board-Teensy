# Firmware version ledger

`FW_VERSION` is a monotonic u16 defined near the top of
`teensy_controller/teensy_controller.ino`. **Bump it on every flash-worthy
behavioral change** — control law, pin/sequencing, scaling, logging format —
and add a row here in the same commit. Comment/doc-only changes do not bump.

The version is:
- stamped into every `.BLG` bench-log header (format v2 and later, u16 at offset 18),
  so every logged dataset is attributable to the firmware that produced it
  (`decode_benchlog` prints it; it lands in each run's `decode_report.txt`);
- printed at boot (`[BOOT] DC balancer firmware vN`);
- printed by the State-98 `S` status dump (`FW_VERSION: N`).

Version **0 / "pre-versioning"** is reserved for all firmware before this
ledger existed. Format-v1 `.BLG` files (no fw_version field) decode as
`fw_version=pre-versioning`; that covers `PS0001`–`PS0002` (pre-averaging)
and `PS0003`–`TP0005` (`analogReadAveraging(8)`).

| FW | Date | Flashed changes (vs previous) | Logs produced |
|----|------|-------------------------------|---------------|
| 0 | — | Everything before versioning. Notable sub-states: PS0001/PS0002 ran the pre-averaging build (Teensyduino default averaging 4); PS0003/TP0004/TP0005 ran the `analogReadAveraging(8)` build; the 2026-08-11 share-setpoint sweep TP0007–TP0013 also decodes as fw 0 (format-v1 headers). | PS0001–TP0005, TP0007–TP0013 |
| 1 | 2026-08-11 | `analogReadAveraging(16)` (was 8). Share-integrator minimum-load hold at `SHARE_I_TOT_MIN_A` = 75 mA. Full-span share command semantics: setpoints/ratios valid over [0,1]; `applyShareRatio()` actuation layer — droop-band clip [0.15, 0.85] moved inside it, out-of-band ratios cut the starved channel off the bus via its RT1987 bus switch (never the boost enable) with 0.01 re-entry hysteresis and a charged-bus re-entry guard; `O`/`P` commands unclamped to [0,1]; Youla anti-windup authority span widened to [0,1]. BLG header format v2 (fw_version stamp). State-98 combined profiles: `Y` (velocity + share) and `W` (commanded current + share) sweep both axes from one shared 16-region table via `advanceComboRegion()`, logged to `YPnnnn`/`WPnnnn`; **the VESC watch moved from `W` to `U`**; both profiles warn when the committed share band reaches the cutoff region and re-close a latched channel cutoff on natural completion; `pollVescWatch()` suppressed during the staged bring-up. | *(never flashed — superseded by v2 before first flash)* |
| 2 | 2026-08-11 | Share-loop limit-cycle mitigation (TP0010/TP0013 sweep + capture 12; whitepaper `docs/share_sweep_whitepaper`): setpoint governor clips the effective in-band share setpoint so the commanded minority-channel current stays ≥ `SHARE_MINORITY_I_MIN_A` = 0.20 A (collapses to 0.5 below 2×; out-of-band setpoints incl. 0/1 bypass — the cutoff path owns them); droop-ratio slew limit `DROOP_RATIO_SLEW_PER_TICK` = 0.02/tick in `applyShareRatio()` (full band in 35 ms, no rail-to-rail MDAC slams); `resetShareControlState()` at every profile start (`R`/`D`/`T`/`Y`/`W`) — runs no longer inherit the prior run's controller state. ΔV₀ CAL-1 recorded in the model (+0.05 V, bench supplies); shipped Youla coefficients unchanged. | *(never flashed — superseded by v3 before first flash)* |
| 3 | 2026-08-11 | State-98 `T` sweep extension: `T <Imax> <hold> <rate> [t,r1,...,rn]` runs one trapezoid per closed-loop share setpoint `r_i` (max 16), each to its own `TPnnnn.BLG`, with the next run gated on the logger being fully idle (`!logActive && !logCloseRequested` — an early start would silently lose that run's dataset) and separated by a `t`-second motor cool-off dwell. Share setpoint applied via `setPowerShareSetpointLive()` BEFORE each run opens its log; setpoint restored to 0.5 / `powerBalanceLive` cleared on completion and on every cancel path (`T`-stop, `X`, `Q`, a new `T` line, or any other profile start). Sweep list refused under plot mode (`L`); the grammar's 4th field ends the old trailing-junk tolerance (any unparsable tail now rejects the whole line). `inputBuf` 32 → 96 B, and `[`/`,`/`]` accepted at the trapezoid prompt only. Automates the hand-run TP0007–TP0013 sweep. | TP0014–TP0038, WP0039–WP0040 |
| 4 | 2026-08-12 | Three changes from the fw v3 validation sweep (TP0014–TP0038, WP0039–WP0040; whitepaper `docs/share_sweep_whitepaper`). (a) `SHARE_MINORITY_I_MIN_A` 0.20 → **0.30 A**: the sweep bracketed the light-load conduction floor — 0.245 A commanded minority still collapsed the bus to 8.2 V (TP0016, sp = 0.15), 0.29 A is clean (TP0017, sp = 0.18) — so the shipped 0.20 A sat below the whole bracket and governed nothing at the setpoints that failed; the governor's collapse-to-0.5 threshold rises to 0.60 A automatically. (b) **Setpoint-latched channel cutoff** (`updateShareSetpointCutoff()`, "one owner per setpoint"): the governor bypassed out-of-band *setpoints* while the cutoff fired on the controller *output* `r`, leaving two gaps — sp = 0.87 settles at r ≈ 0.84 (in band), engaging neither mitigation and running a 19.5 Hz limit cycle (TP0037), and sp = 0.12 fired the cutoff but the standing share error wound `r` back over the 0.01 re-entry hysteresis, hunting at 20 Hz with ~190 `FC_BUS_ENABLE` cycles/run (TP0015). A setpoint outside `[DROOP_R_MIN, DROOP_R_MAX]` now latches the starved channel off the bus (same last-source guard; guard-blocked → no latch, normal governed control), **freezes** the share controller (no governor, no step, no MDAC write), and disables ratio-hysteresis re-entry for that channel; release only on the setpoint returning in-band, gated on `V_bus ≥ V_BUS_CHARGED_THRESH`, then `resetShareControlState()`. State-98 `'1'`/`'2'`, `assertFcChargeEnable()`, `safeAllSwitches()` and the completion restore all clear the latch (operator/state ownership). The r-based cutoff remains as the in-band backstop. (c) **`FAULT_UV_BUS` reworked**: armed by the bus (`V_bus ≥ V_BUS_CHARGED_THRESH` with a source switch closed; disarmed when both source switches are LOW) instead of gated on State 2, persistence-filtered like `FAULT_OV_BUS` (`UV_BUS_PERSIST_MS` = 10, 3 samples, 5 ms gap guard), and **armed under `BENCH_TEST`** — WP0039 sagged to 7.6 V through 89 dropout cycles with `fault_flags = 0` and ended in an MCU brownout. `LIMIT_V_BUS_MIN` stays 12.0 V. `'S'` gains UV-transient/armed and setpoint-latch lines. No telemetry layout change (v4/58 bytes). | TP0041–TP0068, WP0069–WP0073 |
| 5 | 2026-08-12 | Three changes from the fw v4 validation sweep (TP0041–TP0068, WP0069–WP0073). (a) **Governor open-loop low-current fallback**: the fw v4 governor collapsed the effective setpoint to 0.5 below `2·SHARE_MINORITY_I_MIN_A`, which at 0.075–0.60 A of filtered total commands 0.038–0.30 A per channel — at or below the 0.30 A conduction floor it exists to enforce, against ~20 mV of droop authority — and that fallback *ignited* the source-commutation relay limit cycle (six runs collapsed the bus to 7–9 V and latched `ERR_UV_BUS`). `powerBalance()` now has two modes, hysteretic on the filtered total (enter closed loop above 0.60 A, fall back below 0.60 − `SHARE_GOV_OL_HYST_A` = 0.55 A): **closed-loop** runs the Youla controller + the now always-relaxing governor clip (the collapse-to-0.5 branch is deleted as unreachable); **open-loop** never steps the controller — it feeds the raw setpoint forward through the same `DROOP_RATIO_SLEW_PER_TICK` limiter until the closed loop has run once (the sweep showed the commanded hold ratio tracks the setpoint within ~0.01–0.02), and **holds** the last applied ratio thereafter. The open→closed transition reseeds the controller from `droopSlew_prev` via the new `resetShareControllerCore()`, which deliberately does not touch `share_govTotAFilt` (the mode decision variable); `resetShareControlState()` clears both new mode flags, so every profile start (`R`/`D`/`T`/`Y`/`W`) and the setpoint-latch release begin open-loop. (b) **`FAULT_UV_BUS` leaky-dwell filter**: the fw v4 wall-clock window (10 ms + 3 samples + 5 ms gap guard) is evaded by duty cycle — the relay cycle's 9 ms-under / 51 ms-over period reset it every cycle, so runs endured 1.0–1.3 s and up to 24 excursions to 7 V before latching. `UV_BUS_PERSIST_*` is replaced by `UV_BUS_DWELL_LATCH_MS` = 20, `UV_BUS_DWELL_LEAK` = 0.05, `UV_BUS_DWELL_DT_CAP_MS` = 5: dwell accumulates `+dt` under the limit and leaks `−0.05·dt` above it, latching at 20 ms net (TP0053 nets +6.45 ms/cycle → latch ≈180 ms; WP0069's ~19 ms of sparse dips over 208 ms nets ~9.6 ms → correctly no latch). Arming/disarming unchanged; `'S'` reports the live dwell. (c) **BLG format v3** (`hdr[4]` = 3, 68 B/record): record gains `V_fc`, `V_batt`, `V_chg`, `V_rgn` after `I_cmd` — WP0072/WP0073 ended in MCU brownouts with the *bus* in regulation, and the board-powering `V_batt` rail was unlogged. `tools/decode_benchlog.py` updated in lockstep. A source-rail UV *fault* is deliberately **deferred** until logged `V_batt` from the next brownout sets its threshold. `'S'` also reports the share-loop mode; log `flags` gains bit2 (`shareClosedLoopMode`) and bit3 (`shareClosedLoopRun`). **Scope of the dwell latch:** the 20 ms threshold is validated against BUS-referenced collapses only (TP0053-class relay cycles, WP0069-class transients); the WP0072/WP0073 source-rail brownouts are invisible to any bus-referenced fault by construction — that is exactly the deferred source-rail item. Review-round hardening (same version): HOLD no longer strands an outstanding `shareIso*` cutoff nor swallows a commanded setpoint change; orphaned `shareIso*` claims self-heal; `setPowerShareSetpointLive()` resets the share-control state; the controller reset seeds the integrator (not the dead held output); `'R'` and the `'T'` sweep restore a channel cutoff on natural completion like `'Y'`/`'W'`; `FAULT_UV_BUS` arming requires a matched bus-switch + boost pair. | *(pending first flash)* |
| 6 | 2026-08-12 | Five changes from the fw v5 validation sweep (TP0074–TP0094 clean, WP0095–WP0101). (a) **Load-aware handoff guard** on the setpoint-latched cutoff entry (`SHARE_CUT_MAX_HANDOFF_A` = 0.5 A): the cut hands the doomed channel's entire current to the survivor in one tick — WP0097/WP0101 fired it with 1.3–1.5 A on BT, pushing solo FC past this bench's ~2.1 A source knee and collapsing the bus in ~40 ms (`ERR_UV_BUS`), while the same cut at ~0 A is validated clean (TP0074/85/86/87 run-start latches). Entry now also requires the doomed channel's |I| ≤ 0.5 A; blocked → no latch, and the tick raises a per-tick **deferral** (`shareCutDeferredFC/BT`) which clips the closed-loop *reference* from the out-of-band setpoint onto the doomed side's band edge (so the loop actively migrates load toward the survivor) and **suppresses** `applyShareRatio()`'s r-based cutoff on that side — that cutoff has no current guard and would otherwise execute the refused handoff a few ticks later under the wrong ownership flag (`shareIso*`, invisible to the `!shareSpCut*`-gated re-closers → re-close/re-cut cycling). The cut fires on a later tick once the migration has pulled the current under the threshold. *Residual:* at high total current the migration may never get there — the band edge is the maximum droop authority available — and the loop sits at the edge running the self-limiting rail-saturated dropout cycle instead — accepted until the floor-law rework. (b) **Source-rail UV fault on `V_fc`** (`UV_FC_DWELL_LATCH_MS` = 20 ms, reusing `UV_BUS_DWELL_LEAK`/`UV_BUS_DWELL_DT_CAP_MS` as filter-shape parameters): the fault fw v5 deferred, taken now that three fw v5 runs ended in an MCU stop with `V_fc` under 5 V while the **bus still read 15.7 V** — invisible to any bus-referenced fault, and leading the bus event by ~7 ms. Armed under `BENCH_TEST` and in every state; the old State-2-gated single-sample check is removed. Boot-lock is prevented by **arming**, not a state gate: `FC_REG_ENABLE` + `FC_BUS_ENABLE` both HIGH (matched pair) **and** `V_fc` observed ≥ `V_FC_ARM_THRESH` = 7.0 V while so routed — deliberately 1.0 V *above* the 6.0 V trip limit (mirroring `V_BUS_CHARGED_THRESH` vs `LIMIT_V_BUS_MIN`) so a fuel-cell ramp cannot arm the filter inside its own trip band — so a bench with no fuel cell never arms. Evaluated **before** the bus UV block so a same-tick double-cross latches the *cause* (`ERR_UV_FC`) rather than the consequence (`ERR_UV_BUS`). `V_batt` counterpart still deferred (threshold blocked on the LM1084-input capture). `'S'` gains a `UV_FC` line. (c) **Governor handover continuity** (`share_spEffPrev`): open loop feeds the raw setpoint forward while the first closed-loop tick handed the controller the floor-clipped value — a reference step of up to 0.35 share at the 0.60 A crossing (raw 0.15 → 0.50). The clipped target is now approached through a `DROOP_RATIO_SLEW_PER_TICK`-limited **reference**, seeded at every controller reset/seed from the same value the core is seeded with (`droopSlew_prev` at the OL→CL transition, 0.5 on a full reset), band-clipped. Converged behaviour is bit-identical to fw v5 — transients only; the Youla/PI internals are untouched. (d) **Combined-profile table rows 5/6/7** (`W`/`Y`): all five fw v5 `W` failures were **R6-entry** events, R6 being the only region combining peak load (v = 1.0) with an extreme share (s = 1.0). The excursion is kept but **de-rated** — R5 ramps v down to 0.3, R6 runs s = 1.0 at 0.3·Imax (≈0.6 A total, under the source knee), R7 takes the s step-down at that low load then ramps v back to 1.0 so R8 keeps its 1.0 → 0.5 down-step. Region count, durations and the 40 s total unchanged; rows 0–4 and 8–15 byte-identical. (e) **BLG header format v4** (`hdr[4]` = 4; **record format unchanged**, 68 B): header gains the committed per-run profile parameters — `hdr[7]` param-valid flags (bit0 amp, bit1 b), float `profileAmp` at 20–23 (`T`/`W` Imax, `Y` Vmax), float `profileB` at 24–27 (`W`/`Y` clip bound b); bytes 28–31 reserved zero (byte 19 is the high byte of the 2-byte `fwVersion` at 18–19, not a reserved byte). Derived inside `logOpenForProfile()` from `typeMask` plus the already-committed profile globals, so no call site can omit them. `tools/decode_benchlog.py` updated in lockstep. No UDP telemetry change (v4/58 bytes). | *(pending first flash)* |
| 7 | 2026-08-13 | Velocity-chain calibration landed. (a) **Encoder geometry measured** (operator, bench): hand-turning the flywheel exactly one revolution accumulates 120 `encoderPos` counts, so at the ISR-verified x2 quadrature decode the disc carries 60 slots — `ENCODER_SLOTS_PER_REV` 512.0f → **60.0f** (the 512 was unsourced and not credible behind a 3.18 mm beam gap). ⚠️ **The 60 is SUPERSEDED by fw v8's 120** — the "120" measured here was 120 *slots*, not 120 counts, so the ×2 decode was applied backwards; the fw v8 row has the full reconstruction. The ×19.70 discontinuity stated at the end of this row is the fw 6→7 figure and remains correct for fw 7 traces. Flywheel radius measured directly: `FLYWHEEL_RADIUS_M` 0.033f → **0.0762f** (3.00 in), superseding the nominal tire-OD/2 estimate and the reasoning behind it — the encoder disc is mounted on the flywheel, so the flywheel radius is what converts its angular rate to m/s. `RPM_TO_MPS` follows both automatically. (b) **`VELOCITY_CHAIN_CALIBRATED` default 0 → 1**: State-98's velocity entry points (`V` manual velocity, `D` drive cycle) and the `Y` profile's velocity axis are open. The interlock machinery (compile-time `#ifndef` override + runtime `velocityChainCalibratedFlag`) is retained unchanged — the over-drive hazard it guards (under-reading `v_actual` → PI adds current chasing a speed already passed; `commandMotorCurrent()` bounds amps, not speed) belongs to the mechanics, so a new disc or flywheel means rebuilding with `-DVELOCITY_CHAIN_CALIBRATED=0` until re-measured. `T`/`W` still deliberately bypass the gate (they never close the velocity loop). (c) **`L` Serial-Plotter stream: six fields → eight** — `sp`/`act` renamed to `share_sp`/`share_act`, and `v_sp` (`v_setpoint`) + `v_act` (`v_actual`) appended; order `share_sp,share_act,gFC,gBT,ifc,ibt,v_sp,v_act`. Fixed-field-count rule unchanged; the `plotTick()` backpressure guard rises `Serial.availableForWrite() < 80` → `< 110` for the longer line. Help text and the `[PLOT]` banner list the new fields. (d) **`MOTOR_I_CMD_MAX` 5.0 → 10.0 A** (operator decision): the 5 A figure came from the ~67–87 W source power budget, which is a *bus*-current budget, but this constant clamps what `setCurrent()` sends the VESC — three-*phase* motor current, related to bus current only through the duty ratio (`I_bus ≈ D·I_mot/η_esc`). Bounding bus current is therefore not this constant's job — it belongs in the VESC's own Battery Current Max setting (≈ 4.2 A). ⚠️ **That is a PRECONDITION, not a statement of fact:** `docs/VESC_MOTOR_INTEGRATION.md` §4 still records Battery Current Max and Battery Current Regen Max as *not set / not tracked*. Configure them in VESC Tool (≈ 4.2 A / ≈ 1.5 A) and tick §4 **before any velocity-path run at the 10 A ceiling** — until then there is no bus-current bound anywhere: not in this constant (phase current, duty-dependent), not in the VESC (unset), and not in this firmware on a `BENCH_TEST` flash, where the OC faults are compiled out. The old derivation is retained at the constant as the previous bench value; the 15.0 A vehicle `TODO(calibrate)` stands. This is also the State-98 manual-current ceiling (`setManualMotorCurrent()` clamps to ±`MOTOR_I_CMD_MAX`, so `A` now accepts ±10 A). **Amended 2026-08-15 (pre-flash): raised again 10.0 → 12.0 A** with the Castle 1406 1900KV motor fitted, for the Youla-H drive-controller bring-up; same phase-current rationale, §4 precondition unchanged and still open. Deliberately NOT following it: `TRAP_I_ABS_MAX` (25 A ESC rating — what `T`/`W` bound against), `W_IMAX_DEFAULT` (5 A, an independently conservative profile default), `MANUAL_MOTOR_V_MAX` (5.0 m/s, a velocity). The motor PI's anti-windup `integMax` derives from `MOTOR_I_CMD_MAX` and rescales automatically. **No BLG record or header change** (the 68 B record already carried `v_sp`/`v_act`, and `flags` bit1 already reported `velocityChainCalibrated()`) and **no UDP telemetry change** (v4 / 58 bytes). **Scale discontinuity (review S7):** `v_actual` (and the BLG `v_act` column) changed by **×19.70** for identical motion — (1024/120) counts × (0.0762/0.033) m — and BLG `flags` bit1 is now set by default, so fw ≤ 6 and fw 7 velocity traces are **not comparable**; the header `fwVersion` field disambiguates. **Next bench (advisory):** run the first velocity tests scope-armed; use `V` with small setpoints before `D` (the drive cycle's regen phase steps a −3.5 m/s error, which at the new ceiling commands −10 A onto the regen path); consider a temporarily reduced `MOTOR_I_CMD_MAX` for the first run. | *(pending first flash)* |
| 8 | 2026-08-16 | **Encoder pin move (the root cause), observability, and a slot-count correction. No control-path, telemetry, or log-format change; the pin map and one scale constant change.** Raised from a bench report that `v_actual` stayed at 0.000 in the `L` stream while the encoder was visibly producing a signal. **(0) PIN MAP — ROOT CAUSE: `ENC_A` 2 → 14, `ENC_B` 8 → 15, `ENC_ENABLE` (7) DELETED.** Bodge work had relocated both encoder channels and hardwired the optical sensors to power, and the firmware had not been reconciled — it was attaching `CHANGE` interrupts to two pins the encoder was no longer wired to, so no ISR ever fired and `encoderPos` never moved. Pin 7 is now **undriven** (no `pinMode`, no `digitalWrite` anywhere — there is no net to drive) and pins 2/8 are free; the `initControlPeripherals()` enable write is gone, so the encoder is live from power-on rather than from State 0. Teensy 4.1 pins 14/15 are A0/A1 and Serial3 TX/RX — neither alternate function is used by this firmware and both are interrupt-capable, so the ISR path is otherwise unchanged. `references/Scale Car Teensy IO - IO.csv` was updated by the operator in lockstep (rows moved to 14/15, pin 7 marked "No longer in use"), so the CSV remains authoritative and the firmware follows it row-for-row; bodge recorded in `CLAUDE.md`. **No build before fw v8 can produce velocity on this board.** The velocity chain had exactly ONE observable (`v_actual`) and the x2 quadrature decoder in `doEncoderA()`/`doEncoderB()` only counts when BOTH channels transition in the correct ORDER, so a dead channel, a phototransistor swing that never crosses the Teensy's V_IL/V_IH, and two beams that are not 90° apart all produce an identical, silent `encoderPos == 0` — none of them distinguishable from a stationary flywheel. Added: `encEdgeCountA`/`encEdgeCountB` (volatile u32, incremented at the top of each ISR, read by nothing but the dumps), an `--- Encoder ---` block in the State-98 `'S'` dump (live `digitalRead(ENC_A/ENC_B)` **printed with their pin numbers** — this dump is the field tool for exactly the stale-pin-map fault above and must not report the wrong pins silently — `encoderPos`, both edge counts, `v_actual`, and the two scale constants in force), and the same `encoderPos`/edges/pin-number line in the IDLE `printSensors()` dump so a hand-turn check needs no test-mode entry. **`ENCODER_SLOTS_PER_REV` 60.0f → 120.0f** (`ENCODER_COUNTS_PER_REV` 120 → 240) — the one scale change in this version, and the observability work above is what uncovered it. The disc was counted directly and physically carries **120 slots**. fw v7's 60 was a transcription error, not a competing measurement: the 2026-08-13 bench figure of "120" was recorded as 120 `encoderPos` *counts* per hand-turned revolution and divided by the ×2 decode, when it was 120 *slots* and the decode multiplies. The tell is the observability gap itself — no build through fw v7 printed `encoderPos` anywhere, so no count could have been read. **Scale discontinuity:** `v_actual` and the BLG `v_act` column **halve** for identical motion versus fw v7, so fw 7 and fw 8 velocity traces are not comparable (header `fwVersion` disambiguates); versus fw ≤ 6 the chain is (1024/240)×(0.0762/0.033) = **×9.85**. `VELOCITY_CHAIN_CALIBRATED` stays 1 — a direct slot count is a stronger source than the figure it replaces, and needs no working decoder to obtain. **Drive-direction consequence:** fw v7 over-read `v_actual` by 2×, shrinking the velocity error so the PI under-drove; correcting it restores the true error, so fw v8 commands up to **2× the current fw v7 would have at the same `v_setpoint`** — toward truth, but the more aggressive direction on a bench where the fw v7 §4 precondition (VESC Battery Current Max / Regen Max) is still open and `BENCH_TEST` compiles the OC faults out. **Disc coupling RESOLVED 2026-08-16 (operator): surface/roller** — the encoder is coupled to the flywheel and the flywheel's own radius IS the rolling radius, so `FLYWHEEL_RADIUS_M = 0.0762` is correct as shipped and the wheel-angular-speed alternative (which would have forced the tire radius and made `v_actual` over-read 2.31×) is retired, closing the fw v7 S3 residual. No constant changes — a determination, not a measurement. With this the velocity SCALE chain is complete: counts/rev, decode factor, radius and coupling all settled, decoder hardware-confirmed. `v_actual` is flywheel **surface speed**, and `v_setpoint`, the `'V'`/`'D'`/`'Y'` commands and the BLG `v_sp`/`v_act` columns share that definition; no separate vehicle-speed scale exists in the firmware. Everything else — sequencing, faults, PI gains, droop, charger — is byte-identical to fw v7. Tests: the decoder had no coverage at all (every prior test wrote `encoderPos` by hand) — 11 new checks drive it from raw pin levels: forward/reverse ±2 per cycle, the three silent-zero failure modes each asserted to yield zero counts *and* to remain diagnosable through the edge counters, and an ISR-driven end-to-end path to a non-zero `v_actual`. **Cross-check CLOSED on hardware (2026-08-16, same day):** one hand-turned flywheel revolution reads `encoderPos == 240`, the first reading of the counter ever taken on this board. Two independent sources now agree on the constant (physical slot count, firmware counter), and the same reading independently confirms the ×2 decode factor, that both optical channels are alive and genuinely in quadrature, and that the pins-14/15 bodge is correctly reconciled. Scope: counts per flywheel *revolution* only — `FLYWHEEL_RADIUS_M` and its coupling are untouched by a revolution count, and were resolved separately (above). Pin coverage added in the same round: `ENC_A == 14` / `ENC_B == 15` pinned literally, `ENC_ENABLE` asserted undefined, and a collision sweep of `ENC_A`/`ENC_B` against every other assigned pin — load-bearing because the rest of the suite drives the ISRs through these same macros, so a wrong pin number is self-consistent everywhere else and nothing else would fail. **1668 production + 175 bench pass.** | *(pending first flash)* |
| 9 | 2026-08-16 | **Manual SD logging from State 98 — `K` takes an argument. No control-path, sequencing, pin, UDP-telemetry, BLG record or BLG trailer change.** Until now the 1 kHz bench log could only be opened by a *profile* start (`R`/`T`/`D`/`Y`/`W`), so an operator driving the car by hand (`A`/`V` plus switch toggles) had no capture path finer than the 50 Hz `L` stream. New grammar at the `K` prompt: an **empty line** prints the logger status exactly as the pre-v9 bare `K` did; **`1`** opens a manual log and starts sampling; **`0`** requests its close; **anything else rejects the whole line** (`ERROR: K takes 0 or 1 (or empty for status)`) — the no-trailing-junk discipline the `T` parser adopted in fw v3, because a partially understood logging command leaves the operator believing a run is captured when it is not. Because the empty line is *meaningful* here, `PEND_K_PARAMS` is dispatched ahead of `handlePendingInputChar()`'s shared empty-line cancel, like `PEND_Y_PARAMS`/`PEND_W_PARAMS`. New `LOG_TYPE_MANUAL` = **0x08** and filename prefix **`MLnnnn.BLG`**, slotted into `logNextFileName()` after the two combined-mask tests (keeping the "combined masks first" discipline) and added to the `knownPrefix` scan so `ML` files share the session-monotonic counter. **The header's `profile_type` field is a bitmask and simply gains bit 3** — that is what it was for; `tools/decode_benchlog.py` passes `profile_type` through raw, so only its header docstring changed. Manual runs commit no profile parameters, so the v4 header's `hdr[7]` param-valid flags and both floats stay 0. `logSampleTick()` is untouched: with no profile flags set the phase bytes read `LOG_PHASE_NONE` and `flags` bit0 follows `powerBalanceLive`, which is correct for a hand-driven run. Ownership is one flag, `logManualActive`, cleared at **every** point the log ends — `logFinishFile()` (the single close bottleneck: profile force-finish, drain-complete, deadline abandon, IO-error close) and `logDrainTick()`'s `!sdAvailable` early-clear, the one path that clears the flags *without* closing. It deliberately survives `logRequestClose()` so the whole drain window still refuses a second `K 1`. `K 1` guards, each with its own message: refused while `bringupActive` (opening a file walks the directory and `preAllocate()`s 32 MB — a stall that must not land inside the millis()-deadline-sequenced bring-up; this is a **parse-time** guard because `K` cannot join the keypress lockout list without also killing the read-only status path), refused while any profile is active, a **`T` sweep is live in any phase** (safety review S1: between sweep runs no profile flag is set and the logger is idle, so an ML open there would be force-finished at the next run's start against a possibly-busy card — silently costing that sweep run its entire log, the exact loss the sweep's WAIT_LOG gate exists to prevent), or a plot arm is pending (the profile owns the logger), and refused while a log is already open or draining. A failed open (no card, scan failure, header write) prints its own diagnostic and leaves `logManualActive` false — ownership is claimed only if a file actually opened. A profile started over a live manual log takes the logger over through `logOpenForProfile()`'s existing double-open force-finish, which is the intended semantics. `X`, `Q` and `triggerFault()` already close whatever is open (`LOG_CLOSE_X`/`LOG_CLOSE_Q`/`LOG_CLOSE_FAULT`), so a manual log ends cleanly on every existing exit path with **no special case**; `K 0` reuses `LOG_CLOSE_STOP`, so no new trailer code and the decoder's known-reason check is untouched. Review round also added an ownership marker to `printSdStatus()`'s `active:` line — `(manual — K 0 stops)` / `(profile-owned)` while a log runs (safety S2: `K 0` refusals turn on ownership, so the status dump must show it). | *(pending first flash)* |
| 10 | 2026-08-16 | **Youla-H robust DRIVE (velocity) controller replaces the placeholder motor PI. No pin, sequencing, fault, BLG or UDP-telemetry change; `commandMotorCurrent()` remains the sole VESC writer **on the velocity path** (the `T` trapezoid and `W` profile still call `commandMotorCurrentLimited()` directly at the 25 A ESC rating, unchanged).** User-directed exception to the CLAUDE.md “do not change the PI controllers” rule, scoped to the velocity loop — the second such exception after the 2026-07-10 share-loop round. The motor PI it replaces was never tuned: its gains were `Kp = Ki = 1` placeholders, and no build before fw v8 was reading the encoder at all, so no build could have tuned them against a working `v_actual`. The new law is synthesized in `controller_design_MIMO/synthesize_drive_siso.py` (H-infinity mixed sensitivity, γ = 27.37, plus a Youla-H DC rescale so T(0) = 1 exactly, Tustin at Ts = 2 ms) against the **calibrated** drive plant `G22` (`controller_design_MIMO/calibration/motor_id_20260815.md`; the plant was re-identified 2026-08-16, so the synthesis is downstream of measured k_t, R_m, m_eff, r_t, τ_v and drag). (a) **New runtime `teensy_controller/drive_controller.h`** implementing the **Hanus self-conditioned** 5-state realization, NOT a biquad cascade: `u_unsat = CD·x + DD·e`, `u = clamp(u, ±12 A)`, `x_next = AC·x + BD·(u/DD)` — the *clamped* output drives the state update, so the FULL controller state de-winds. Integrator-only back-calculation (the shipped share controller's idiom) is **measurably wrong here**: the non-integral branch has 745.5 A/(m/s) of low-frequency gain and its lag states saturate independently of the integrator, leaving a −0.48 m/s standing error and a 22.2 mm/s limit cycle on the 0→2 m/s step (it is clean only for steps below ~0.1 m/s). (b) **The state vector is `double`, deliberately.** The realization carries an exact integrator (eigenvalue at 1) plus an exact integrator (`AD[0][0] == 1.0`) alongside `CD` entries of order 50 (the realization's discrete max |z| is 0.9998; an earlier version of this row cited "a second mode at ~0.9999", which is stale against the fw v12 re-synthesis — the second section's poles are ~0.158), so perturbations are integrated rather than damped; a float32 recursion was **measured** to diverge ~1.4e-2 A at rail RELEASE on the saturated regen episode (`validate_drive_siso.py` check 4), which shortening the run does not bound. Coefficients stay the shipped float32 values (the replay reference vectors were generated from exactly those roundings) and are promoted per operation. Cost is ~35 double MACs per tick (5 for the output, 30 for the 5×5 state update) at 500 Hz — negligible on the Teensy 4.1 FPU. (c) **New generated header `teensy_controller/drive_controller_coeffs.h`.** `synthesize_drive_siso.py` now emits the firmware copy and the study copy (`controller_design_MIMO/drive_siso_coeffs.h`) from **one emitter**, so the two cannot drift; only the top banner differs. Never hand-edit either — regenerate. (d) **Units: `motorConstant` is bypassed on the Youla path.** The controller maps velocity error [m/s] to motor current [A] directly, because the synthesis plant already carries the k_t/r_t/m_eff chain; dividing by `motorConstant` would rescale the loop gain by 1/`motorConstant` and void every synthesis gate. `motorConstant` (still an uncalibrated TODO) now survives only on the PI fallback. (e) **`USE_YOULA_DRIVE_CONTROLLER`** (default 1, `#ifndef`-overridable, mirroring `USE_YOULA_SHARE_CONTROLLER`) selects the law; at 0 the fw ≤ v9 PI path compiles verbatim and behaves identically. Independent of `BENCH_TEST` and of `velocityChainCalibrated()` — the Youla path sits behind the same `V`/`D`/`Y` calibration gates as before. (f) **Clamp pairing:** `DRIVE_CTRL_I_MAX` is synthesized to equal `MOTOR_I_CMD_MAX` (12.0 A), so the clamp the anti-windup conditions against is the clamp `commandMotorCurrent()` enforces downstream; the downstream clamp becomes a redundant backstop rather than the binding limit. A future `MOTOR_I_CMD_MAX` change requires **re-synthesis** (`I_CLAMP` in the script), not an edit to the constant alone — the anti-windup boundary scales with the clamp. This is now a **compile-time tripwire**: `MOTOR_I_CMD_MAX` became `constexpr` and a `static_assert` in the `.ino` fails the build if it ever diverges from `DRIVE_CTRL_I_MAX`/`I_MIN`. The emitter was changed to declare those two coefficients `constexpr` for the same reason — a namespace-scope `const float` is not a constant expression in C++17, so the guard would not compile against one. Values are unchanged; both headers were regenerated (script exit 0, ALL GATES PASSED). (g) **Reset sites.** `resetDriveControlState()` (states + held output + a back-dated Ts gate) is called from `haltMotorOutput()` — the single primitive that covers every velocity-path profile start (`D`/`T`/`Y`/`W` all call it immediately before their `resetShareControlState()`) and every stop, `Q` exit, `X` universal stop and natural completion; from `setManualMotorVelocity()` on the **entry edge only** (a re-typed `V` during a live manual-velocity run is a setpoint *step* the loop must answer from its operating point — resetting there would discard that operating point and restart the transient: the first tick is `DD·e` alone, 1.81 A per m/s, but the non-integral branch's 745.5 A/(m/s) low-frequency gain then **rails the command at ±12 A within roughly 20–40 ms** for any error above ~16 mm/s, so the cost is a full-rail excursion, not a gentle coast); and on **Idle→Run**, where `v_setpoint` is zeroed alongside it. The Idle→Run reset deliberately **diverges from the share loop**, which is pointedly not reset there: the MDAC droop ratio is physically held across Idle so the share loop resumes, whereas Idle holds the motor at `setCurrent(0)` and can last minutes while the machine coasts to a stop, making stored `i_cmd` history a live current command derived from a machine state that is no longer true. **The reset alone is not sufficient**, which is why the setpoint is zeroed with it: `doState3()` (Finish) does not clear `v_setpoint`, so the Pi's last commanded speed survives Run→Finish→Idle, and a freshly reset controller meeting a stale non-zero setpoint sees the full error on its first tick — the 745.5 A/(m/s) low-frequency gain then rails the command at ±12 A within roughly 20–40 ms for any error above ~16 mm/s, so Run entry would begin with a full-rail current excursion nobody commanded. Zeroing costs at most one tick of tracking (the Pi rewrites `v_setpoint` within one packet period, and a Run genuinely meant to move starts from 0 m/s anyway). Deliberately **not** added beside the two `resetShareControlState()` calls that lack a `haltMotorOutput()` — the `R` profile start and the `P` operator setpoint — since neither drives the velocity loop and a reset there would zap a live `V` run the operator did not stop. (h) **Wrapper gate tolerance.** `youlaController_Drive()`'s internal Ts gate fires at `DRIVE_CTRL_TS_US − DRIVE_CTRL_GATE_TOL_US` (2000 − 200 µs). The outer gate (`rl_motor_last` / `MOTOR_CTRL_PERIOD_US`) is nominally the same 2 ms period, and two equal-period gates on the same clock beat: an outer tick arriving 1 µs early leaves the inner gate one microsecond short, the update is skipped, and the output holds 4 ms instead of 2. Benign (the design delay margin is 54 ms) but nondeterministic — it would make the effective sample rate depend on jitter. The margin absorbs the beat while still bounding a hypothetical *ungated* future caller to 1.8 ms, which is the inner gate's only real purpose. **Bench precondition unchanged and still open:** the VESC Battery Current Max / Regen Max are still unset (`docs/VESC_MOTOR_INTEGRATION.md` §4, open since fw v7), and this round changes the control law feeding that unbounded path — treat the first `V` run as gain identification, scope-armed, and prefer `A`/`T` (velocity-loop-free) first. | *(pending first flash)* |
| 11 | 2026-08-16 | **BLG RECORD FORMAT v5 — drive-controller internals in the SD bench log. Observability only: no control-law, pin, sequencing, fault or UDP-telemetry change (the Pi packet stays v4 / 58 B), and the drive controller's update law is byte-for-byte untouched.** The first closed-loop velocity runs are imminent and the drive loop is the least observable thing on the board: the record's `I_cmd` is *post*-clamp, so a transient spent on the ±`MOTOR_I_CMD_MAX` (12 A) rail decodes identically to one that merely asks for exactly the limit, and the controller state that produced it appears nowhere at all. **Layout:** two little-endian float32 are **appended at the END** of the record — offset 68–71 `u_unsat` [A], 72–75 `drive_x0` — so **every v1–v4 field keeps its offset** and the record grows 68 → 76 B (`LOG_REC_SIZE`). Header `hdr[4] = 5`; header layout is otherwise **identical to v4** (param-valid flags at 7, `fwVersion` at 18–19, Imax/Vmax at 20–23, b at 24–27, reserved 28–31) — only `hdr[4]` and `hdr[5]` (the record size, which decoders already read from the header) differ. Trailer unchanged. **Semantics, both builds:** `u_unsat` is the PRE-clamp motor-current command and `drive_x0` the integrator state. Youla build (`USE_YOULA_DRIVE_CONTROLLER=1`) → `driveCtrl_uUnsat`, a new file-scope capture in `drive_controller.h` written immediately before the clamp inside `driveControllerStep()` and zeroed by `driveControllerReset()` (hence by `resetDriveControlState()`), and `driveCtrl_x[0]`, the EXACT integrator (`AD[0][0] == 1.0`) — together they show the Hanus self-conditioning working, or a rail episode, from the log alone. PI fallback build → `targetMotorTorque/motorConstant` and `pi_motor_accum`, the same two roles, so an A/B flash logs **comparable** signals. **`flags` gains bit4** (this record's motor command came from the Youla DRIVE controller) **and bit5** (the share loop is the Youla SHARE controller); bits 0–3 unchanged. Both are compile-time constants, stamped per record anyway so a decoded run is self-identifying without cross-referencing this ledger. **Sampling relationship:** the drive controller steps at 500 Hz (`DRIVE_CTRL_TS_US`) while `logSampleTick()` samples at 1 kHz, so the log carries **pairs of identical `u_unsat`/`drive_x0` samples per controller tick by design** — the same zero-order hold `youlaController_Drive()` applies to its output. The logger **exposes the stored value and never re-evaluates** the output equation, which would run against an already-advanced state and would not be the value the clamp acted on. **Ring/bandwidth re-checked at 76 B/ms:** the ring stays 1024 records = 77824 B (76 KB, still DMAMEM, 1.024 s of coverage); `logDrainTick()` floors a 512 B chunk to 6 whole records = 456 B, i.e. 456 B/ms drained against 76 B/ms filled (6.0× catch-up, was 7.0× at 68 B), and the 32 MB preallocation covers ~7.4 min (was ~8). **No buffer constant needed to grow.** Logging stays observability-only — never faults, no card I/O in the sampling path. Decoder/analysis tooling updated in lockstep (v1–v4 files decode byte-identically). **Bench precondition CLOSED:** the VESC Battery Current Max (6.0 A forward) and Regen Max (1.5 A) were **set on 2026-08-16** and recorded in `docs/VESC_MOTOR_INTEGRATION.md` §4, closing the fw v7 S1 open precondition that fw v8–v10 carried. Regen matches the §12.4 derivation exactly; forward bounds the bus at **1.43× the derived 4.2 A allowance and above the 5.4 A conditional ceiling whose scope validation has not happened**, and a 6.0 A bus draw split evenly is exactly `LIMIT_I_BT_MAX` = 3.0 A/channel with zero margin (an FC-heavy share setpoint exceeds `LIMIT_I_FC_MAX` = 1.4 A) — with the OC faults compiled out under a `BENCH_TEST` flash, so nothing in firmware catches either. **Outstanding: re-derive §12.4 against 6.0 A, or lower the setting, before a vehicle run.** This round also adds `resetDriveControlState()` to `setManualMotorCurrent()` (the State-98 `A` path): entering current mode abandons the velocity loop, and without the reset a `K`-logged `A` run following a `V`/`D` run would log the previous run`s frozen `u_unsat`/`drive_x0` against an unrelated `I_cmd` with flags bit4 set. UNCONDITIONAL, unlike `setManualVelocity()`s entry-edge reset — in current mode the controller is never stepped, so there is no operating point to preserve. The first `V` run is still gain identification against a never-tuned velocity loop — scope-armed, `A`/`T` (velocity-loop-free) first. | *(pending first flash — fw v10 was never flashed either, so the first flash carries v10 + v11 together)* |
| 12 | 2026-08-16 | **Edge-period velocity estimator replaces the position-buffer boxcar. Root-cause fix for the ML0136–ML0139 limit cycle. No pin, sequencing, fault, BLG-format or UDP-telemetry change; the drive controller's update law is untouched.** **Root cause:** the four first closed-loop `V` runs (ML0136–ML0139) limit-cycled at the drive controller's 16 rad/s design crossover. `updateWheelSpeed()` differenced `encoderPos` across a ring of position/timestamp samples pushed **once per main-loop iteration** (~1.13 ms) over ~98 slots — a **~113 ms boxcar with ~56 ms of group delay**, which at 16 rad/s is ~51° of lag against the synthesis design's **49.6° phase margin**. The entire margin was consumed by a block the synthesis plant never contained; its quantisation was one count per window, **0.0177 m/s**. **The fix:** `updateWheelSpeed()` now measures the **period between consecutive A-rising edges** — one edge type on one channel, so each interval spans exactly one full slot pitch (`ENC_SLOT_PITCH_M` = 2π·`FLYWHEEL_RADIUS_M`/`ENCODER_SLOTS_PER_REV` = **3.990 mm** of rim travel) and the two OPB829DZ sensors' threshold/duty asymmetry cancels instead of appearing as per-edge ripple. `v = dir · N·pitch / Σ(last N periods)`, `N = ENC_PERIOD_AVG_N` (configurable, **default 2**). **ISR:** the tap sits at the end of `doEncoderA()` and reuses the pin levels the quadrature decode already read (`pinA_read == 1` *is* the rising edge on a CHANGE interrupt), adding no `digitalRead`; at 5 m/s the edge rate is 1.25 kHz against ~800 µs spacing, so the added subtraction/compare/stores are negligible. **The ×2 decode, `encoderPos` and `encEdgeCountA/B` are untouched** — `encoderPos` stays the position/direction truth and the estimator takes its **sign** from it (Δ`encoderPos` per cycle is +2 forward, −2 reverse); a direction flip or an ambiguous zero delta **invalidates the ring** rather than averaging across it. **Reader:** consistent ring snapshot under a brief `noInterrupts()` section (chosen over a sequence-counter retry — the copy is far shorter than the 200 µs minimum edge interval, so no edge can be lost); the old per-loop `sampleTime` gate is gone. **Zero speed:** standstill when the time since the last accepted edge exceeds `max(1.5 × last period, ENC_VEL_TIMEOUT_US = 150 ms)`, flooring the reportable speed at 3.990 mm / 150 ms = **0.027 m/s** — below ~0.03 m/s `v_actual` reads exactly 0. Boot/post-reset read 0 until N periods accumulate; all timing uses the file's unsigned-subtraction idiom, so `micros()` wrap is a non-event. **Glitch rejection:** intervals below `ENC_PERIOD_MIN_US` = 200 µs (≈20 m/s, 4× the fastest command) are dropped **without advancing the period base**, so the next genuine edge still spans a full pitch. **Plant interface (documented at the estimator):** the reading is the average speed over the last N·pitch of travel, latched once per pitch → effective delay ≈ (N+1)·pitch/(2v), **speed-dependent**: ~3 ms at 2 m/s, ~12 ms at 0.5 m/s, ~48 ms at 0.125 m/s (N = 2). Quantisation is timer-limited (<0.1% above 0.2 m/s), **not** the old 17.7 mm/s ladder. Worst case is at LOW speed, so margin checks belong at the slowest tracked speed. **Deleted:** `posArr`/`timeArr`, `averagingTime`/`arraySize`/`BUF_DEPTH`, the buffer-depth `static_assert` and the boxcar's `lastMicros`/`index` statics — all function-local, so nothing outside `updateWheelSpeed()` referenced them. `wheelSpeedResetPending` survives with its name and its single producer (`doState3()`) and now calls the new **`encoderVelReset()`** (clears ring, period base, position reference, direction latch), which also runs on a stale timeout. The State-98 `S` dump gains a ring line (`periods=n/N last=…us dir=… pitch=…mm`) because the estimator adds a second silent-zero source beyond "no counts". **Trace comparability:** BLG `v_act` is the same field carrying finer values — fw ≤ 11 traces are comparable in **scale** but not in **dynamics** (they carry the 113 ms smoothing; fw 12 does not). **Controller re-synthesised in the SAME round — a v12 trace vs a v11 trace is TWO DIFFERENT CONTROL LAWS, not just two estimators.** `controller_design_MIMO/` was re-run against a plant that now contains this estimator: `K_v` recentred to 1.25, weights `WC = 60` / `Wu(0.25, 300, 12.5)`, γ = 33.07, `DRIVE_CTRL_KI` 73.63 → 53.42, both SOS sections changed, and `teensy_controller/drive_controller_coeffs.h` regenerated in this same diff. Do not attribute a v11→v12 behaviour difference to the estimator alone. **Validity floor — the "root-cause fix for ML0136–139" claim is QUALIFIED:** the design is gate-checked for **v ≥ 0.5 m/s only**. This estimator floors at 0.027 m/s and needs 3 A-rising edges (~N+1 pitches, ~12 mm of travel) before it produces any reading at all, so a commanded step BELOW 0.5 m/s closes the loop around a deadband relay and is **expected to limit-cycle** — that is a validity boundary of the design, not an estimator defect. The ML0136–139 runs at 0.1 m/s therefore are NOT covered by this fix; only the ≥ 0.5 m/s portion of that failure mode is. **Ring-invalidation hold (safety review, MED-HIGH):** a ring invalidated mid-motion (direction flip, or the ISR's ambiguous zero-delta case — one noisy B sample suffices on the un-Schmitted OPB829DZ) HOLDS the last valid reading while it refills rather than reporting 0; a 0 at speed is a full-scale error step into a 544.8 A/(m/s) controller for N edge intervals (4 ms at 2 m/s, 16 ms at 0.5), a failure class the old boxcar could not produce. `v_actual` is forced to 0 on exactly three events — boot, `encoderVelReset()`, and the stale timeout — and the hold is bounded by that timeout (evaluated *before* the ring-depth test for exactly this reason). **Next bench:** re-run the ML0136-class `V` step scope-armed, at **≥ 0.5 m/s**, and treat the first fw v12 run as a margin check of the new law + new estimator together, not a tuning run. | *(pending first flash — v10/v11 were never flashed either, so the first flash carries v10 + v11 + v12 together)* |
| 14 | 2026-08-16 | **Drive-controller coefficient regeneration following an upstream `K_F` force-axis correction. No firmware logic change whatsoever** — no pin, sequencing, fault, telemetry, BLG-format or command change, and no edit to any control code. The only firmware deltas are the regenerated `teensy_controller/drive_controller_coeffs.h`, the `FW_VERSION` bump, and two stale comments that quoted the old gear ratio. **Upstream cause:** the design plant's force chain carried the wrong gear ratio *and* the wrong radius. `φ` **9.49 → 6.86** (the 9.49 was a stock-gearing figure; the fitted pinion is 29T against a counted 70T spur — Traxxas 4-Tec manual p.24 formula gives 6.88, the manual's chart cell 6.87, operator rolling counts 2.84–2.86 for the shaft/tire stage), and the FORCE radius **0.0762 → 0.033 m**: torque acts through the gearbox on the **tire**, while the encoder and the inertia belong to the **flywheel**, and the two had been conflated. Net `K_F` 0.4516 → **0.7538 N/A (×1.669)**. The drag law rescales with it (`b_eff` 0.32 → 0.534 N·s/m, `F_c` 1.2 → 2.00 N) because it was derived FROM hold currents THROUGH `K_F`, so `i_m0` = 4.07 A is invariant. The old ×2 ramp-vs-cruise gain contradiction **dissolves** and **`m_eff` = 3.5 kg is confirmed** — the 1.6–2.4 kg inferences were `F/a` fits through the understated axis, closing the fw v13 freeze. `K_v` re-centred 1.25 → 1.00, corners → {0.75, 1.00, 1.35}. Re-synthesis needed **no weight change**: the shipped rung (WC = 60, Wu(0.25, 300, 12.5)) passes every gate — crossover 17.52 rad/s, PM 50.8°, DM 50.6 ms, worst ‖S‖∞ 2.427 cont / 2.535 disc over 72 corners, PM 41.8° at the 0.5 m/s floor. ⚠️ **A v14 `'V'` trace is NOT comparable with a v13 one: the control law changed** (new coefficients, new `K_I`, new plant gain — the loop is ×1.34 stiffer in DC plant gain). The VELOCITY chain is untouched — `FLYWHEEL_RADIUS_M` 0.0762 m, 240 counts/rev and the estimator are all unchanged, so `v_act` traces ARE comparable across v13/v14. `MOTOR_I_CMD_MAX` unchanged at 12 A. Sources: `controller_design_MIMO/calibration/motor_id_20260815.md` §"K_F force-axis correction (2026-08-16c)", `mimo_system_model.md` §4.2/§4.4, `drive_siso_metrics.txt`. **Replay-tolerance pipeline fix (same round, no firmware effect):** the regenerated coefficients failed `test_drive_controller_replay_regen` at **86.27 mA** against a **hardcoded 50 mA** gate inherited from the fw v12 coefficients. Characterised as benign clamp-boundary dither, not divergence — deviation is confined to the saturated transient (k ≈ 213–442 of 2500), decays to **1.64 mA** over the last 200 samples, and the unsaturated `'small'` episode replays **bit-exactly**. Root cause of the 25 mA generation-time gate passing while C++ measured 86 mA: the gate perturbed only the *stimulus* while keeping numpy/BLAS summation order, and the **dominant** perturbation is the **arithmetic order** of the dot products — `driveControllerStep()` accumulates `Cd·x`/`Ac·x` with sequential scalar loops, worth **67.68 mA on its own** versus 19.07 mA for float32 stimulus storage, because the trajectory approaches a clamp decision boundary to within **255.9 µA**. Fix: `synthesize_drive_siso.py` now replays the episode through the firmware's exact arithmetic, measures all four sensitivities, derives the tolerance as 2× the worst rounded up (**180 mA** this round), interpolates every figure into the CSV/coefficient-header/metrics comments (all stale literals removed), and gates on non-divergence + tail decay; the tolerance ships machine-readably as `DRIVE_REPLAY_{SMALL,REGEN}_TOL_A` via `tools/gen_drive_replay_header.py`, which the replay tests now read instead of hardcoding. **Tests: 2860 production + 175 bench pass** (+2: generated-tolerance sanity and tail-decay checks). `validate_drive_siso.py` 15/16, the documented float32-state exception. | — (not yet flashed) |
| 13 | 2026-08-16 | **Edge-corruption hardening of the fw v12 edge-period estimator, plus the `v_setpoint` zero cutoff. No controller-coefficient, plant-model, telemetry or BLG-format change; no pin, sequencing or fault change.** The `controller_design_MIMO/` artefacts (coefficients, plant model, `m_eff`, `K_F`) are **frozen** this round pending a separate operator investigation — the operator's ruling is that `m_eff = 3.5 kg` is a *floor* from the flywheel inertia measurements, and the observed gain discrepancy most likely lives in `K_F`. **Root physical cause of all three estimator defects** (scope capture 15, `references/scope_captures/`): the OPB829DZ is a bare phototransistor with a 4.7 kΩ pull-up and **no hysteresis**, so the channels present full-swing signals with ~0.5–1 ms *analog* edge ramps and the Teensy input re-crosses its threshold on ramp noise — producing both extra and swallowed edges. **A Schmitt-buffer hardware bodge is the real fix and is planned separately; this round is the firmware backstop**, and every mechanism below stays correct and beneficial after the Schmitt lands (the filters simply stop finding anything to reject). **(a) Adaptive period plausibility** (`ML0145`, `ML0143`). fw v12's glitch floor was a fixed `ENC_PERIOD_MIN_US` = 200 µs (≈20 m/s) against real bench periods of **0.8–11 ms**, so it caught nothing: ML0145's spurious A-edges split one pitch into two multi-millisecond halves, putting `v_act` on rungs at **1.33× and 2× true speed** and reaching **100 % contamination at low speed** — where the trace looks perfectly clean while reading uniformly double. The test is now **relative** to an EWMA (α = 1/4) of accepted per-pitch periods (`encPeriodRefUs`), armed after `ENC_PERIOD_REF_SEED_N` = 2 accepted periods. *Low side:* a period below **0.625 × ref** (shift-only form of the ~0.6 design threshold) is a spurious-edge artefact and is dropped **without advancing the period base**, so the next genuine edge spans the full true pitch and the two halves **merge**. Physics bound: the per-pitch fractional period change under acceleration is `a·pitch/v²`, a few percent at any speed where the ring is populated, so genuine consecutive periods cannot approach halving. *High side* (ML0143's missed edges, ~68 events/s at 3 m/s, speed/current-independent, trimodal `v_act` at 1, 2/3 and 1/2 of true speed): a period near `k × ref` means **k pitches were traversed**, so it is **interpreted, not rejected** — the ring entry stored is `period/k`, the mean per-pitch period, which is the correct distance/time for an unchanged reader. `k` is nearest-integer against shift-built 1.5×/2.5×/3.5× midpoints and **bounded at `ENC_PERIOD_MAX_MULT` = 3**; beyond 3.5× ref the period is accepted as-is, because genuine strong deceleration is the likelier explanation and reading slow is the safe direction. Rejecting long periods instead would starve the ring during exactly the decel where the loop needs a reading. **Speed arming (safety review S1 + S2) — the mechanism is deliberately incomplete at low speed.** The entire adaptive mechanism (low-side gate *and* `k > 1` reinterpretation) is armed **only while `ref < ENC_ADAPT_MAX_REF_US` = 13000 µs**, i.e. above ~**0.307 m/s**. Below that, the *true* per-pitch ratio `T(n+1)/T(n) = 1/sqrt(1 + 2·a·pitch/v²)` crosses both thresholds on **genuine** rail transients: it falls under the 0.625 gate when `v² < 1.667·a·pitch` (v < 0.22 m/s at a = 6.96 m/s², so a hard launch has its **true** edges rejected, the merged interval is then accepted as k = 1, and v reads m× **low** while dragging `ref` up — recovering only via the 100 ms reading-age reset), and it exceeds the 1.5× k = 2 midpoint when `v² < 3·a·pitch`, which can drop `ref` into the **k = 2 basin** (0.40T–0.667T) where every genuine period is halved and **re-confirms the poison** — v reading exactly **2× forever on a clean trace**, in the destabilising direction (reads fast → brakes harder). The arm speed is set by the stricter (decel) condition, `v_arm = sqrt(3·a_max·pitch)` = 0.289 m/s, with ~6 % margin. **Stated plainly: the 0.04–0.30 m/s regime is unmitigated by firmware** — which is exactly where ML0145's contamination was worst. That is not an oversight: in that band a spurious edge and a hard-launch truth produce the **same period ratio**, so no period-ratio filter can separate them. **That band belongs to the planned Schmitt-buffer hardware fix**, which removes the spurious edges at the source. **Poisoned-reference tripwire (S1 backstop):** `ENC_KBRANCH_RUN_MAX` = 4 consecutive accepted periods all taking the `k > 1` branch raise `encRefPoisonPending`, and `updateWheelSpeed()` performs a full `encoderVelReset()` — a genuinely persistent miss stream is indistinguishable from a poisoned reference and a reset is correct for both. The reset runs in the **reader**, not the ISR, because it writes reader-side hold state (documented deviation from the review's wording; effect is identical within one ~1.13 ms main-loop tick). `ENC_PERIOD_MIN_US` is **kept** as the absolute floor beneath the adaptive one, and is the bootstrap's only protection for the first two periods off standstill (documented breakaway caveat; self-correcting within a few pitches and capped by (b)). **Delay model unchanged:** a rejected edge never advances the base, so accepted periods still span *true* pitches and a clean edge stream still measures the nominal `(N+1)·pitch/(2v)` — the filter costs the plant model nothing. **(b) Reading-age stale bound** (`ML0140`, `ML0145`). fw v12's stale timeout was keyed to the age of the last raw **edge**, so it could never fire in the case that mattered: under direction dither the ISR's `dpos == 0` and flip branches re-invalidated the ring **faster than it refilled** while edges kept arriving fresh, and the reader held a stale — at a flip, **wrong-signed** — value for **120–560 ms** (ML0145 shows a **468 ms frozen hold** at breakaway, ~0.4 m/s). `updateWheelSpeed()` now also bounds the age of the last accepted **reading** (a completed `v` computation) by `max(ENC_VEL_STALE_K × that reading's period sum, ENC_VEL_TIMEOUT_US)`, zeroing `v_actual` and resetting the estimator when exceeded, **regardless of the raw edge stream**. Holds *shorter* than the bound behave exactly as in fw v12. `ENC_VEL_TIMEOUT_US` drops **150 ms → 100 ms** now that it is actually reachable, raising the reportable-speed floor to 3.990 mm / 100 ms = **0.0399 m/s** — deliberately kept **below** `V_SP_ZERO_THRESH`; that ordering is load-bearing and must survive any future edit to either constant. **(c) Reversal as data** (`ML0140`). A confirmed flip already stores the flipping edge's own period, so one valid same-direction period exists immediately; fw v12 nonetheless waited for a full ring. The reader now averages over **whatever the ring holds** (`cnt`, not a fixed `N`), so a signed reading is available after the **first** full same-type period in the new direction, returning to `N`-averaging as the ring fills. Cost: a `cnt = 1` reading is a single-pitch measurement, `N`× less averaged — **accepted**, because a noisier reading of the right sign beats a clean one of the wrong sign. **Sign embargo (safety review S3):** a `cnt = 1` reading whose **sign differs** from the last published one is **held**, not published, until `cnt ≥ 2` corroborates the reversal; sign-*matching* `cnt = 1` readings stay allowed, preserving the fast warm-up and the same-direction partial-ring value. Without it the ML0140 dither publishes a **sign-alternating single-pitch reading at the dither rate** — a *stronger* excitation into the 544.8 A/(m/s) controller than the frozen hold it replaced — and, decisively, each publication refreshed `encVelLastReadingUs`, so (b) could **never** age the dither out. An embargoed tick therefore must **not** touch `encVelLastReadingUs`. **Correction to an earlier draft of this row:** (c) alone does *not* terminate the ambiguous `dpos == 0` / dither starvation case — it is (b) **plus the embargo's refusal to refresh the reading timestamp** that does, and only because embargoed ticks let the hold genuinely age. **(d) `v_setpoint` zero cutoff** (`ML0144`). Commanding `v_setpoint = 0` *through* the velocity loop is closing the loop **below its own validity floor** — the estimator reads exactly 0 under ~0.04 m/s, so the loop wraps a 544.8 A/(m/s) controller around a **deadband relay**. ML0144 recorded sustained **90 %-rail ±12 A bang-bang**; the same run at 1.0 m/s settled cleanly. `motorControl()` — **both** build paths, Youla and the PI fallback — now commands 0 A and holds the controller reset while `|v_setpoint| < V_SP_ZERO_THRESH` = **0.05 m/s** (above the 0.0399 m/s estimator floor, far below the 0.5 m/s slowest gate-checked synthesis speed), skipping the controller step entirely. The reset fires on the **entry edge** only, so the exit is already clean under the existing reset semantics. This makes State 2 and the State-98 profiles behave the way `doState1()` (Idle) already did — Idle commands 0 A directly rather than regulating a zero setpoint. **Behavioural consequence for the profiles:** segments commanding genuine zero speed now **coast at 0 A** instead of being actively regulated to a standstill the estimator could not observe anyway — `DRIVE_CYCLE` phases 0 and 5 (standstill) plus the tails of phases 1 and 3, and `COMBINED_PROFILE` regions 0 and 15 plus the ends of regions 1 and 14. The regen hold (−0.5 m/s) and every other waypoint are above the threshold and unaffected; at the default `Y_VMAX_DEFAULT` = 1.0 m/s the smallest non-zero normalised waypoint is 0.2 m/s, 4× the cutoff, so no `Y` plateau is clipped. **Safety review S4:** on the PI-fallback build `pi_motor_lastMicros` is refreshed on **every** cutoff tick (the accumulator clear stays entry-edge only) — a frozen `dt` reference would make the first tick after the setpoint rises integrate the *entire* cutoff duration in one step and slam the integrator to `integMax`, turning the cutoff into a windup source instead of a reset. **Safety review S5:** `'Y'` warns at parse time when `Vmax × 0.2` (region 13, the table's smallest non-zero waypoint) falls below `V_SP_ZERO_THRESH`, so a deliberately slow bench run's flat 0 A trace is not misread as a controller failure; accepted with a warning, not refused. **Safety review S6:** `static_assert`s now pin the estimator-floor `<` `V_SP_ZERO_THRESH` ordering (`V_SP_ZERO_THRESH` promoted to `constexpr` for it) and tie `ENC_PERIOD_LO_FRAC` to the `(ref>>1)+(ref>>3)` shift form it documents. **Next bench:** re-enter the ML0143/ML0145 conditions scope-armed and confirm the `v_act` rungs (1.33×, 2×, 2/3, 1/2) are gone and that no *genuine* accel/decel transient is being filtered into a frozen reading; re-enter the ML0140 dither case and confirm every hold now terminates within ~100 ms; re-run ML0144's `v_sp = 0` segment and confirm 0 A coast in place of the bang-bang. The Schmitt bodge remains the root fix — this backstop is not a reason to defer it. | *(pending first flash — v10–v12 were never flashed either, so the first flash carries v10 + v11 + v12 + v13 together)* |
| 15 | 2026-08-17 | **`dpos`-based pitch count in the edge-period estimator ISR — the estimator's last ratio *inference* becomes a decoder *measurement*, and both reference-poison basins are eliminated. No pin, sequencing, fault, UDP-telemetry (v4/58 B), BLG-format (record v5/76 B) or controller-coefficient change; the entire reader side (`updateWheelSpeed()`, the holds, the sign embargo, the reading-age bound) is untouched and the estimator's `(N+1)·pitch/(2v)` delay model is unchanged, so `v_act` traces stay comparable with fw v12–v14.** **Defect.** The fw v13 adaptive filter had an **absorbing slow-reading basin at `ref ≈ 2T`** (`T` = true per-pitch period). Every genuine edge then arrives at 0.5 × ref, fails the 0.625 low-side gate and is rejected **without advancing the base** — which is the *correct* handling of a real spurious edge and is what makes the ML0145 split pair merge. The next genuine edge therefore measures `2T` from the un-advanced base, i.e. ratio 1.0 against ref, and is accepted as a **single** pitch; the EWMA is fed `2T` and re-anchors the basin. `v_actual` reads **exactly half** true speed indefinitely **on a clean trace**, so the drive controller sees double the speed the car is making. The `ENC_KBRANCH_RUN_MAX` tripwire could not catch it: it counted *consecutive `k > 1` acceptances*, and in this basin every acceptance is `k == 1`, which **reset** the counter — it only ever guarded the mirror-image `ref ≈ T/2` fast-reading basin. Entry is cheap: a missed-edge burst that trips any reset re-seeds `ref` with **no ratio gating at all** (`ENC_PERIOD_REF_SEED_N` = 2 accepted periods), so a reset taken mid-burst can seed straight into the basin and lock when the burst ends. **Bench motivation:** an abrupt apparent **2× speed-up** observed 2026-08-17, and the ML0151 t ≈ 27.5 s "speed collapse" of 2.55 → 1.30 m/s (**ratio 1.96**) is a candidate instance of the same mechanism — noting that the ML0151 drag step-change reading is *not* thereby retired, since the two explanations are separable by bus input power, which did change. **Fix.** `pitches = nearest-integer(|Δ encoderPos| / 2)`, minimum 1, computed as `(|Δ| + 1) >> 1` — shift/add only, no float, and the single pre-existing `period /= pitches` UDIV is unchanged, so ISR cost is unchanged. It is sound because a **rejected edge advances neither `encLastEdgeUs` nor `encPosAtLastEdge`**, so the position delta accumulates across rejections in lockstep with the period measurement: an accepted interval that really spans `k` pitches carries `|Δ| ≈ 2k` regardless of what the reference believes. Being a **count** rather than a ratio, it requires no reference and **no speed arming** — it runs during ref seeding and below `ENC_ADAPT_MAX_REF_US`, where the S1/S2 arming rationale (ratio ambiguity under hard accel/decel) simply does not apply. Both basins become non-stable: at `ref ≈ 2T` the `2T` interval counts as **two** pitches, stores `T` and feeds `T` to the EWMA, walking `ref` back to truth; at `ref ≈ T/2` the true period passes the gate and counts as one pitch, again feeding `T`. The seed-poisoning entry path therefore needs no separate hardening. **Removed:** the ratio-derived `k = 2/3` branch; `ENC_PERIOD_MAX_MULT` (the count is **uncapped** — a long rejection streak legitimately spans more than 3 pitches, and capping would store a per-pitch period knowingly *too long* for the distance actually travelled, i.e. read fast); and the `ENC_KBRANCH_RUN_MAX` tripwire with `encKBranchRun` / `encRefPoisonPending` and its `updateWheelSpeed()` consumer. The tripwire is not merely unused but **actively wrong** under the new mechanism: escaping the `2T` basin produces several consecutive `pitches == 2` acceptances, so a run-length reset would fire during exactly the **recovery** it was meant to protect, discard the ring and re-seed from the same corrupted stream. Its removal also deletes the one path that injected a full-scale `v = 0` step into the 544.8 A/(m/s) controller on the basis of an *inference* rather than a measurement. **Kept as-is:** the 0.625 low-side gate and its no-base-advance merge (with its S1/S2 speed arming, now the only thing that arming governs — the threshold *value* is left at the S1-derived figure, which is the stricter of the two conditions), `ENC_PERIOD_MIN_US`, the EWMA, the direction handling and the `dpos == 0` ring invalidation, the ring, and every reader-side semantic. **Known residual:** if **both** edges of one slot are lost on channel A, the `Afirst*`/`Bfirst*` handshake loses that pitch's counts too, so `|Δ|/2` under-reads by one pitch and the interval is stored **slow**. That is the safe direction (reads low → the loop drives, it does not brake) and the EWMA absorbs it; the ratio cross-check that would have caught it is the very mechanism whose ambiguity created the basins, so it is deliberately **not** reinstated. The 0.04–0.30 m/s band and the underlying un-Schmitted OPB829DZ edge corruption still belong to the planned **74HC14 hardware fix**, which this round is not a reason to defer. **Observability (fw v8 lesson):** diagnostic-only `encLastPitches` and `encMultiPitchCount` are added and printed in the State-98 `'S'` dump alongside `ref`; no control path reads either. | *(pending first flash — v10–v14 were never flashed, so the first flash carries v10 + v11 + v12 + v13 + v14 + v15 together)* |
